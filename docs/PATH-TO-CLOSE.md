# Path to a close — unaugmented NS

16 September 2026.
**How we get a close. Not a close.
Ordinary NS is not solved.
Catalog B open stays 1.**

Operator asked for a close
and the path. A close is a
**seated inequality**, not a
scoreboard and not a prize
sentence.

Score: [`NS-STATUS.md`](NS-STATUS.md).
Plan: [`MASTER-PLAN.md`](MASTER-PLAN.md).
Tape: [`YES-NO-OPEN.md`](YES-NO-OPEN.md).
First write: [`ROUTE-A-WRITE.md`](ROUTE-A-WRITE.md).
Triangle geometry (identities,
then the missing \(K\)):
[`FOURIER-TRIANGLE.md`](FOURIER-TRIANGLE.md).
First lift:
[`TRIANGLE-LIFT.md`](TRIANGLE-LIFT.md).
Energy-class ladder (dead):
[`ENERGY-K.md`](ENERGY-K.md).
Pairing CS doors:
[`L-DOOR.md`](L-DOOR.md).
Instantaneous \(MN\):
[`MN-CANCEL.md`](MN-CANCEL.md).
First jet:
[`PATHWISE.md`](PATHWISE.md).

This desk is **unaugmented**.
A is not B. Keep \(1/r^4\).
No \(Q_1\). No \(\Phi\)-cancel.
No \(K(t)\) in the PDE.
Do not start leftover 1.
Do not weld \(\star\).

---

## What “a close” means here

Official Statement (B): smooth
divergence-free finite-energy
unforced data on \(\mathbb{T}^3\)
or \(\mathbb{R}^3\) stay smooth.

One of these two seated pages
is a close in the packaging
already built:

1. **Route B sits.** Useful
   \(K\in L^1_{\mathrm{loc}}\) in
   \(T_c\le\theta\nu\mathcal D_s+K(t)X\),
   \(\theta<1\). Then
   \(\Lambda'\le 2K\), hence
   \(X\le\|u\|_2^2\Lambda\) stays
   finite on the torus. That is
   an a priori on enstrophy
   **in this packaging**.
2. **Route A sits, then a written
   local-to-global arrow sits.**
   Pairing control of the local
   block plus far-shell Young
   moves leftover 5. That is
   **not** Statement (B) by
   itself. A later page must
   pass from the local block
   to global \(X\). That arrow
   does not sit today.

Route B is the close-shaped
target. Route A is first
because the local pairing is
exact, the death is cheap, and
leftover 5 is a real piece.
We do not skip A. We do not
pretend A alone closes (B).

A forced profile, a blowup
hunt, or Theorem A is not
this close.

---

## Gates

### G0 — freeze (DONE)

Theorem H. Exact-shell sweeps.
Designed 9D. Blowup-search as
the living line. Leftover 1.
\(\Phi\) / Q / Ring. Pólya
two pages. Unrestricted ★
as a bound.

### G1 — Route A write or kill (NOW)

Control the **stretch pairing**,
not \(\|(\alpha)_+\|_\infty\).

Exact identity already sits:
\[
T_{j\leftarrow j}
=\int\alpha_{\mathrm{loc},j}\,\lvert\Delta_j\omega\rvert^2
+T_{j\leftarrow j}^{\mathrm{comm}}.
\]
The crude majorant
\(\int\alpha\,\lvert\Delta_j\omega\rvert^2\le a_+ Z_j\)
is EXACT and is the wrong first
packaging: bounding \(a_+\) by
\(\|\nabla u\|_\infty\) is BKM.

Living target **A-pair**:
\[
\bigl(\textstyle\int(\alpha_{\mathrm{loc},j})_+\,\lvert\Delta_j\omega\rvert^2\bigr)
+(T_{j\leftarrow j}^{\mathrm{comm}})_+
\le
\varepsilon\nu D_j
+K_j(t)\,Z_j,
\]
with \(K_j\) useful (energy
class or a named dynamical
cancellation), not
\(\|\nabla u\|_\infty\), not
Bernstein \(2^{cj}\), not
tautological.

Page: [`ROUTE-A-WRITE.md`](ROUTE-A-WRITE.md).
C10: [`C10-CHAIN.md`](C10-CHAIN.md).

If A-pair dies, record the
failed arrow. Stop patching
C10. Leftover 5 stays OPEN.

### G2 — leftover 5 (only if G1 sits)

Energy-class remainder plus
the far Young that already
sits. Then
\(\int\rho_j<\infty\) on the
axisymmetric-with-swirl class,
or a named field with
\(\int\rho_j=\infty\).

### G3 — local block \(\to\) global \(X\) (only if G2 sits)

A written inequality. None
sits. Do not draw a conceptual
arrow. This is the missing
step from leftover 5 to
Statement (B).

### G4 — Route B write or kill (after G1)

Independent. Keep
\(T_c-\theta\nu\mathcal D_s\)
as one pairing. Hunt
cancellation from the moving
center. Test the remainder on
\(v_n\), amplitude, shears,
single shells.

Page: [`CENTERED-DRIFT.md`](CENTERED-DRIFT.md).
B★ hunt: [`BSTAR.md`](BSTAR.md),
[`BSTAR-PROOF.md`](BSTAR-PROOF.md).
No universal \(C\). Imag cutoff
grows \(R_B\). Not a useful \(K\).
Do not cash B★ as G5.
Energy-class ladder dead:
only \(p=1/2\) matches
amplitude, and \(v_n\) kills
the X, mid, and Y doors.
[`ENERGY-K.md`](ENERGY-K.md).
Pairing CS: tight is ★,
LE dead, LX not seated.
[`L-DOOR.md`](L-DOOR.md).
Instantaneous \(MN\) and
above/below \(\Lambda\) die
on \(v_n\) (\(N=0\),
\(R_{mn}=R_{ab}=1\)).
Signed vertices are not a
useful \(K\).
[`MN-CANCEL.md`](MN-CANCEL.md).
The \(t=0\) jet does not
seat a useful \(K\): Stokes
keeps \(N=0\), Euler
generates \(N\), \(T_c\)
grows.
[`PATHWISE.md`](PATHWISE.md).
Pathwise \(K\) on a
positive-time interval is
still the living G4 target.

Useful \(K\) or a named death.
Do not restore
\(\sup\mathcal R_\star<\infty\).

### G5 — torus close (if G4 sits)

\(\Lambda'\le 2K\) plus
\(X\le\|u\|_2^2\Lambda\).
One direction. No converse.
★ stays killed. This is the
Statement (B) close on
\(\mathbb{T}^3\) in this
packaging.

### G6 — if both G1 and G4 die

Leftover 4 needs a **new**
estimate shape that \(v_n\)
does not kill. Need★ cannot
repair the dead box.
Leftover 1 stays OPEN and is
still not started unless the
operator names that switch.

---

## What we will not do to “get a close”

- Invent depletion.
- Bound \(a_+\) by
  \(\|\nabla u\|_\infty\) and
  call it dynamical.
- Define
  \(K=(T_c-\theta\nu\mathcal D_s)_+/X\).
- Repair H.
- Weld ★.
- Start leftover 1 from
  ABC_λ.
- Switch to augmented /
  Theorem A.
- Hunt a blowup as this line.
- Mail a panel a close.
- Write a near-proof sentence.

---

## Execution from this page

1. Finish G1: A-pair write or
   named death. No new families.
2. Score G1 in four buckets.
3. G4 next, independent.
4. G2 / G3 only after G1 sits.
5. G5 is the close if G4 sits.
6. The tape wins.

Talking is not G1.
A path is not a close.

---

## Lock

Wanted: a seated a priori.
Path: G1 then G4.
Close-shaped target: G5
(Route B).
Route A first, pairing not
\(L^\infty\).
A is not B.
Catalog B open stays 1.
NS not solved.
