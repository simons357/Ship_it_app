# What “New” means on this branch

Cursor’s right-hand / Source Control / PR panels often label items **New**. That means **new files on this branch vs `main`** (or untracked locally)—not “a new product launched.”

Branch: `cursor/tao-snd-h-panel-a0eb` (PR #22). Roughly **128 added files**.

## Buckets

| Bucket | What showed up |
|--------|----------------|
| **Math papers / DA closures** | SND/H panel notes, Bridge* / positive-GCD closures, Zenodo submit PDFs + spectral archive mirrors, DA report |
| **SFE-BH spells** | Black-hole overlay spellbook + Python spell scripts (`scripts/sfe_*`, `run_spell.py`) |
| **Product docs** | Search-engine report, ChatVault hookup, Lego platform spine, VR Surgeon pipeline / story / animation script |
| **PFPI search code** | Runnable ingest → SQLite FTS5 → CLI search → FastAPI stub under `tools/pfpi/` |
| **Tests** | Bridge*, SFE spells, PFPI, shared_core unit tests under `tests/` |

## One-line inventory

This branch added the Tao SND/H math panel work (honest closures, no Clay claim), cleaned paper packs for Zenodo, overlay “spells,” product writeups (search / ChatVault / Lego / VR), and a small PFPI search tool with tests—not separate shipping products, just repo artifacts labeled “new” because they weren’t on `main` yet.
