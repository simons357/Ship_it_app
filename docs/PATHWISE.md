# First jet of \(N\) and \(T_c\)

23 September 2026.
**Stokes keeps \(N=0\) on \(v_n\).
Euler generates \(N\).
\(T_c\) grows at \(t=0\).
A Taylor coefficient is not a
useful \(K\) and not a death
of G4.
Not a close. ★ stays killed.
Catalog B open stays 1.**

Instantaneous \(MN\):
[`MN-CANCEL.md`](MN-CANCEL.md).
L-doors:
[`L-DOOR.md`](L-DOOR.md).
Energy-class:
[`ENERGY-K.md`](ENERGY-K.md).
Short interval:
[`INTERVAL.md`](INTERVAL.md).
Incoming ledger:
[`CENTERED-LEDGER.md`](CENTERED-LEDGER.md).
Chart reset:
[`RESET.md`](RESET.md).
Relative width:
[`WIDTH.md`](WIDTH.md).
SBP / \(\Phi_e\):
[`SBP.md`](SBP.md).
Target:
[`CENTERED-DRIFT.md`](CENTERED-DRIFT.md).
Machine: `python3 scripts/pathwise.py`.
Does not overwrite `stokes_moments.py`.
Do not start leftover 1.
Do not weld \(\star\).
Do not cash B★ as G5.
This is **not**
[`PATH-TO-CLOSE.md`](PATH-TO-CLOSE.md).

---

## Three kinds of sentence

**Exact.** Galerkin
\(X',Y',\Lambda'\), the
Gateaux of \(N\) and \(M\),
and \(T_c'=M'-\Lambda'N-\Lambda N'\).
On \(v_n\), \(N=0\), so
\(T_c'=M'-\Lambda N'\).

**Numerical.** Printed
jets. Finite is not \(C\)
and not a remainder.

**Illustrative.** None.
A \(t=0\) jet is not a
trajectory.

---

## The identities (exact)

\[
X'=-2\nu Y+2N,\qquad
Y'=-2\nu Z+2M,\qquad
\Lambda'=\frac{2}{X}(T_c-\nu\mathcal D_s).
\]

Along the NSE,
\(u_t=-B(u,u)-\nu Au\).
\(N\) and \(M\) are cubic.
Their derivatives are the
Gateaux along \(u_t\).

Locked: polarized
\(B(u,u)\) matches
`nonlinear_B`. Stokes
Gateaux matches a finite
difference on \(v_1\).

On the live \(2,2,4\)
triangle,
\(N'_{\mathrm{St}}/T_c=-8/3\),
flat in amplitude.
\(N'_{\mathrm{Eu}}/(\sqrt{X}\,T_c)\)
stays flat at \(0.925\).

---

## Stokes keeps \(N=0\) on \(v_n\)

\(N'_{\mathrm{St}}=0\) on
\(v_1,v_4,v_8\). Linear
dissipation does not leave
the knife-edge. The
instantaneous \(R_{mn}=1\)
of
[`MN-CANCEL.md`](MN-CANCEL.md)
is Stokes-invariant at
first order.

---

## Euler generates \(N\); \(T_c\) grows

| Family | \(N'_{\mathrm{St}}/T_c\) | \(N'_{\mathrm{Eu}}/T_c\) | \(T_c'/T_c\) |
|---|---|---|---|
| \(v_1\) | \(0\) | \(-0.645\) | \(7.48\) |
| \(v_4\) | \(0\) | \(-0.583\) | \(34.5\) |
| \(v_8\) | \(0\) | \(-0.542\) | \(100\) |
| live \(2,2,4\) | \(-8/3\) | — | — |
| sep \(m=1\) | \(-2.67\) | — | \(38.5\) |
| sep \(m=8\) | \(-8.17\) | — | \(132\) |

NSE column \(T_c'\) is at
\(\nu=1\). \(T_c\) *grows*
on \(v_n\). Negative
\(N'_{\mathrm{Eu}}\) makes
\(-\Lambda N'\) add to
\(T_c\). That is the
opposite of helpful
cancellation.

\(N'_{\mathrm{Eu}}/T_c\)
falls \(0.645\to 0.542\).
Finite is not a closed
form. Do not seat \(1/2\).

\(\Lambda'\) at \(\nu=1\)
is negative because
\(\nu\mathcal D_s>T_c\)
in these units. At
\(\nu=0\),
\(\Lambda'=2T_c/X>0\).
Do not cash a units
choice as viscosity
winning.

---

## What this is not

A first jet is not a
pathwise \(K\in L^1_{\mathrm{loc}}\).
It is not a bound on a
positive-time interval.
It is not a named death
of G4. \(v_n\) is still
an admissible class, not
a trajectory.

Tautological \(K\) is
still forbidden.

---

## What remains

Energy-class \(K(E)\) is
dead. Tight CS is ★.
LE is dead. LX is not
seated. Instantaneous
\(MN\) is dead.
The \(t=0\) jet does not
seat a useful \(K\) and
does not kill G4.

The short interval is
scored separately:
[`INTERVAL.md`](INTERVAL.md).
G4 stays OPEN only for a
\(K\) that is more than a
snapshot, more than a
\(t=0\) Taylor
coefficient, and more
than that short path.

---

## What this page is not

- A close. Catalog B open
  stays 1.
- A seating of B★ or of LX.
- A restoration of ★.
- An NSE trajectory.
- Leftover 1. Do not start
  H1.
- [`PATH-TO-CLOSE.md`](PATH-TO-CLOSE.md).

---

## Lock

Stokes keeps \(N=0\) on
\(v_n\). Euler generates
\(N\). \(T_c\) grows at
\(t=0\). A jet is not a useful \(K\).
G4 stays OPEN. ★ stays killed.
NS not solved.
