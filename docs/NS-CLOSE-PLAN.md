# Close plan — classical Navier–Stokes

16 September 2026.
Jonathan R. Simons.
**This is the synthesis.** Every other
AI write, Cursor tape, leftover sheet,
and magazine line was scored against
the later desk (PR **#104**) and the
lock tape. What survives is below.
What does not is named so it cannot
come back under a new title.

Ordinary unforced 3-D Navier–Stokes
is **not solved**. Clay Statement B
is **OPEN**. Catalog B open stays 1
(`B_regularity`). A is not B.
Soft X silent.

**Enough information?**
Yes to order the next writes and to
stop spending cycles on dead boxes.
No to close the equation.

A score of leftovers is not a close.
Talking is not a close.
Agreement among AIs is not evidence.

---

## 1. The honest ceiling

Closing Statement B means one of two
things, and only those two:

1. an a priori bound that keeps
   smooth, divergence-free,
   finite-energy data on
   \(\mathbb{T}^3\) or \(\mathbb{R}^3\)
   from losing regularity in finite
   time, **or**
2. a named smooth field in that class
   that blows up.

This desk is aimed at (1).
OpenAI’s forced construction is
Fefferman (C)/(D) if it holds. It is
not (1) and not (2) for unforced data.
Do not switch the living line to a
blowup hunt unless you name that
switch out loud.

The Honest Ceiling, already seated
on the later tape:

> Unconditional \(X\le M\) forcing
> \(\|\nabla u\|_\infty\) to stay
> finite, with no further hypothesis,
> does **not** follow from more
> careful bookkeeping on any of the
> three current lanes.

So “get over the goal line” is not
another KEEP card, another magazine,
or another 9D sweep. It is **one
seated inequality** that

- does not assume \(H^1\), \(L^\infty\),
  BKM, or the conclusion,
- survives the named adversarial
  families (\(v_n\), shears, amplitude,
  equal-shell \(v_L\), mixed swirl),
- and implies an a priori bound on
  \(X=\|\omega\|_2^2\) (Route B) **or**
  controls the local block **and**
  writes the missing passage from that
  block to global \(X\) (Route A plus
  a later global step that does not
  exist today).

Until that page exists, NS is open.

---

## 2. What was crap (do not rebuild)

These came from ChatGPT boards, older
Cursor agents, Grok pastes, magazine
copy, and prize packaging. They are
**DEAD** or **false glue**. The later
tape already killed most of them.
This list is so they cannot be
revived by renaming.

### False closes

| Claim | Why it is crap |
|---|---|
| Ordinary NS / Clay B is solved | Theorem A is a **different PDE**. Killing unrestricted \(\star\) is not a blowup. |
| SFE / Harmonic Blueprint ⇒ NS / six Millenniums | Canonical SFE is **unresolved**. HB Experiment 01 is a **held-out null**. Millennium-from-SFE is retired. |
| Triple Lock `SND ≡ GNC ≡ Bridge` | Identity is false. Retired. |
| Φ-renorm ⇒ Lemma★ ⇒ Clay B | Two books. Algebra KEEP. Barrier \(\|u^r/r\|_\infty\) still open and **not** Statement B. |
| Closing SND ⇒ ordinary NS | SND is a hypothesis / instrument. Displayed Theorem H is **false even under \(X\le M\)**. |
| Unrestricted \(\sup\mathcal R_\star<\infty\) | **KILLED** by growing-layer \(v_n\). \(\mathcal R_\star\gtrsim n/165888\to\infty\). Instantaneous class, not a singular NSE solution. GPT boards that still say “★ OPEN” are **stale and dangerous**. |
| Energy-linear remainder for (A) / \(T_{j\leftarrow j}\) | Scaling-killed: \(T\sim\lambda^{9/2}\) vs \(\sim\lambda^4\). Not open. **False**. |
| C10 as leftover 5 closed / “C10 died” as the program | (A) did not seat. “Died” means this attempt failed. The leftover stays OPEN. Repairing with \(\|\nabla u\|_\infty\) is BKM. |
| T2 Lemma 2 / Theorem F / Φ-glue / Theorem G | Circular or dead. G needs a uniform \(\rho^{1/2}\) that B7c kills. |
| B6 \(\int X\,dt<\infty\Rightarrow X\in L^\infty\) | Counterexample \(X=(T_*-t)^{-1/2}\). |
| Occupation clock / detector decay as a bound on \(X\) | Withdrawn. Cover, not continuation. |
| Exact-shell \(K\le 16/9\) ⇒ regularity | CLAIMED at most. Single-shell. Does not control multi-shell \(v_n\). No continuation criterion. |
| “9D is claimed” / designed \(\Theta(m^2)\) 9D | Designed 9D is **NO** (Freiman-AP). The claimed object is the exact-shell bound. Do not glue the word. |
| Finite \(0.641\), \(0.610\), \(0.327\), \(0.456\), three-shear \(2/3\) as \(C_0\) or as \(16/9\) | Samples. Floor \(\neq\) ceiling. |
| Ring Lemma as proved alignment | REPAIR. Bernstein on a band-limited piece is the honest reading. |
| Peak-fraction / Lower-Dini \(\rho\)-floor as unconditional | ChatGPT listed it as rigorous. It is conditional on \(\Gamma=\|\nabla u\|_\infty\) and a bound on \(j_*\). \(A.3\) is a Dini **ceiling**, not a floor. |
| “Every §2 result waits on Kato–Ponce” | Seated \(A.2\) is Hölder \((6,2,3)\) + Poincaré. Kato–Ponce is a cheap check, not the foundation. |
| \(Q_1\) / extra-stress / Track A ⇒ Statement B | `A_implies_B` **fails**. Uniform \(H^1\) as \(\varepsilon\to 0\) is leftover 7 and **not required** for Theorem A. |
| Inverse-GCD / Q6 / E8 / Q-stack / Route C as NS | Arithmetic book. Off this track. Full-spectrum \(\lambda_{\min}(Q_N)>-1/2\) is retired. |
| RH proved / ARCHON / Bridge* as fluids | Not RH. Not NS. |
| CMB / Saturn / Kabbalah / Cosmic Graffiti as confirmation | Analogy / street. Not a theorem. |
| Dream Team vote / SuperGrok stamp / “near proof” | Theater. Public rule: no Clay / prize / QED / solved language. |
| Glue leftover 1 + 4 + 5 into one sentence | Ordinary NS is **one** of those rows, not all three welded. |
| Start H1 from ABC\(_\lambda\) | Named forbidden start. ABC snapshots do not rescue unrestricted (6). |
| Switch the living line to OpenAI / Córdoba–Martínez-Zoroa blowup search | Different program. Forced or singular. Not this a priori line. |

If a later paste contradicts this
table, the table wins until a **named
error with a date** is added.

---

## 3. What was good (keep and use)

These survived every honest audit.
They are tools. They are not a close.

### Identities and seated lemmas

- Spectral bookkeeping
  \(X,Y,Z,\Lambda,T_c,\mathcal D_s\).
  \(\mathcal D_s\ge 0\).
  \((\log\Lambda)'=2(T_c-\nu\mathcal D_s)/Y\).
  \(X\le\|u\|_2^2\Lambda\).
- T2 Lemma 1: divergence-free kills
  low self-flux. Unconditional. **Not
  enough for regularity.**
- TJJ-Trans, TJJ-α, stretch
  \(\le a_+ Z_j\). Exact.
- Far-shell Young on the axisymmetric
  face. Does not touch \(T_{j\leftarrow j}\).
- Φ-renorm algebra
  \(r^{-4}\partial_z(\Gamma^2)=\partial_z(\Phi^2)\).
  Other book. KEEP the identity.
- Boxed shell identity
  \(\tfrac12\dot X_j+\nu 2^{2j}\|\nabla\Delta_j u\|_2^2=-2^{2j}F_j\).
- \(A.2\) bound on \(F_j\) under
  \(X\le M\) (Hölder \(6,2,3\) + Poincaré).
  **Not Theorem H. Not propagation.**
- Theorem A on extra-stress /
  \(Q_1\)-NS, \(\varepsilon>0\).
  This PDE only.

### Named kills (these are wins)

Killing a false bridge is progress.
Do not spend another cycle re-killing
them.

- Unrestricted \(\star\) / uniform
  geometric remainder: \(v_n\).
- Displayed Theorem H / SND-C as
  written: high-tail shears, dropped
  viscous tail, invalid 3-D Sobolev.
- Energy-linear \(R\) for (A):
  \(u^\lambda\), \(\lambda^{1/2}\).
- Designed \(\Theta(m^2)\) 9D:
  Freiman-AP.
- \(K=0\); \(\lvert T_c\rvert\le C\|u\|_2 X^{3/2}\);
  uniform pre-Young \(C\).
- Theorem G; universal \(t=0\) SND
  floor (\(v_L\), \(\rho(0)=1/L\)).
- \(M\)-free quadratic \(F_j\).
- Detector occupation decay.

### Instruments (print, do not theorem)

- SND diagnostics: \(X_j\), \(J\),
  \(\rho\), \(j_*\), tail, \(F_j\),
  peak migration. No persistence.
- Exact-shell \(C=4/3\),
  \(K\le 16/9\): **CLAIMED.** Freeze
  sweeps. Independent reproduction
  only. Not this desk’s next write.

### Discipline that is good

- Four buckets only: PROVED /
  CLAIMED / OPEN / DEAD.
  “LIVE” is a research order, not
  a bucket.
- `YES-NO-OPEN.md` wins on
  contradiction.
- Do not start leftover 1 from the
  energy-budget page.
- Do not weld lanes without a
  displayed inequality.
- Cover-sheet posture: *active
  research program, validated
  conditional partials, catalog of
  killed mechanisms* — never
  “near proof.”

---

## 4. The live question

Stop asking whether SND persists.
The live question, already localized:

> Can the nonlinear term drive
> high-frequency concentration
> faster than viscosity can
> control it, **without assuming
> the bound you want?**

Exact same-shell internal energy
transfer at \(b=0\) cancels in the
relevant bookkeeping. The obstruction
is near-scale \(b\ge 1\) /
enstrophy-side transfer. That is
where stretching can beat spread
and viscosity.

Three lanes sit **beside** each
other, not in series through H:

| Lane | Target | Status |
|---|---|---|
| **Route A / C10 / leftover 5** | Dynamical \(a_+\) then (A) on the local block | Identities sit. First new arrow (template \(\to a_+\)) is **empty** or BKM-adjacent. (A) not seated. |
| **Route B / leftover 4** | \(T_c\le\theta\nu\mathcal D_s+K(t)X\) with **useful** \(K\in L^1_{\mathrm{loc}}\) | Identities sit. **Loss A first:** Signed Assembly Gate (`SIGNED-ASSEMBLY-GATE.md`) before any \(T_c\) bound. Estimate OPEN. Unrestricted \(\star\) stays killed. Tautological \(K=(T_c-\theta\nu\mathcal D_s)_+/X\) is not content. |
| **SND** | Instrument | Diagnostic. No persistence. Displayed H withdrawn. |

Exact-shell 9D is a **fourth object**,
frozen CLAIMED, not a lane into
regularity.

Leftover 1 = H1 = WRITE (6) is a
**different integral** (Bad-pair
stretching on the cylinder). It stays
OPEN. It is **not** started from this
plan. If both Route A and Route B
die, it is still there — as itself,
not as glue.

---

## 5. How we get over the goal line

There is no third fake bridge.
There are two killable routes and a
named fallback map.

### Route A — write or kill (first)

Need the whole chain, every arrow
marked EXACT / STANDARD / NEW / NUMERICAL:

\[
\text{NS}
\Rightarrow
a_+\text{ from the dynamics}
\Rightarrow
\int a_+<\infty
\Rightarrow
(A)
\Rightarrow
\text{local-block control}.
\]

What already sits: stretch
\(\le a_+ Z_j\) (EXACT).
TJJ-template (STANDARD; remainder
is \(L^\infty\) of local velocity
and strain).

What is missing: a dynamical bound
on \(a_+\) that does **not** use
\(H^1\), \(L^\infty\), or BKM.

**If it works.** (A) plus
\(\int a_+<\infty\) plus an
energy-class remainder stops the
local block from feeding a
finite-time enstrophy blowup by
near-scale stretching. Far-shell
Young already sits, so leftover 5
moves. That is **not** leftover 1
and **not** unrestricted 3-D by
itself. A later write still has to
pass from the local block to global
\(X\). That arrow does not exist
today. Do not pretend it does.

**Death condition.** Any of:

- \(a_+\) controlled only by
  \(\|\nabla u\|_\infty\) or a
  Sobolev embedding of the unknown
  field,
- remainder of (A) estimated by
  Bernstein so Gronwall wants the
  \(H^1\) ceiling,
- an adversarial family with
  \(a_+\) large and the local block
  uncontrolled,
- a silent strengthening that
  assumes the conclusion.

If the page cannot be written
without the forbidden control,
**name the death and stop
patching.** Leftover 5 stays OPEN.
Do not insert
\(\|\nabla u\|_\infty\le\Gamma\)
and call it depletion.

**This is the only Route A write.**
One page. Adversarial families
already on the desk (high-tail
shears, amplitude, \(v_L\), \(v_n\),
compact mixed swirl). No new 9D
sweeps.

### Route B — write or kill (independent, next)

Class: unrestricted smooth
divergence-free fields on
\(\mathbb{T}^3\).

**Do not start at the clock.**
The screenshot canon already seats
\(\Lambda'=2(T_c-\nu\mathcal D_s)/X\).
The first missing implication is
the signed sum. That object is now
a gate before old Gate 5:

\[
\boxed{\textbf{SIGNED ASSEMBLY GATE}}
\]

(`docs/SIGNED-ASSEMBLY-GATE.md`).
Loss A = assemble
\(\sum\mathrm{Im}[(q\cdot v_p)(v_q\cdot\overline{v_k})]\)
while keeping \(k_\perp\), gap-cancel,
and phase. Loss B = evolve a
snapshot bound to integrable
\(K(t)\). Do not write Loss B
before Loss A has a write or a
named death. Do not replace the
product by \(\lvert\widehat B_k\rvert\)
early.

Target:

\[
T_c
\le
\theta\nu\mathcal D_s
+K(t)X,
\qquad
0\le\theta<1,
\qquad
K\text{ useful and }K\in L^1_{\mathrm{loc}}.
\]

Useful means: \(K\) is controlled by
the energy inequality or by a named
cancellation in \(T_c=M-\Lambda N\),
not by defining
\(K=(T_c-\theta\nu\mathcal D_s)_+/X\).

Keep \(T_c-\theta\nu\mathcal D_s\) as
**one pairing**. Do not split \(M\)
and \(\Lambda N\) if that destroys
the centering.

**If it works.** Useful integrable
\(K\) keeps \(\Lambda\) finite, hence
\(X\le\|u\|_2^2\Lambda\) finite, hence
no finite-time enstrophy blowup on
the torus, **in this packaging**.
One direction. No converse.

**Death condition.** Any of:

- every proposed \(K\) is tautological,
- \(K\) is a uniform function of
  energy-class norms of \(\star\)
  scaling (\(v_n\) already kills that),
- \(K\) is \(\|\nabla u\|_\infty\) (BKM),
- centering is lost and the remainder
  is worse than the dead box.

**This is the only Route B write,
and it now has two stacked pages.**
Page 1 (next): signed assembly on a
named class, or a named death that
every sum reintroduces occupancy
\(s\) or \(\|\nabla u\|_\infty\).
Page 2 (later): useful integrable
\(K\) from that snapshot, or a named
death of the evolution arrow.
Test both on \(v_n\), amplitude,
shears, and single shells.
Do not restore
\(\sup\mathcal R_\star<\infty\).
Need★ cannot repair the dead box.

### Crossover — only after (A) sits

There is no seated inequality

\[
T_c\le C\sum_j a_+ Z_j
\]

and none of the form
near-scale depletion \(\Rightarrow\)
\(\lvert T_c\rvert\le\theta\nu\mathcal D_s+K(t)X\).

Do not draw a conceptual arrow.
If a later write produces an actual
inequality, display it and re-test
\(v_n\) and the shears.

### If both routes die

Then leftover 4 needs a **different**
estimate that \(v_n\) does not kill.
Leftover 5 stays OPEN until a class
bound or a field with
\(\int\rho_j=\infty\).
Leftover 1 stays the cylinder write
(shapes 1–3, or \(\mathcal G\to\infty\);
do not start from ABC\(_\lambda\)).

That map is honest. It is not a
third fake bridge.

A BKM-type **conditional**
(controlled shell occupancy plus
extra \(j_*\) control, no
\(M\)-smuggling) is the only
theorem-shaped object the SND desk
still endorses. That is **not**
Statement B. It is a publication
fallback, not the living attack.

---

## 6. Execution order

One person, one row. Do not glue.

1. **Freeze now.**
   Theorem H. Universal \(\rho\)-persistence.
   Exact-shell \(K\) sweeps. Designed 9D.
   Blowup-search as the living line.
   Starting leftover 1. Welding C10
   to \(T_c\). Kato–Ponce as step 1.
   Pólya \(\rho\)-floor document.
   SFE / HB / \(Q_1\) / magazine / RH
   as this PDE. “9D is claimed.”
   “★ still OPEN.” “C10 died” as a
   burial. “Near proof.”

2. **Route A write-or-kill.**
   Dynamical \(a_+\) without \(H^1\),
   \(L^\infty\), or BKM — or a named
   death of that arrow. File under
   four buckets. Tape wins if a
   sentence contradicts
   `YES-NO-OPEN.md`.

3. **Signed Assembly Gate, then
   Route B.**
   Independent of leftover 1.
   First page: sum the signed
   triangle identity without
   \(\lvert\widehat B_k\rvert\)
   early, on a named class — or
   name that death. Only then:
   centered pairing, useful \(K\),
   or a named evolution death.
   Same families. Same tape.

4. **Crossover** only if (A) sits.

5. **SND stays an instrument.**
   Print \(\rho\), \(j_*\), tail,
   \(F_j\), \(\Delta j_*\). Do not
   assert persistence. Do not repair
   displayed H.

6. **Exact-shell bound stays CLAIMED.**
   Independent specialist
   reproduction of the projected
   identity and weighted incidence.
   Not this desk’s next write. A
   true \(4/3\) still does not
   restore \(\star\) and does not
   give a continuation criterion.

7. **Score every write** in
   PROVED / CLAIMED / OPEN / DEAD.
   If both routes die, open leftover
   1 as itself, or write a new
   leftover-4 shape. Do not invent
   a weld.

---

## 7. How the other AIs scored

| Source | Keep | Discard |
|---|---|---|
| **Later Grok / Cursor desk** (PR #104: `NS-STATUS.md`, `UNAUG-NS-UNIFIED.md`, `MASTER-PLAN.md`) | Living score. Two routes. Four buckets. Honest ceiling. | None as strategy. Naming fix only: do not title the claimed object “Exact-shell 9D.” |
| **ChatGPT “Global Regularity Program”** (16 Sep paste) | OPEN; H dead; \(M\)-free \(F_j\) dead; \(t=0\) floor dead; 16/9 CLAIMED not a close; BKM demoted; cover sheet honest. | Peak-fraction floor as unconditional; Kato–Ponce as foundation of \(A.2\); “C10 died” as a program; Lower-Dini \(\rho\)-floor as next write. |
| **Older NS-search agents** | Same-shell energy-linear \(R\) dead; Ring + T2 Lemma 1 as **conditional tools**. | “Lemma★ still OPEN”; “Attack 9D next”; “centered-drift never found” (later tape has the identities). |
| **SND spectral (PR #101 / #103)** | KEEP card; boxed \(\dot X_j\); Tests A–C; displayed H withdrawn; C10 arrow empty. | SND \(\Leftrightarrow\) Clay packaging; C10 as producer of (A); persistence. |
| **Unaugmented pathway** | Two books; Φ algebra KEEP; `A_implies_B` fails; do not start H1 from ABC\(_\lambda\). | Welding ★ / H1 / \(T_{j\leftarrow j}\) / SND. |
| **\(T_j\) estimate** | Split identities; “do not invent the energy-linear line.” | “Only \(T^{\mathrm{ss}}\) remains.” |
| **H1 / centered summaries** | H1 is a write, not a theorem; Outside-ℰ blocked; do not glue H1 to ★. | Estimate-complete ⇒ GR; inventing the unrecovered centered package. |
| **Status-map / Cosmic Graffiti / magazine** | Street layer. Standing rule: no solved language in public. | Any use as a theorem or as Clay confirmation. |
| **Rocks / OpenAI briefing** | Statement B is unforced; OpenAI is forced (C)/(D) if it holds. | “Path forward = blowup hunt.” Not this line. |
| **Pólya \(\rho\)-floor rebuild** | — | **REJECT as primary.** Weaker than regularity; \(1-\rho\) can still sit at high frequency. |

Supersession rule: PR **#104** and
`YES-NO-OPEN.md` are the later tape.
Older “★ still OPEN” and “Attack 9D
next” do not win.

Canonical files (on
`cursor/unaug-ns-unified-status-a7a2`,
PR #104):

- `docs/YES-NO-OPEN.md` — lock
- `docs/NS-STATUS.md` — four buckets
- `docs/UNAUG-NS-UNIFIED.md` — unified score
- `docs/MASTER-PLAN.md` — two routes
- `docs/ISSUES-SHEET.md` — leftovers
- `docs/REPORT-AUDIT.md` — incoming AI score
- `docs/INCOMING-WRITEUPS.md` — who said what
- `docs/C10-CHAIN.md` — Route A chain
- `docs/CENTERED-DRIFT.md` — Route B target
- `docs/FOURIER-TRIANGLE.md` — triad geometry; first missing implication
- `docs/SIGNED-ASSEMBLY-GATE.md` — gate before old Gate 5; Loss A
- `docs/SAG-5-COMPATIBILITY.md` — 5A rank / 5B cocycle; \(\Gamma(\mathcal H)\)
- `docs/SAG-6-LATTICE-CIRCLE.md` — one-output equal-input circle; \(N^{1/2}\) **OBSTRUCTED**
- `docs/SND-TO-REGULARITY.md` — named hole (iii) frequency drift
- `docs/SND-INSTRUMENT.md` — diagnostics, no persistence

This page does not replace those.
It is the operator order after
they were all read at once.

---

## 8. Lock

Ordinary NS is not solved.
Clay Statement B is open.
Unrestricted \(\star\) is KILLED,
not OPEN.
Displayed Theorem H is withdrawn.
C10 is a candidate, not a theorem.
Centered useful \(K\) is OPEN.
SND is an instrument.
Exact-shell \(K\le 16/9\) is CLAIMED,
frozen, not regularity.
Frequency drift from the field is
still missing.
Do not glue leftover 1, 4, and 5.
Do not start H1 from this page.
Do not repair H.
Do not run another thousand sweeps.
Do not mail a panel.
Do not put SFE, \(Q_1\), or the
magazine on this PDE.

Primary attack = nonlinear
high-frequency transfer.

Route A1 parked (UNRESOLVED): no
noncircular \(\int a_+\).
Fourier triangles reconstructed:
`docs/FOURIER-TRIANGLE.md`. First
missing implication is the signed
sum, not the time integral. Prime
masks do not fill it.

Signed Assembly Gate
(`docs/SIGNED-ASSEMBLY-GATE.md`, 24 Sep)
sits before old Gate 5. The blank
is now SAG-5
(`docs/SAG-5-COMPATIBILITY.md`):
global compatibility defect, split
5A (shared-input rank \(\le 2\))
and 5B (partner/output cocycle).
Do not stamp SAG-5 from rank
alone. SAG-6
(`docs/SAG-6-LATTICE-CIRCLE.md`)
ran the one-output equal-input
circle with the centered weight
kept off. Hemisphere alignment
kills \(\|\Sigma\|\lesssim N^{1/2}
w_{\mathrm{nat}}\). That test is
**OBSTRUCTED**. Do not massage it
into \(16/9\) or into occupancy.
A later write must name a
different gain.

Two routes, both OPEN, both
killable. If both die, the leftover
is a **new estimate shape**, not a
repair of a dead box.

Enough for this order.
Not a close.
NS not solved.
