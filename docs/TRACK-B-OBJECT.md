# Regularity leftover as an a priori

`python3 scripts/track_b_object.py`  
`python3 scripts/da_machine.py trackb`

Dated 4 September 2026. The PDE is not being tuned.
Leftover knobs are scored. This write asks whether
they decide classical regularity. They do not.

---

## The knob on this write

Tesla: leftover knobs are scored. The leftover is
the object. Sit down. Cashing a finished leftover
list as continuation is a knob.

No \(Q_1\). No \(\varepsilon\). No Biot–Savart slogan.
Do not write \(c=8\) into the PDE. Do not spawn
\(n=64\).

---

## What the apparatus does

**B36, pass.** Leftover catalog miss and leftover-close
miss are readable together. Same caches as B35.
No new FFT.

**B36a, fail** of “leftover knobs decide regularity.”
The leftover is a closed estimate for \(X\).

**B36b, fail** of “scoring the leftover catalog is
continuation.” B35b already missed that slogan.

**B36c, fail** of “naming the leftover as the object
is still an NS a priori.” A desk sentence is not a
type.

**B36d, fail** of “the leftover catalog is
\(\int\|\omega\|_\infty\).” A leftover list is not
the max criterion.

**B36e, fail** of “this leftover close decides
regularity.” No. Domain B stays open.

**B36f, fail** of “this retunes the PDE.” Leftover
knobs are knobs on the check.

**B35e, fail** of “classical regularity is decided
by leftover closes.” Scored here.

---

## They work it

**Tesla.** You asked if leftover knobs write the
bound. They do not. The object is still \(X\). Sit
down.

**Leray.** I own dissipation on these packets. I
did not hand you all data.

**Ladyzhenskaya.** Same weight, both sides. I still
will not give you \(\varepsilon\).

**Majda.** A leftover list is not a class.

**Beale.** Nobody votes a leftover list into
\(\int\|\omega\|_\infty\).

**Feynman.** Missable: whether leftover knobs are
scored, and whether they decide \(X\). The first
held. The a-priori slogan missed.

**Einstein.** The object stayed the classical field.
A desk is not an estimate.

**Operator.** Regularity leftover is scored. B35e
is scored. Regularity stays open. Do not spawn
\(n=64\). B4c stands. Do not cancel to \(\Phi\).
Do not write \(c=8\) into the PDE.

---

## Score

| id | Verdict | What it is |
|---|---|---|
| B35e | **fail** | leftover closes decide regularity |
| B36 | **pass** | leftover catalog miss, leftover-close miss readable |
| B36a | **fail** | leftover knobs decide regularity |
| B36b | **fail** | scoring the leftover catalog is continuation |
| B36c | **fail** | naming the object is an NS a priori |
| B36d | **fail** | leftover catalog is \(\int\|\omega\|_\infty\) |
| B36e | **fail** | this leftover close decides regularity |
| B36f | **fail** | this retunes the PDE |
| domain B | **open** | regularity stays open |

Tesla’s line: leftover knobs are scored. The leftover
is the object. Sit down.
