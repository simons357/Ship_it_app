# Track A gap — extra dissipation leaves

`python3 scripts/da_machine.py tracka`

Object on A after Theorem A: a bound on \(\|u\|_{H^1}\) that
does **not** blow up as \(\varepsilon\to 0\). That row stays
**open**.

---

## What the box shows

Fourier–Galerkin Taylor–Green, \(n=12\), \(\nu=0.05\),
\(\beta=1/2\), \(\alpha=1\), \(T=0.1\).

As \(\varepsilon\) falls, the \(Q_1\) integral falls and
enstrophy at \(T\) rises toward the \(\varepsilon=0\) value.
The extra term that closed Theorem A leaves. The classical
energy identity is what remains.

That is A6 **pass** and A7 **fail** of “this box is uniform.”
A8 **fail**: a decaying box is not a no-go for all data.
A9 **fail**: vanishing of \(Q_1\) is the gap, not a bound.

---

## What would move the open row

A proof that Lemma 4’s constant stays finite as
\(\varepsilon\to 0\), or a named obstruction that it cannot.
Neither sits. Old C/I is not reused.

The write stays on A. Olga stays on A.

Catalog: [`TRACK-A-LEMMAS.md`](TRACK-A-LEMMAS.md)  
Chain: [`AUGMENTED-NS-PROOF-CHAIN.md`](AUGMENTED-NS-PROOF-CHAIN.md)
