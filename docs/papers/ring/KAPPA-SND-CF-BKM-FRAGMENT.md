# Historical face note — κ-SND / CF93 / BKM tail paste

**Banner:** **CONDITIONAL.** Clay **NOT CLAIMED.** DA-VC-01 still **FAIL.**
Unconditional SND remains **OPEN.** GNC is incomplete on the Paper2 chain.
This is **not** a complete `.tex`. **Do not invent a preamble.** **Do not
compile.** **Do not import into** `domain_architect/`. **Do not overwrite**
[`02_ring_lemma_snd_conditional.pdf`](02_ring_lemma_snd_conditional.pdf).

Jon pasted a mid-document TeX tail (after `\end{equation}`): Bony/paraproduct
shells, Lemma “Borromean Triadic Transfer Bound” under
\(\kappa_j(t)\le\kappa^*<1\), Ring Lemma
\(\|\nabla\xi\|_\infty(B_{j^*})\le C_R 2^{j^*}\), CF93 closure via
\(E_{\min}:=\inf E_{j^*(t)}>0\), then a BKM bootstrap claiming
Theorem `thm:main` global \(C^\infty\) on \(\mathbb{T}^3\) for
**unaugmented** NS, plus a comparison table vs PSL/BKM/CF.

## Unique labels — absent from every filed Ring / Paper2 source

Searched 25 Aug 2026:

`lem:triad_bound_formal`, `eq:triad_bound_result`,
`prop:CF_integrability`, `E_{\min}`, `thm:CF93_theorem`.

**No hits** in:

- [`RingLemma_Final.tex`](RingLemma_Final.tex) (SHA `4602065ef68a…`)
- [`RingLemma_Simons_June19_2026.tex`](RingLemma_Simons_June19_2026.tex)
  (SHA `a73d949f…`)
- [`02_ring_lemma_snd_conditional.pdf`](02_ring_lemma_snd_conditional.pdf)
  (SHA `0304f039…`; **not overwritten**)
- Paper2 faces: June FIXED PDF, August REPAIRED TeX, May 18 DRAFT_original,
  Final Polished

This tail is **not** the later sections of `RingLemma_Final.tex`. That file’s
`thm:main` is **augmented** \(Q_1,Q_3,Q_6\) NS; its dashboard leaves classical
SND **Open** and unconditional classical regularity **Not claimed**. It has
no triad-bound labels, no \(\kappa_j\le\kappa^*\), no \(E_{\min}\), no CF93
theorem, no Bony paraproduct shells.

Full TeX for the unaugmented \(\kappa\)-SND / \(E_{\min}\) argument was **not
received**. Do not stitch a fake complete paper from the tail.

Recovered title of a matching manuscript family (historical ledger only, no
bytes here): *Conditional Regularity for the Standard 3D Incompressible
Navier–Stokes Equations via Spectral Non-Dispersal and the Ring Lemma*.

## Mathematical classification (do not upgrade)

- **Conditional** on an SND hypothesis. Clay is **NOT CLAIMED**.
- This \(\kappa_j=E_j/E\le\kappa^*<1\) is an **upper** bound on shell
  **energy** fraction (non-concentration style). They still call it Spectral
  Non-Dispersal. **Record the naming collision; do not “fix” the paper.**
- It is **not automatically** usable Ring SND
  \(\inf J/X\ge c_*\) (a **lower** bound on the dominant-shell **enstrophy**
  fraction). Do not glue. The 21 Aug PDF already freezes the lower-bound form
  and notes that an upper-bound companion convention is related but not
  identical (Remark 1.2).
- Do not identify with Paper2 operator SND
  \(\|H_N[a]-\widehat H_N^\mu\|_{\mathrm{op}}\).
- **Flag (FACES/map only, not a live DA proof):** \(E_{\min}>0\) is used as
  if it were free. The \(\kappa\)-SND upper bound does not supply that lower
  bound on dominant-shell energy. Do not declare CF “closed” for Clay.
- Live DA must **not** import this as Spe / `TRANSFORMABLE` without a real
  structure map \(T\).

Map: [`FACES.md`](FACES.md).
