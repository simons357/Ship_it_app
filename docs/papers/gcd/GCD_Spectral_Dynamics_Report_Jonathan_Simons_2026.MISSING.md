> **Missing-receipt banner (25 August 2026).** Filename-only chat drop.
> **Not** the August Q6 PDF (`a2391122…`). **Not** MAGNUM / mix TeX
> (`f41194c76cf4…`). **Not** `GCD_Spectral_Paper1_UPDATED_2026-08-14.tex`.
> **Not** PAPER_A / PAPER_B. Clay is **NOT CLAIMED**. Do **not** invent the
> report. Do **not** import into `domain_architect/`.

# `GCD_Spectral_Dynamics_Report_Jonathan_Simons_2026` — not received

**Found?** No. **SHA-256:** none (0 report bytes). **Title / date / extension:**
unknown; not invented. A message that is only this filename is **not** an
attachment.

This filename is **not** a substitute for:

- Controlling Q6 arithmetic PDF
  [`04_q6_inverse_gcd.pdf`](04_q6_inverse_gcd.pdf)
  (SHA-256 `a239112289a1a150d7a7a2212ec7f3649382b81542b49d5d2300f8611df3d6b1`).
- May mix / MAGNUM TeX
  [`docs/archive/gcd-spectral-attractor-2026-05/Simons_GCD_Spectral_Attractor_Unified.tex`](../../archive/gcd-spectral-attractor-2026-05/Simons_GCD_Spectral_Attractor_Unified.tex)
  (SHA-256 `f41194c76cf422a227d6b7489d4a6c95bf7f717404213e6a02a29239a14aeec5`).
- Pack-only `GCD_Spectral_Paper1_UPDATED_2026-08-14.tex`,
  `PAPER_A_Inverse_GCD_CORRECTED.tex`, `PAPER_B_Mobius_GCD_Q6.tex`
  (also **not** on this VM).
- June Paper2 FIXED PDF `7de9444d…`.

## Hunt (25 August 2026)

| Probe | Result |
|---|---|
| `/workspace`, git tree, `docs/papers/gcd/`, `docs/archive/` | No `GCD_Spectral_Dynamics_Report*`. Never added. |
| Cursor uploads `/home/ubuntu/.cursor/projects/workspace/uploads` | No matching file. |
| `/tmp`, `/app` name walk | 0 hits. |
| Base44 app `69b28657b0df374441f0302e` bare name + `.pdf` `.tex` `.md` `.docx` `.txt` | HTTP **302** then CDN **403**, 0 bytes. Same empty forbid as invented names. |
| Base44 app `69b2865953e46e195fc302f0` same extensions | HTTP **302** then CDN **403**, 0 bytes. |
| Zenodo API `q="GCD_Spectral_Dynamics_Report"` | **0** hits. |
| Zenodo API `q="GCD Spectral Dynamics"` | **0** hits. |
| Zenodo API creators + title Dynamics (Jonathan / Jonathan R. Simons) | **0** hits. |

`base44.app` **302**s arbitrary names under `files/mp/public/…`. Existence is
the CDN hop. **403 / 0 bytes here is not proof the object exists.** Without a
9-hex prefix this VM cannot fetch a session download.

## Classification if bytes later arrive

Identify from `\title` + `\date` + SHA-256, **not** the filename. Do **not**
treat MAGNUM or the August Q6 PDF as this report unless the SHA matches.
Usable Q6 arithmetic stays `HN = D^((-1)/2)*Qtilde*D^((-1)/2)`. Withdrawn:
all-\(N\) \(\lambda_{\min}>-1/2\); dark-state \(\Leftrightarrow\) Goldbach;
NS from GCD matrices.

## How to send it

Cursor **upload**, **paste**, git-push, or a Base44 URL with the 9-hex prefix
that HTTP **302** then **200**. Mac `/Users` paths do not mount. Caption the
extension (`.pdf` / `.tex` / `.md`) if you know it.

Map: [`FACES.md`](FACES.md). Lookup:
[`docs/packets/OLD-PAPERS-LOOK-UP.md`](../../packets/OLD-PAPERS-LOOK-UP.md).
