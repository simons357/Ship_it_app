# Exact-shell lock — 13 September 2026

**Word:** CLAIMED.  
**Not a regularity close.** Ordinary unaugmented Navier–Stokes stays **open**.  
**Soft X silent.**

This page is the lock for the two objects below. It does not upgrade either object. A later sentence that calls the first object proved, or that repairs the second object with the first, is wrong until this page is edited with a named reason.

Working write-up (not on `main`): [PR #74](https://github.com/simons357/Ship_it_app/pull/74), pages `docs/ATTACK-9D-FULL-SUPPORT-BOUND.md` and `docs/LEMMA-STAR-GROWING-LAYER.md`.

---

## 1. Exact-shell bound — CLAIMED

If

\[
K_{\alpha,\beta}(w)\le\frac{16}{9}
\]

holds for \(Aw=\alpha w\) on a **single input shell**, then occupancy \(s\) is gone from that estimate. Equivalently the written form is

\[
\|\Pi_\beta B(w,w)\|_2
\le
\frac43\frac{\alpha}{\sqrt{\beta}}\|w\|_2^2,
\qquad\beta>0.
\]

This is a **written bound on exact-shell fields**, not a sweep maximum. Printed numbers such as \(K\approx 0.456\) or \(K\approx 0.641\) sit under \(16/9\). They are not the bound and they are not \(C_0\).

The three-shear field then sits under that **named ceiling**, not under a rumor.

Until a specialist signs the algebra, **or** a sweep shows nothing near \(16/9\), the word stays **CLAIMED**.

Designed \(\Theta(m^2)\) 9D stays dead. Grow-\(s\) stays historical.

---

## 2. Unrestricted \(\star\) — already dead

A true \(4/3\) does **not** kill unrestricted \(\star\).  
A true \(4/3\) does **not** repair unrestricted \(\star\).

That box is already dead by the multi-shell family

\[
v_n=D_n(z)\,(U_1(nx,ny),U_2(nx,ny),0),
\qquad
D_n(z)=\sum_{j=-n}^{n}e^{ijz}.
\]

On the locked evaluators, \(\mathcal R_\star(v_n)\to\infty\). That is a counterexample to the boxed instantaneous estimate. It is not a singular Navier–Stokes solution.

---

## 3. What this does not give

| Sentence | Status |
|---|---|
| Occupancy \(s\) removed on one exact input shell, if \(K\le 16/9\) holds | **CLAIMED** |
| Three-shear field under the named ceiling \(16/9\) | **CLAIMED**, contingent on the same algebra |
| Unrestricted \(\star\) with one finite geometric constant | **Dead** by \(v_n\) |
| Exact-shell \(4/3\) as a kill or repair of that box | **No** |
| Continuation criterion | **No** |
| \(\star\Rightarrow\) global regularity restored | **No** |
| Ordinary Navier–Stokes closed | **No. Open.** |

Do not glue the claimed exact-shell estimate to leftover energy-budget closure, H1, or the axisymmetric shell door. Those are other integrals.

---

## 4. How the word may move later

The word is still CLAIMED.

It may move only if a specialist signs the exact-shell algebra, or if a sweep is run that actually tests near \(16/9\) and is filed here with the fields and the code. A clean sweep still does not by itself become a theorem. Silence on Soft X remains either way.
