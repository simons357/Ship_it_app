# Simple desk — see Domain Architect as a scientific product

The kitchen-sink PWA on PR #43 mixed ChatVault search, Route C, swirl
faces, and inbox filing into one screen. That is why it felt
complicated. This desk is three boxes.

```bash
python -m domain_architect --site
```

Open http://127.0.0.1:8765/

1. **Inquire** — paste an equation. Functional Role Analysis. Not a proof.
2. **See** — jigsaw + known-rule wiring (energy, viscosity). No outside. No floor.
3. **Compute** — live `--jigsaw` / `--tube` from this package.

Not ChatVault. Not CosmoEvolution. Not a Zeta-zero prover.

## What we did not weld in

| Source | Status |
|---|---|
| ChatVault | Search. Stays off this screen. |
| CosmoEvolution 3D | Visualization only. |
| Connor / Zeta-zero app | Not in this git. Track Q jigsaw is the honest arithmetic face. No operator→ζ lemma. |
| Aron (external compute) | Not in this git. Compute here is Domain Architect itself. |
| PR #43 PWA | Kitchen sink. Keep it. Do not reopen it as this product. |

The store of record is still git. Pictures are not a proof. T3b stays open.
