# SND and “6” — labeled leftovers, not a proof

**Status:** cleanup card, 13 September 2026  
**Math/proofs:** parked until Monday. Nothing here is a close.  
**Clay:** **NOT CLAIMED.** DA-VC-01 stays **FAIL.**  
**Do not glue.** No sentence on this page makes SND leftover = \(T_{j\leftarrow j}\) = WRITE (6) = estimate Step 6 = Q6.

These are **different leftovers**. Cleanup = label, park, unmix.

## SND (one family, two remainders)

| What it is | Remainder name | Status |
|---|---|---|
| Ring SND (usable fluids criterion) | \(\inf J/X\ge c_*\) for large \(H^1\) | **CONDITIONAL** / OPEN. Energy does not give it. |
| Paper2 / Statement-B / SND-on-torus | simplex closeness; leftover-split #3; chain steps **7–8** (Lemma 6.1 + dynamic SND) | **CONDITIONAL** / OPEN. T2 Gronwall TeX stays **MISSING**. |
| Route J | all-\(N\) frozen gap | **STILL OPEN** / NUMERICAL. Separate from SND leftover. |
| Faces that say “SND implies global regularity” | — | Archive; claim line is **OPEN** / withdrawn. Not generic 3-D. |

SND leftover is **not** \(T_{j\leftarrow j}\). Route J stays separate.

## “6” (several, labeled separately)

| Which 6 | Remainder name | Status |
|---|---|---|
| **Estimate Step 6** (moved this week onto the Door-1 note) | proposed closure **IF (A)** (or depletion implying (A)); remainder of that program is still \(T_{j\leftarrow j}\) | **PARK** / not claimed. Do not write the proof. |
| **WRITE (6) / H1** | Bad-pair / \(A_{\mathrm{bad}}\) on \(Q_r\) (Lemma I on the ball) | **PARK.** Named geometric leftover. Not proved. Not this Door-1 remainder. Lives on `origin/cursor/tjj-estimate-chain-e5c5` `docs/WRITE_6.md`. |
| **Statement-B table Step 6** | conditional \(H^1\) **IF [SND]** (program row NS-7/NS-8) | Different object. Leftover of **that** chain is **7–8**, still **OPEN**. Not Clay. Not “SND implies generic 3-D.” |
| **Leftover-split item #6** | — | **Does not exist.** The list is three: (1) swirl strain, (2) Ring SND, (3) Paper2 simplex. |
| **OPEN-board math slot 6** | — | **Does not exist.** Math list is 1 GAP1, 2 Route J, 3 NS-open, 4 \(T_{j\leftarrow j}\). Software id `e8-nav42-product` is REJECTED Track C, not Step 6. |
| **Q6** | constitutive Q1–Q6 as Clay NS | **DISCARD.** Other PDE. Usable Q6 is the arithmetic \(H_N\) definition only. **Not** Step 6. |

## What moved this week (inventory only)

- **SND:** leftover-split already named Ring \(J/X\) and Paper2 7–8 as CONDITIONAL. This week added “\(T_{j\leftarrow j}\) is a fourth remainder” — keep that split; do not set \(\sigma_{\mathrm{strain}}=T_{j\leftarrow j}\).
- **6:** estimate Step 6 was packed onto the \(T_{j\leftarrow j}\) board/estimate lines as if it were the leftover. WRITE (6)/H1 was mentioned as left on the Sept 11 branch. Both stay named and **not** the Door-1 remainder.

## What this does not do

- It does not prove \(T_{j\leftarrow j}\), (A), estimate Step 6, WRITE (6), or Paper2.
- It does not merge. It does not touch PR #30.
- It does not invent missing T2 Gronwall TeX.
- It does not treat Q6 as Step 6.
