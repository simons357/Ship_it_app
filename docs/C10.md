# C-10?

18 September 2026.
Phone card. Not a theorem. Not leftover 5
closed. Route A parked after the
depletion-canon mine.

C10 is the leftover-5 candidate on
classical unaugmented 3-D Navier–Stokes:
local positive stretch \(a_+\) is
supposed to deplete hard enough that
the same-scale block \(T_{j\leftarrow j}\)
enters (A). It does not, as an
unaugmented mechanism.

NS not solved. Clay Statement B open.
Do not repair Theorem H. Do not start
an A2 staircase. Do not weld this
card to \(T_c\).

---

## (A), verbatim

On the unaugmented shell face, with
\(D_j=\|\nabla\Delta_j\omega\|_2^2\)
and \(0<\theta<1\),

\[
\boxed{
(\mathrm{A})\qquad
(T_{j\leftarrow j})_+
\le
\theta\nu D_j
+
R_{\mathrm{allowed}}.
}
\]

**Allowed \(R\):** energy \(\mathcal{E}\),
shell enstrophies \(\{Z_k\}\), maybe a
direction factor.

**Forbidden in \(R\):** \(\dot e_j\),
\(\dot Z\), \(\dot Z_j\), \(\Lambda'\).

Closing (A) is not Clay B.

---

## Verdict

\[
\boxed{\text{C10 does not produce (A)}}
\]

Identities through TJJ-\(\alpha\) sit.
The first new analytic arrow does not.

| Question | Answer |
|---|---|
| Does NS force depletion of \(a_+\)? | **NO.** Empty. Occupancy samples refused. |
| Does depletion \(\Rightarrow\) (A)? | Tautology if depletion means the \(a_+\) line. |
| Noncircular? | **NO** on the first new arrow. Template remainders and \(\|a_+\|_\infty\) are BKM-adjacent or are (A). |
| Energy-linear (A) as an absolute bound? | **False.** Dies on \(u^\lambda\) and on amplitude. |
| Closer to done than BKM because it is phrased dynamically? | **NO.** Same unresolved hope in different clothes. |
| Leftover 5 closed because “C10 died”? | **NO.** “Died” means this attempt failed. The leftover stays OPEN. |

**Failed arrow:** TJJ-template \(\to a_+\) estimate (Step VI).

Do **not** repair it by inserting
\(\|\nabla u\|_\infty\le\Gamma\), a
geometric tail, occupancy, or BKM.
That changes the question.

---

## What sits (algebra, not a bound)

Exact, on the axisymmetric-with-swirl
face used for the TJJ identities:

- AS-Id: \(\tfrac12\dot Z_j+\nu D_j=T_j\).
- AS-Split: IR / local / UV.
- TJJ-Trans: local transport vanishes.
- TJJ-\(\alpha\): main stretch is
  \(\int\alpha_{\mathrm{loc},j}\lvert\Delta_j\omega\rvert^2\).
- One-sided: that integral
  \(\le\|a_+\|_\infty Z_j\). Not an
  estimate of \(a_+\).

Constantin kernel and Miller/Chae
\(\langle S,\omega\otimes\omega\rangle=-4\int\det(S)\)
also sit as algebra. They are the
**converse** of Arrow 2: if \(a_+\)
(or \(\lambda_2^+\)) is large, enstrophy
can grow cubically. NS does not force
them small.

---

## What does not sit

Needed after the template, with
\(\varepsilon<\theta\):

\[
\|a_+\|_\infty Z_j
+
\text{(commutator remainders)}
\le
(\theta-\varepsilon)\nu D_j
+
R_{\mathrm{allowed}}.
\]

No seated proof. The integrable
substitute \(\int a_+\,\lvert\Delta_j\omega\rvert^2\)
is the same claim (Door-3 / C6).

Commutators that insert
\(\|u_{\mathrm{loc}}\|_\infty\) or
\(\|\nabla u_{\mathrm{loc}}\|_\infty\)
are BKM-adjacent. Bernstein on those
remainders is the cubic wall.

18 Sep Route A1 mine: no outside
depletion theorem yields a noncircular
\(\int_0^T a_+\,dt<\infty\).
Constantin–Fefferman, Beirão da Veiga–
Berselli, Grujić, Miller / Neustupa–Penel,
frequency-localized LPS/BKM are
**criteria**. They assume the geometric
smallness Arrow 2 was supposed to derive.

---

## Adversarial families (do not revive C1–C5)

- High-tail shear / \(v_L\): local block
  typically zero. Inert. Does not support
  C10.
- Amplitude \(u=Aw\): \(\|a_+\|_\infty Z_j\sim A^3\)
  against \(\nu D_j\sim A^2\). Absolute (A)
  fails unless \(a_+\) depletes like \(1/A\).
  C10 does not produce that.
- Concentration \(u^\lambda=\lambda^{3/2}\varphi(\lambda x)\):
  \((T_{j\leftarrow j})_+/(\theta\nu D_j+C\mathcal{E}Z_j)\sim\lambda^{1/2}\to\infty\).
  Energy-linear (A) is **false**.

Conditional on an explicit
\(\|a_+\|_{L^1_t L^\infty_x}<\infty\),
C10 becomes a criterion downstream of
BKM. Publishable as conditional. Not
the missing estimate (A).

---

## Independence lock

C10 does not have to work for centered
drift. Drift does not have to work for
C10. There is no seated comparison of
\((T_c)_+\) with \(\sum_j(T_{j\leftarrow j})_+\).
Do not merge the lanes.

SND is an instrument. Persistence (P)
under only \((M,\nu,\rho_*)\) is **false**
on Family H ([PR #101](https://github.com/simons357/Ship_it_app/pull/101)).
Do not repair Theorem H.

Exact-shell \(K\le 16/9\) is CLAIMED,
frozen. Not unrestricted \(\star\). Not
regularity. No new 9D sweeps.

---

## Route A1 (18 Sep) — park, do not patch

Question asked: what does NS itself
force \(a_+(t)\) to do?

Verdict: **UNRESOLVED.**

Per the close-plan stopping rule:
**park Route A. No A2 staircase.
Move to Route B.**

If a later write produces a genuinely
new exact identity that bounds \(a_+\)
without \(H^1\), \(L^\infty\), BKM, or
Bernstein, reopen Arrow 2 under that
identity. Do not reopen it by citing a
harder criterion.

---

## Next write (not this card)

Route B, independently: useful integrable
\(K\) for centered \(T_c\), or a named
death. Do not restore unrestricted
\(\star\). Do not weld \(a_+\) to \(T_c\).

Crossover only if (A) sits. It does not.

---

## Pointers (other branches; not re-proved here)

- Full chain: [PR #103](https://github.com/simons357/Ship_it_app/pull/103)
  `docs/ns-review/C10-CHAIN.md`
- Same-scale candidates: [PR #102](https://github.com/simons357/Ship_it_app/pull/102)
- Route A1 mine: [PR #108](https://github.com/simons357/Ship_it_app/pull/108)
  `docs/ROUTE-A1.md`
- Operator order: [PR #107](https://github.com/simons357/Ship_it_app/pull/107) /
  [PR #108](https://github.com/simons357/Ship_it_app/pull/108)
  `docs/NS-CLOSE-PLAN.md`

Machine lock: [`data/c10/2026-09-18.json`](../data/c10/2026-09-18.json).
