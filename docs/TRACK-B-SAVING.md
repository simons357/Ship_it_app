# NS climb as an a priori

`python3 scripts/track_b_saving.py`  
`python3 scripts/da_machine.py trackb`

Dated 4 September 2026. The PDE is not being tuned.
B20: \(c\) readable on the blob and on B18 paths.
Blob \(t=0\) does not produce \(c\ge 8\). Path means
do not. This write asks whether that reading closes
\(X\). It does not.

---

## The knob on this write

Tesla: \(c\) the field makes, not a \(c\) we type.
It did not give you \(c=8\). \(j_{\mathrm{bar}}\) versus
typed \(j_*\) at \(t=0\) is an offset, not a climb.

No \(Q_1\). No \(\varepsilon\). No Biot–Savart slogan.
Do not spawn \(n=64\). Do not write \(c=8\) into the PDE.

---

## What the apparatus does

**B31, pass.** Field \(c\), blob \(t=0\) miss, and
path-mean miss are readable together. Same caches
as B20. No new FFT.

**B31a, fail** of “a field climb at this box closes
\(X\).” Viscous means stay negative. A missing saving
rate is not continuation.

**B31b, fail** of “\(j_{\mathrm{bar}}>\text{typed }j_*\)
is continuation.” Packet \(j_{\mathrm{bar}}\approx 2.97\)
versus typed \(2\) is a static offset. Then it falls.

**B31c, fail** of “viscosity pulling \(j_{\mathrm{bar}}\)
down is a class that bounds \(X\).” Dissipation owned
the barycenter here. That is a reading, not a type.

**B31d, fail** of “reading \(c\) is an integral bound
on the max vorticity.” A barycenter rate is not
\(\int\|\omega\|_\infty\).

**B31e, fail** of “matching the prescribed-\(c\) sketch
closes \(X\).” Scored as B21e / B32. A short window
is not the sitting.

**B31f, fail** of “this retunes the PDE.” \(c\) is a
knob on the estimate.

**B20e, fail** of “a field climb closes \(X\).”
Scored here.

**B30e, fail** of “the climb leftover closes \(X\).”
Leftover is now scored.

---

## They work it

**Tesla.** You typed \(c=8\) and the ODE sat. The field
at this box went the other way. Do not type the saving
rate into the equation.

**Leray.** Viscosity pulled \(j_{\mathrm{bar}}\) down.
That is my dissipation, not a ladder.

**Ladyzhenskaya.** Same weight, both sides, at the
scale you are actually on. I still will not give you
\(\varepsilon\) so you can skip the climb the field
refused.

**Majda.** The blob stayed CONC. Occupation of CONC
is not a cascade. A fall is not a class.

**Beale.** Nobody votes a falling barycenter into
\(\int\|\omega\|_\infty\).

**Feynman.** Missable: \(t=0\) \(c\),
\(\Delta j_{\mathrm{bar}}/T\), and whether a missing
rate is a bound. The first two held. The a-priori
slogan missed.

**Einstein.** The object stayed the classical field.
A rate you typed is not a rate the metric produced.

**Operator.** The saving climb is scored. B20e is
scored. Climb sketch is not an a priori (B21e). Next: finer box (B22e). Finer stays B22e.
Do not spawn \(n=64\). B4c stands. Do not cancel to
\(\Phi\). Do not write \(c=8\) into the PDE.

---

## Score

| id | Verdict | What it is |
|---|---|---|
| B20e | **fail** | field climb closes \(X\) |
| B30e | **fail** | climb leftover closes \(X\) |
| B31 | **pass** | field \(c\), blob miss, path-mean miss readable |
| B31a | **fail** | a field climb closes \(X\) |
| B31b | **fail** | \(j_{\mathrm{bar}}\) offset is continuation |
| B31c | **fail** | visc fall is a class |
| B31d | **fail** | reading \(c\) is \(\int\|\omega\|_\infty\) |
| B31e | **fail** | matching the prescribed-\(c\) sketch closes \(X\) |
| B31f | **fail** | this retunes the PDE |
| domain B | **open** | finer leftover is B22e |

Tesla’s line: \(c\) the field makes. It did not give
you \(c=8\).
