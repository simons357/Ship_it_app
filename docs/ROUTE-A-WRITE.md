# Route A write — pairing, not \(L^\infty\)

16 September 2026.
**G1. Write or kill. Not a theorem.
Not leftover 1. NS not solved.**

Path: [`PATH-TO-CLOSE.md`](PATH-TO-CLOSE.md).
C10 packaging: [`C10-CHAIN.md`](C10-CHAIN.md).
Identities: [`AXISYM-SHELL.md`](AXISYM-SHELL.md).
Arithmetic: `python3 scripts/route_a_write.py`.

Do not work Theorem H.
Do not extend 9D sweeps.
Do not invent depletion.
Do not weld \(\star\).

---

## Why this page exists

C10 named
\(a_+:=\|(\alpha_{\mathrm{loc},j})_+\|_\infty\)
and asked for
\(\int a_+<\infty\) from the
dynamics. That arrow is
**unwritten**. Bounding
\(a_+\) by
\(\|\nabla u_{\mathrm{loc}}\|_\infty\)
is BKM, not an a priori.

The sitting identity never
needed the \(L^\infty\):

\[
T_{j\leftarrow j}
=\int\alpha_{\mathrm{loc},j}\,\lvert\Delta_j\omega\rvert^2
+T_{j\leftarrow j}^{\mathrm{comm}}.
\]
(TJJ-α + TJJ-Trans; EXACT.)

One-sided stretch
\(\int\alpha\,\lvert\Delta_j\omega\rvert^2\le a_+ Z_j\)
is the definition of \(a_+\).
It is a majorant, not a
dynamical bound.

**This write replaces the first
packaging.** Control the pairing.
If the pairing cannot be
controlled without \(H^1\),
\(L^\infty\), or BKM, Route A
dies as an a priori and we
stop patching C10.

---

## Target A-pair (OPEN)

On the local block,
\[
P_j
:=\int(\alpha_{\mathrm{loc},j})_+\,\lvert\Delta_j\omega\rvert^2,
\qquad
C_j
:=(T_{j\leftarrow j}^{\mathrm{comm}})_+,
\]
\[
P_j+C_j
\le
\varepsilon\nu D_j
+K_j(t)\,Z_j,
\]
with \(\varepsilon\in(0,1)\) and
\(K_j\) **useful**: controlled
by the energy inequality or by
a named cancellation in the
pairing, and
\(K_j\in L^1_{\mathrm{loc}}\)
on \(\{Z_j>0\}\).

If this sits, (A) sits with
the pairing in place of
\(a_+ Z_j\), and leftover 5
can move after the far Young.
That is **not** global \(X\).

---

## Candidates already dead

| id | Sentence | Status |
|---|---|---|
| A0 | \(K_j=a_+\) and \(a_+\le\|\nabla u_{\mathrm{loc}}\|_\infty\) | **DEAD as a priori.** BKM. |
| A1 | Energy-linear \(P_j\le C\,\mathcal E\,Z_j\) | **DEAD.** C1 / TJJ-E-false: \(T/D\sim\lambda^{1/2}\). Amplitude: pairing \(\sim A^3\), energy \(\sim A^2\). |
| A2 | Bernstein \(K_j\lesssim 2^{cj}\|u\|_{H^1}\) | **DEAD as a priori.** Cubic wall. Gronwall wants the \(H^1\) ceiling. |
| A3 | All-data geometric depletion of \(\lvert\cos\alpha_3\rvert\) | **DEAD as a priori.** B25a. Constantin–Fefferman is the converse. |
| A4 | Assume [ρ] or assume \(\int a_+<\infty\) | **Conditional.** Not this write. |
| A5 | Bound \(P_j\) by \(\dot Z_j\) or by \(\Lambda'\) | **DISCARD.** Restates the leftover. |

Shears: \(\alpha=0\), \(P_j=0\),
\(T=0\). Holds as \(0\le 0\).
Does not test A-pair.

Amplitude: \(P_j\sim A^3\)
matches \(a_+ Z_j\sim A^3\).
Any useful \(K_j Z_j\) must
be cubic in \(A\) unless a
cancellation kills the
leading stretch.

Equal-shell \(v_L\): no
universal \(t=0\) floor.
A-pair is a rate, not a
floor.

Growing-layer \(v_n\): does
not resurrect ★. Do not
merge A-pair with \(T_c\).

---

## What is still allowed

A useful \(K_j\) has to come
from the **pairing itself**:

- a divergence / pressure
  structure that moves
  stretch onto a perfect
  derivative or onto
  \(\varepsilon\nu D_j\);
- a monotone quantity along
  the filtered vorticity
  that controls \(P_j\)
  without embedding the
  unknown field;
- an exact cancellation
  that kills \(C_j\) or
  absorbs it into
  \(\varepsilon\nu D_j\)
  without Bernstein.

None of those three sits on
this page. They are the
only remaining shapes. If
the next estimate is not
one of them, it is a
repackaging of A0–A5.

TJJ-template still sits
with an \(L^\infty\)
remainder. That remainder
is \(C_j\) estimated by
Bernstein. It is A2 unless
replaced.

---

## Brutal questions

1. Does the proposed \(K_j\)
   use \(\|\nabla u\|_\infty\),
   \(X\in L^\infty_t\), or BKM?
   If yes: dead as an a priori.
2. Does it scale like \(A^2\)
   in amplitude? If yes:
   dead (A1).
3. Does it grow like
   \(2^{cj}\) from Bernstein?
   If yes: dead (A2).
4. Is it
   \(K_j=(P_j+C_j-\varepsilon\nu D_j)_+/Z_j\)?
   If yes: tautological.
5. Does it assume leftover 5
   or leftover 1? If yes:
   not this write.

Until a \(K_j\) answers no
to all five, A-pair is
**OPEN**, not seated.

---

## What this page does not do

It does not seat (A).
It does not bound \(a_+\).
It does not move leftover 5.
It does not imply global \(X\).
It does not merge with \(T_c\).
It does not restore H.

C10’s \(a_+\) remains the
named crude majorant.
The living Route A target
is A-pair.

---

## Lock

G1 is A-pair, not
\(\|(\alpha)_+\|_\infty\).
A0–A5 are dead or discard.
Three shapes remain; none
sit. Route A is write or
kill, not a burial.
Catalog B open stays 1.
NS not solved.
