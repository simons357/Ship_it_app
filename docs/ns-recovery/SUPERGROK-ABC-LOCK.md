# SuperGrok ABC_λ filing — locked

**Locked:** 10 September 2026  
**This wording is the record.** Longer notes do not override it.

**NS is not solved. Lemma★ is OPEN.**

---

Keep SuperGrok’s honesty line. Refuse the falsifier stamp.

Shared line that sits: the table is an evaluator, not a proof. Exact core ≠ proof. Lemma★ still open. Unaugmented NSE not solved. H1 is a different integral and was not run on ABC_λ. Augmented / Q-stack is out of this book.

## What SuperGrok overclaimed

“Kills Target A / CS / Lemma★ as a closer” and “falsifier-of-record: yes, pending exact-script confirmation” treat a finite λ³ fit as a kill. This book’s rule: \(\mathcal R_\star\to\infty\) kills ★; one large finite value only raises \(C_{\mathrm{geom}}\). Largest number on disk is **0.327** at λ = 8 (FFT, after reverse). That is not a falsifier.

The exact script is **not** pending. `cs_remainder_exact_check.py` already ran. It does four jobs:

1. 3-mode triad vs `probe` (on that field \(T_c=0\); \(D_s\) matches).
2. Export 99% energy modes and sum the triad — λ = 2, 3, 4 only.
3. Put that **same** truncated field on a grid: FFT \(T_c\) = triad to \(10^{-13}\).
4. \(v(n\cdot)\) leaves \(\mathcal R_\star\) flat.

It never ran λ = 8 or 16. SuperGrok’s “reprint on 2, 4, 8, 16 then stop patching ★” asks for rows that are not in the script. More finite rows still would not close the kill.

## Claude’s two items (locked)

1. **Same \(\mathcal R_\star\)?** Yes after reverse. JSON `R_star` is \((T_c)_+\) and is **0** here (\(T_c<0\)). The published climb is `R_star_signed` \(=T_c^2/(D_s E Y)\) = canonical \(\mathcal R_\star(-v)\). Not an ABC-only variant.
2. **H1 on ABC_λ?** Untested. Named only.

N-shell max saturates; this family does not on the grids they ran. That is a live kill-lane clue. It is not a logged reject of ★.

Do not stop patching Lemma★. Do not start H1 from this screenshot. Do not glue Q-stack.

Score detail: [`CS-REMAINDER-VS-DA-REJECT.md`](CS-REMAINDER-VS-DA-REJECT.md) · PR https://github.com/simons357/Ship_it_app/pull/45
