# SuperGrok / DA REJECT score — refuse the falsifier stamp

**Date:** 10 September 2026
**This book:** `cursor/unaugmented-r4-vorticity-f80e` · PR https://github.com/simons357/Ship_it_app/pull/24
**Scored note:** PR https://github.com/simons357/Ship_it_app/pull/45 · `docs/ns-recovery/CS-REMAINDER-VS-DA-REJECT.md`

**NS is not solved. Lemma★ is OPEN.** This note is a score.
It does not stamp a kill or a proof. Do not start H1.
Do not glue Q-stack. Do not stop patching Lemma★.

---

## Shared line that sits

The table is an evaluator, not a proof.
Exact core ≠ proof. Lemma★ still open.
Unaugmented NSE not solved.
H1 is a different integral and was not run on ABC_λ.
Augmented / Q-stack is out of this book.

---

## What SuperGrok overclaimed

“Kills Target A / CS / Lemma★ as a closer” and
“falsifier-of-record: yes, pending exact-script confirmation”
treat a finite λ³ fit as a kill.

This book’s rule: \(\mathcal R_\star\to\infty\) kills ★.
One large finite value only raises \(C_{\mathrm{geom}}\).
Largest number on the gate table is **0.327 at λ=8**
(FFT, after reverse). That is not a falsifier.

The exact script is **not pending**.
`scripts/ns_attacks/cs_remainder_exact_check.py` already ran.
Four jobs:

1. 3-mode triad vs `probe` (on that field \(T_c=0\); \(\mathcal D_s\) matches).
2. Export 99% energy modes and sum the triad — λ=2, 3, 4 only.
3. Same truncated field on a grid: FFT \(T_c\) = triad to \(10^{-13}\).
4. \(v(n\cdot)\) leaves \(\mathcal R_\star\) flat.

It never ran λ=8 or 16. SuperGrok’s “reprint on 2, 4, 8, 16
then stop patching ★” asks for rows that are not in the script.
More finite rows still would not close the kill.

This branch briefly computed extra finite rows after that ask.
Those rows are withdrawn as a stamp. A larger finite number
is still a larger finite \(C_{\mathrm{geom}}\).

---

## Claude’s two items, locked

1. **Same \(\mathcal R_\star\)?** Yes after reverse.
   JSON `R_star` is \((T_c)_+\) and is 0 here (\(T_c<0\)).
   The published climb is `R_star_signed` \(=T_c^2/(\mathcal D_s E Y)\)
   = canonical \(\mathcal R_\star(-v)\). Not an ABC-only variant.
2. **H1 on ABC_λ?** Untested. Named only.

---

## What DA actually rejected

No Target A stamp until the field matches the exact triad core.
That rejected the first reconstruction: FFT-only stamp, then
full-field vs 99% cutoff treated as one field. Those are
different fields. \(T_c\) lives in the tail (`Tc_rel ≈ 0.53`–`0.56`).

The four-job script answers that gate on same-field identity.
It does not convert a finite climb into a kill.

---

## What still fails the kill rule

| Claim | This book |
|---|---|
| Target A / CS / ★-as-closer killed | **Refuse.** Finite climb. |
| Falsifier-of-record | **Refuse.** |
| Stop patching ★ | **Refuse.** |
| λ=2,4,8,16 exact-core table | **Not in the gate script.** Core is λ=2,3,4. FFT gate table ends at λ=8. |
| NS solved | **No.** |
| Start H1 from ABC_λ | **No.** |
| Glue Q-stack | **No.** |

N-shell max saturates; this family does not on the grids
they ran. Live kill-lane clue. Not a logged reject of ★.

Phone: [`../CS-REMAINDER.md`](../CS-REMAINDER.md).
JSON: `results/cs_remainder_bump/exact_core_check.json`.

Stay in this chat.
