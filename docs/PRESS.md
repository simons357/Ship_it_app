# Full-court press — Lemma A

24 September 2026.
**Lemma A sits.
\(\Phi_e\le W_{\lambda_e}\) on the unit torus.
Lemma B stays OPEN.
The SBP identity is not a useful K.
Not a close. ★ stays killed.
Catalog B open stays 1.**

SBP identity:
[`SBP.md`](SBP.md).
Incoming ledger:
[`CENTERED-LEDGER.md`](CENTERED-LEDGER.md).
Chart reset:
[`RESET.md`](RESET.md).
Relative width:
[`WIDTH.md`](WIDTH.md).
Overnight page:
[`DA-NS-2.md`](DA-NS-2.md).
Target:
[`CENTERED-DRIFT.md`](CENTERED-DRIFT.md).
Sign gate:
[`SIGN-GATE.md`](SIGN-GATE.md).
Finite-\(N\) run:
[`SIGN-RUN.md`](SIGN-RUN.md).
Machine: `python3 scripts/press.py`.
Does not overwrite `stokes_moments.py`.
Do not start leftover 1.
Do not weld \(\star\).
Do not cash B★ as G5.
Do not seat JGC.
Do not invent a bridge.
Do not run Taylor–Green.
Do not mix Heavy scalene
homochiral loops into this
heterochiral charge bill.

---

## Three kinds of sentence

**Exact.** Lemma A on
\(m\ge 1\), \(\kappa\ge 1\).
\(\Phi_e\le W_{\lambda_e}\).
Young L2. Sharp L3 without
\(\eta\). Off-torus kill.

**Named comparison.**
RESET \(\lvert\Lambda-\lambda_e\rvert=c\sqrt{\Lambda}\).
A chart scale, not a payment.

**Not on this desk.**
Lemma B. Taylor–Green
\(\Phi_e/Y\). Scalene
homochiral loops. \(\mathbb{R}^3\).

---

## Lemma A — PROVED
on the unit torus

Need, for \(\kappa\ge 1\) and
\(m\ge 1\),

\[
\tfrac12(m-\kappa)^2
(m^2+2\kappa m+2\kappa^2)
\le
m^2(m^2-\kappa^2)^2.
\]

Cancel \((m-\kappa)^2\)
(\(m=\kappa\) is \(0\le 0\)):

\[
m^2+2\kappa m+2\kappa^2
\le
2m^2(m+\kappa)^2.
\tag{A}
\]

Set

\[
P(m,\kappa)
=
2m^2(m+\kappa)^2
-(m^2+2\kappa m+2\kappa^2).
\]

Then \(P(1,\kappa)=1+2\kappa\ge 0\).
For \(m\ge 1\), \(\kappa\ge 1\),

\[
P'(m)
\ge
6m(1+2\kappa)+2\kappa(2\kappa-1)
\ge 0.
\]

So \(P\) is increasing in \(m\)
and nonnegative. (A) sits.

The modal ratio is

\[
C_\kappa
=
\sup_{m\ge 1}
\frac{m^2+2\kappa m+2\kappa^2}
{2m^2(m+\kappa)^2}
\le 1.
\]

At \(m=1\),
\(C_\kappa=(2\kappa^2+2\kappa+1)/(2(\kappa+1)^2)\to 1\)
as \(\kappa\to\infty\).
At \(m=\kappa\),
\(5/(8\kappa^2)\).
At \(m\to\infty\), \(0\).
The constant \(1\) is sharp
and not attained at finite
\(\kappa\).

Therefore, on every mean-zero
field on the unit torus,

\[
\Phi_e
\le
D_e^{\mathrm{frozen}}
=
W_{\lambda_e}
=
\sum_k
\lambda_k(\lambda_k-\lambda_e)^2
\lvert a_k\rvert^2.
\tag{L1}
\]

The lowest lattice mode is
\(m\ge 1\). Poincaré. The
\(k=0\) mode is absent:
\(\varphi_e(0)=\kappa_e^4\)
and the frozen weight at
\(\lambda=0\) is zero, so a
constant is not controlled.
Mean-zero is required.

This is a numerical inequality
in torus units. It is **not**
scale-invariant. Off the
torus, \(m=0.1\), \(\kappa=0.5\)
kills (A). Do not export to
\(\mathbb{R}^3\).
[`WIDTH.md`](WIDTH.md).

Locked today.

---

## Sharp L3 — Young is weaker

The sitting identity
already is

\[
W_{\lambda_e}
=
\mathcal D_s
+
X(\Lambda-\lambda_e)^2.
\]

[`RESET.md`](RESET.md).
Young

\[
(\lambda-\lambda_e)^2
\le
(1+\eta)(\lambda-\Lambda)^2
+(1+\eta^{-1})(\Lambda-\lambda_e)^2
\]

gives L2, and L2 is true.
It is strictly weaker than
\(W\). Do not keep \(\eta\).

Sharp L3:

\[
\Phi_e
\le
\mathcal D_s
+
X(\Lambda-\lambda_e)^2.
\tag{L3}
\]

In torus units,

\[
\frac{\Phi_e}{Y}
\le
\frac{\mathcal D_s}{Y}
+
\zeta_e^2,
\qquad
\zeta_e
=
\frac{\Lambda-\lambda_e}{\sqrt{\Lambda}}.
\tag{L4}
\]

The low tail, the high tail,
and the core are all inside
this one comparison. No
occupancy theorem. The radial
apparatus collapses to live
variance plus center
displacement.

\(r^2\) alone still does not
control \(\Phi_e/Y\).
\(\mathcal D_s/Y=\Lambda r^2\)
plus \(\zeta_e^2\) does, on
this torus.
[`SBP.md`](SBP.md).

An identity bound on
\(\Phi_e\) is not a bound on
\(T_c\). Integrating
\(\Phi_e'/Y\) still returns
\(\Phi_e Y'/Y^2\).

---

## RESET \(\lvert\zeta_e\rvert=c\)
is a named chart

\(\lvert\Lambda-\lambda_e\rvert=c\sqrt{\Lambda}\)
makes \(\zeta_e^2=c^2\) a
fixed number on this torus.
That is a choice.

It is not the unique
dimensionless displacement.
\((\Lambda-\lambda_e)/\Lambda\)
is dimensionless in general
units. Matching the named
BROAD size \(\mathcal D_s/Y\sim\sqrt{\Lambda}\)
would need
\(\lvert\Lambda-\lambda_e\rvert\sim\Lambda^{3/4}\),
a different threshold.

The rule does not pay
summability. Frequent reset
is still circular for
\(\mathrm{TV}(\Lambda)\).
JGC stays a target.
[`RESET.md`](RESET.md).

Live tracking \(\lambda_e=\Lambda\)
makes \(W_\Lambda=\mathcal D_s\)
and \(\Phi_e\le\mathcal D_s\),
and it moves the weight.
Frozen versus live stays
unmixed.
[`SBP.md`](SBP.md).

---

## Charge — still a different bill

\[
2\kappa_e^3 Q_a
=
\kappa_e^3 H_a'
+
2\nu\kappa_e^3 D_a
\]

if \(\tfrac12 H_a'+\nu D_a=Q_a\).
The derivative is a capacity.
The viscous remainder is not
paid by \(\mathcal D_s\) on a
narrow annulus:
\(D_a\sim\kappa X\) while
\(\mathcal D_s\) can be tiny.
That confirms the charge bill
is genuinely different.

Lemma B, the joint
charge / epoch ledger, is
**not written** and **not
seated**. Do not invent it.

Heavy keeps scalene
homochiral loops on their
own desk. Do not mix them
into this heterochiral
charge calculation.

A finite Taylor–Green
\(\Phi_e/Y\) is not \(C\).
Do not run it as a close.

---

## What the press paid,
and what it did not

Paid today: the low-tail
scare on the unit torus.
\(\Phi_e\) is bounded by the
sitting frozen variance.

Not paid: \(2\kappa_e^3 Q_a\),
epoch variation, and the
Y-cousin integral. Those
were leftover 4 / G4
already.
[`DA-NS-2.md`](DA-NS-2.md).

The board is smaller. It is
not closed.

---

## What this page is not

- A close. Catalog B open
  stays 1.
- A seating of DA-NS-2.
- A restoration of ★.
- A seating of B★, LX,
  SAG, JGC, or Lemma B.
- A named death of G4.
- Leftover 1. Do not
  start H1.
- A Taylor–Green run.
- An \(\mathbb{R}^3\) theorem.
- [`PATH-TO-CLOSE.md`](PATH-TO-CLOSE.md).

---

## Lock

Lemma A sits. \(\Phi_e\le W_{\lambda_e}\) on the unit torus.
Lemma B stays OPEN.
The SBP identity is not a useful K.
G4 stays OPEN. ★ stays killed.
NS not solved.
