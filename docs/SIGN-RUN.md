# Sign run — finite \(N\)

24 September 2026.
**BOTH SIGNS appears on
\(N=2,\dots,8\).
Persistence stays OPEN.
Universal first-order
one-sided narrow depletion
is not killed.
Not a close. ★ stays killed.
Catalog B open stays 1.**

The gate (unaltered):
[`SIGN-GATE.md`](SIGN-GATE.md).
Lemma A (unaltered):
[`PRESS.md`](PRESS.md).
Triangles:
[`FOURIER-TRIANGLE.md`](FOURIER-TRIANGLE.md).
Machine: `python3 scripts/sign_run.py`.
Seeds: `results/A_N_plus_seeds.json`.
Does not overwrite `stokes_moments.py`.
Do not start leftover 1.
Do not weld \(\star\).
Do not invent a bridge.
Do not run Taylor–Green.
Do not mix scalene
homochiral loops.
Do not alter \(L_{1,N}\).
Do not alter Lemma A.
No more potentials.

---

## Three kinds of sentence

**Exact.**
\(T_c=\Lambda\langle\delta,T\rangle+\sum\delta_m^2 T_m\).
Equal-length kills the
\(T_c\) weight.

**Printed, finite \(N\).**
On the eight narrowest
genuine neighbors at each
\(N=2,\dots,8\), heterochiral
helical labels realize both
signs of \(L_{1,N}\) with
\(\lvert\rho\rvert\ge 0.84\).
\(\lvert R_{2,N}/L_{1,N}\rvert\)
falls \(0.047\to 0.0045\).
Homochiral \(L_{1,N}\) stayed
under the floor on those
scored triangles.

**Not on this desk.**
Persistence \(N\to\infty\).
A seated death of universal
one-sided depletion.
An \(I_3\) bridge.
Vandermonde as a remainder.
Lemma B. Evolution /
\(\Theta_N\).

---

## What was run

Neighboring annulus
\(\lvert k\rvert\in[N-1,N+1]\).
Integer triangles
\(p+q=k\), all three in the
annulus, not collinear.
Equal-shell triangles are
dropped: that is not a
neighbor, and it is not
sign depletion.

Helical basis \(h^s\),
\(s=\pm 1\). Homochiral means
all three labels equal.
Heterochiral is the rest.
Amplitudes \(1\). Not Heavy
scalene loops.

The gate stays
\(L_{1,N}=\Lambda_N\langle\delta_N,T_N^{(0)}\rangle\)
with live neighboring \(T\)
as \(T^{(0)}\) of that
snapshot.
\(\rho=\langle\delta,T^{(0)}\rangle/(\lVert\delta\rVert\lVert T^{(0)}\rVert)\).
Floor \(0.05\).

Eight narrowest positive
\(\lambda\)-spreads per \(N\).

---

## Printed table

| \(N\) | neighbors | hetero signs | \(\lvert R_2/L_1\rvert\) |
|---|---|---|---|
| 2 | 534 | both | 0.047 |
| 3 | 1440 | both | 0.023 |
| 4 | 3864 | both | 0.016 |
| 5 | 7812 | both | 0.009 |
| 6 | 12606 | both | 0.0075 |
| 7 | 17142 | both | 0.0057 |
| 8 | 32040 | both | 0.0045 |

NO NEIGHBOR did not appear
on this range.

Homochiral signs on the
scored neighbors: none
above the floor. That is
consistent with a
Vandermonde delay. It is
**not locked**. It does
not rescue heterochiral
NSE.

---

## Seeds \(\mathcal A_N^{+}\)
— finite \(N\) only

Saved when BOTH SIGNS
appears: integer vectors,
helicity, polarizations,
amplitudes. Not merely
\(\delta\) and \(T^{(0)}\).
`results/A_N_plus_seeds.json`.

Example at \(N=8\):

\[
(-8,-4,-1)+(4,0,8)=(-4,-4,7),
\quad
s=(1,-1,-1)
\]

gives \(\rho\approx +0.867\).
The opposite helical
triple gives \(\rho\approx -0.867\).

These are finite-\(N\)
seeds. They are **not**
a persistent family.

---

## What this does not do

The operator’s death
sentence needs a
*persistent* BOTH SIGNS
family with
\(\lvert\rho_N^\pm\rvert\ge c>0\).
A printed range is not
that.

So:

- Static closure is **not**
  stopped.
- Universal first-order
  one-sided narrow
  depletion is **not**
  killed.
- \(\Theta_N\) is **not**
  computed.
- The dynamic frontier
  is **not** opened.
- ONE SIGN is **not** the
  printed hetero bucket,
  so no Gram / lattice
  bridge is asked.
- NO NEIGHBOR stays its
  own empty category on
  this range.

Lemma B stays OPEN.
G4 stays OPEN.
★ stays killed by \(v_n\).

---

## What this page is not

- A close. Catalog B open
  stays 1.
- A seating of DA-NS-2.
- A restoration of ★.
- Persistence.
- Leftover 1.
- A Taylor–Green run.
- [`PATH-TO-CLOSE.md`](PATH-TO-CLOSE.md).

---

## Lock

BOTH SIGNS on \(N=2,\dots,8\). Persistence OPEN.
Universal depletion is not killed.
G4 stays OPEN. ★ stays killed.
NS not solved.
