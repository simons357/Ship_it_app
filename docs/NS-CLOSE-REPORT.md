# How we close unforced Navier–Stokes

16 September 2026.
**Operator report after every peer
note was scored.**
Not a proof. Ordinary NS is not
solved. Catalog B open stays 1.

This page is the answer to:

> Use all the other AIs. Throw the
> crap. Keep the good. Where are
> we. Where do we go.

Adjudication:
[`REPORT-AUDIT.md`](REPORT-AUDIT.md).
Plan:
[`MASTER-PLAN.md`](MASTER-PLAN.md).
Path to a close:
[`PATH-TO-CLOSE.md`](PATH-TO-CLOSE.md).
Route A write:
[`ROUTE-A-WRITE.md`](ROUTE-A-WRITE.md).
B★:
[`BSTAR.md`](BSTAR.md),
[`BSTAR-PROOF.md`](BSTAR-PROOF.md).
Fourier triangle:
[`FOURIER-TRIANGLE.md`](FOURIER-TRIANGLE.md).
First lift:
[`TRIANGLE-LIFT.md`](TRIANGLE-LIFT.md).
Energy-class ladder:
[`ENERGY-K.md`](ENERGY-K.md).
Pairing CS doors:
[`L-DOOR.md`](L-DOOR.md).
Score:
[`NS-STATUS.md`](NS-STATUS.md).
Tape:
[`YES-NO-OPEN.md`](YES-NO-OPEN.md).

This desk is **unaugmented**
Track B. Keep \(1/r^4\). No
\(Q_1\). No \(\Phi\)-cancel. No
\(K(t)\) in the PDE. Theorem A
sits on a different equation.
A is not B. Do not switch.

Honest first sentence: **we do
not have a seated close.**
Talking is not the close.
Two OPEN routes can still carry
an a priori. Both can die.

Do not start leftover 1.
Do not weld \(\star\).
Do not mail a panel.

---

## Where we are now

The leftover is no longer vague.
False bridges are gone. The
live question is:

> Can near-scale nonlinear
> transfer beat viscosity and
> spectral spread, from unforced
> admissible data, without
> assuming \(\|\nabla u\|_\infty\)?

Four buckets only.
PROVED / CLAIMED / OPEN / DEAD.
“LIVE” is not a bucket.

### PROVED

- Spectral identities
  \(X,Y,Z,\Lambda,T_c,\mathcal D_s\).
  \(X\le\|u\|_2^2\Lambda\).
  \((\log\Lambda)'=2(T_c-\nu\mathcal D_s)/Y\).
- Exact shell budget
  \(\frac12\dot X_j+\widetilde D_j=-2^{2j}F_j\).
  Envelope \(D^+J\le\max\dot X_j\).
- Commutator
  \(F_j=\langle[\Delta_j,u\cdot\nabla]u,\Delta_j u\rangle\).
- Stretch \(\le a_+ Z_j\) (EXACT).
  TJJ-Trans, TJJ-α. TJJ-template
  (STANDARD; \(L^\infty\) remainder).
- \(A.2\):
  \(|F_j|\le C\sqrt{M/\nu\lambda_1}\,X^{1/2}\mathcal D^{1/2}\)
  under \(X\le M\). Hölder \(6,2,3\)
  + Poincaré. Not Kato–Ponce.
- Unrestricted
  \(\sup\mathcal R_\star<\infty\)
  is false. Kill: \(v_n\).
  Instantaneous admissible class,
  not a trajectory, not a blowup.
- Displayed Theorem H fails even
  under \(X\le M\). Packet
  \(\Pi_j=F_j-S_j\). Dropping
  \(S_j\ge 0\) kills
  \(|\Pi_j|\). Shear ratio
  \(\to\infty\) at \(K=24,28,32\).
  \(H^1\not\to L^\infty\) in 3-D.
- \(F_j\sim A^3\) kills any
  \(M\)-free quadratic.
- Equal-shell \(v_L\) kills a
  universal \(t=0\) SND floor
  (\(\rho(0)=1/L\)).
- Energy-linear local \(R\) DEAD
  (\(T/D\sim\lambda^{1/2}\)).
- Designed \(\Theta(m^2)\) 9D
  DEAD (Freiman-AP).
- Theorem A sits — extra-stress
  / \(Q_1\)-NS only.

### CLAIMED

Exact-shell \(C=4/3\),
\(K\le 16/9\). Independent
reproduction of the projected
identity and weighted incidence
only. Three-shear \(K_{1,2}=2/3\)
is a floor, not the ceiling.
No more sweeps. Do not write
“9D is claimed.”

### OPEN

- Route A: dynamical \(a_+\)
  that avoids \(H^1/L^\infty/\)BKM,
  then (A) with an energy-class
  remainder.
- Route B: useful \(K\) in
  \(T_c\le\theta\nu\mathcal D_s+K(t)X\),
  \(\theta<1\), \(K\in L^1_{\mathrm{loc}}\).
- Leftover 4 if both die: a
  **different** estimate that
  \(v_n\) does not kill.
- Leftover 5 until Route A
  seats or a field with
  \(\int\rho_j=\infty\) is written.
- Leftover 1 = H1 = WRITE (6).
  Different integral. **Not
  started from this page.**

### DEAD as a close

Ordinary NS solved.
Unrestricted ★ as a bound.
Displayed Theorem H as a bridge.
Universal \(\rho\) floor.
Pólya two pages (\(D^+\) cannot
make a floor).
C10 as a seated theorem.
“C10 died” as a program death.
SND + tail as a seated route.
BKM from an assumed tail as
primary.
\(\Phi\)-cancel / QStack / Ring
as constitutive Track B.
Endgame covering of dead triads.
Blowup-search as this line.
Augmented PDE as this line.

---

## What we kept from the peers

GPT board / bottom line:
false bridges gone; leftover
less vague; stop asking whether
SND proves regularity; two
questions (depletion of \(a_+\);
absorb centered drift); C10
first, centered independently,
H frozen. **KEEP that order.**

Grok 16 Sep handoff:
living order. Two naming fixes
stay: do not write “9D is
claimed”; do not mix leftover 1
with Theorem H.

Rocks:
Official Statement (B) is
unforced. Forced construction
is (C)/(D) if it holds. 11 Sep
note is an evaluation, not an
award. Viscosity-prevents is
this desk **only** as an a
priori from unforced data.

Long Global Regularity write:
NS open; H / \(M\)-free / \(t=0\)
floor dead with named kills;
\(A.2\) sits; exact-shell stays
CLAIMED; BKM demoted; cover
sentence (“active program,
conditional partial, catalog of
kills — not near proof”).
Rec 4 = Route A. Rec 6 =
publication fallback.

Company status map:
standing rule (no prize / QED /
“solved” in public). Swirl is
a map. QStack is an instrument.
Portfolio is not a leftover.

Theorem H audit:
items 1–7 correct. Neither a
repaired H from §8 nor a panel
mailing from this desk.

---

## What we threw

- Unrestricted ★ as OPEN.
  **KILLED** by \(v_n\). Most
  dangerous GPT error.
- “Unconditional peak-fraction
  floor.” \(A.3\) is a \(D^+\)
  ceiling. The \(\Gamma,\Lambda\)
  write is BKM-adjacent and not
  seated.
- Kato–Ponce as the foundation
  of \(A.2\). Seated \(A.2\) is
  Hölder + Poincaré.
- “C10 died” as a program.
  (A) failed to seat. Lane stays
  write-or-kill.
- Pólya two pages. Wrong Dini
  direction. Even a \(\rho\)
  floor leaves \(1-\rho\) free
  at high frequency.
- “Every lane is
  \(\|\nabla u\|_\infty\) from
  \(X\le M\).” Circularity
  **risk**, not a theorem.
  Routes A / B must **avoid**
  that.
- Three lanes as “the same bet
  in different notation.” Same
  difficulty class. Different
  shapes. Different deaths.
- Endgame: \(\Phi\) kills
  \(1/r^4\); QStack / Ring as
  sitting machine; uniform
  triadic covering as the
  leftover; \(Q_6\) spectral
  gap; fold H1 now; mail a
  panel. **NO.**
- Blowup-search
  (Córdoba–Martínez-Zoroa /
  self-similar) as this line.
- Switch to augmented / Theorem A.

---

## How we close it — if we can

A close on this desk is an a
priori that keeps smooth
admissible unforced data
smooth. Not a forced profile.
Not a prize sentence.

Two independent target shapes
are the only ones inside the
machinery already built.

### Route A — first

A-pair: control the stretch
pairing, not
\(\|(\alpha)_+\|_\infty\).
Bounding \(a_+\) by
\(\|\nabla u\|_\infty\) is
BKM. Page:
[`ROUTE-A-WRITE.md`](ROUTE-A-WRITE.md).
Then leftover 5 can move
with the far Young that
already sits.

If it works: leftover 5 moves.
That is **not** leftover 1 and
**not** unrestricted 3-D by
itself. A later write still
has to pass from the local
block to global \(X\). That
arrow does not sit today.

Death: \(a_+\) only from
\(\|\nabla u\|_\infty\);
Bernstein remainder that wants
the \(H^1\) ceiling; an
adversarial family with \(a_+\)
large; assuming the conclusion.
If it dies, record the failed
arrow and stop patching.

### Route B — independent

Useful \(K\) in
\(T_c\le\theta\nu\mathcal D_s+K(t)X\).
Useful means: energy inequality
or a named cancellation in
\(T_c=M-\Lambda N\), not
\(K=(T_c-\theta\nu\mathcal D_s)_+/X\).

If it works:
estimate \(\Rightarrow\Lambda'\le 2K\)
\(\Rightarrow X\) bound on the
torus, in this packaging. One
direction. No converse. ★ stays
killed.

Death: every \(K\) tautological;
★-scaling remainder (\(v_n\)
already kills that); \(K\) is
\(\|\nabla u\|_\infty\); the
centering is split and the
remainder is worse than ★.

### Crossover

Only if (A) sits. Then ask for
an actual inequality from
\(a_+\) to \(T_c\). None sits.
Do not draw a conceptual arrow.

### If both die

Leftover 4 needs a **new**
estimate shape that \(v_n\)
does not kill. Need★ cannot
repair the dead box. Leftover 1
stays OPEN and is still not
started from this page.

---

## Execution order

1. Freeze H, 9D sweeps, designed
   9D, blowup-search as the
   living line, leftover 1,
   \(\Phi\) / Q / Ring.
2. Route A write or kill.
3. Route B write or kill.
4. Crossover only after (A).
5. SND stays an instrument.
   Print \(\rho\), \(j_*\), tail,
   \(F_j\). No persistence.
6. Score every write in four
   buckets. The tape wins.

Publication fallback if nothing
above closes: conditional
partials already in hand, plus
the catalog of named kills.
That is a cover sentence, not
a close.

---

## Lock

Enough for this plan.
Not enough for a close.
Route A then Route B.
Catalog B open stays 1.
NS not solved.
