# Tube budget as an a priori

`python3 scripts/track_b_tube.py`  
`python3 scripts/da_machine.py trackb`

Dated 4 September 2026. The PDE is not being tuned.
B4c budgets \(|I_{\mathrm{tube}}|\) on packets. B5b: the
extra \(1/r^2\) piece, alone, does not. This write asks
whether either reading closes \(X\). They do not.

---

## The knob on this write

Tesla: you asked the tube. Split the dissipation.
Full \(D_{\mathrm{tube}}\) versus the leftover angular
piece. If a ratio that sits below 1 on a packet class
is still not a bound for classical \(X\), the tube has
no more dial that stays legal.

\[
R_D=\frac{|I_{\mathrm{tube}}|}{D_{\mathrm{tube}}},\qquad
R_{\mathrm{ang}}=\frac{|I_{\mathrm{tube}}|}{A_{\mathrm{tube}}}.
\]

On resolved packets \(R_D\) falls through \(10^{-2}\).
\(R_{\mathrm{ang}}\) sits above 1 and climbs. Full
dissipation budgets the source. Angular does not.

No \(Q_1\). No \(\varepsilon\). Do not cancel to
\(\Phi\). Do not spawn \(n=64\).

---

## What the apparatus does

**B24, pass.** Both ratios are readable on the same
packet class. The two knobs disagree.

**B24a, fail** of “the extra \(1/r^2\) piece closes
\(X\).” \(R_{\mathrm{ang}}>1\) and climbs. A failed
Poincaré is not continuation.

**B24b, fail** of “B4c is an a priori for classical
\(X\).” A packet budget is not all data. B4b killed
all-data Hardy. B23c: one triad is not Leray.

**B24c, fail** of “\(R_D\ll 1\) implies \(X\in L^\infty\).”
A ratio is not Beale. Tube-ratio-never-blew-up is
refused the same way DNS-never-blew-up is.

**B24d, fail** of “therefore revive all-data Hardy,
or cancel to \(\Phi\).” B4b and B5e already missed.
Keep \(\Gamma\). Keep \(1/r^4\).

**B24e, fail** of “packet geometry closes \(X\).”
Scored as B14d / B25. Stretching budget is scored
(B15e). Enstrophy balance is scored (B16e).
Coherent leftover is B17e.

**B24f, fail** of “this retunes the PDE.” The ratio
is a knob on the estimate.

**B5f, fail** of “the angular piece closes \(X\).”
Same slogan as B24a. Scored here.

---

## They work it

**Tesla.** Two knobs, two directions. Packet: \(j_*\)
up, angular loses, full \(D\) wins. That is a budget.
It is not a bound. Do not cancel to \(\Phi\) to hide
a ratio that climbed.

**Ladyzhenskaya.** Same weight, both sides, at the
scale you are actually on. The \(1/r^2\) piece is not
that weight once the packet has a frequency. I still
will not give you \(\varepsilon\).

**Kato.** B4c stands. B5b as domination died. The
check could fail. The close failed too.

**Leray.** You did not ask \(\int X\) for \(L^\infty\).
Good. Do not ask \(R_D\) for it either.

**Majda.** CONC may still use B4c. Do not replace it
with an angular Poincaré the packets killed, and do
not sit the packet budget as all data.

**Beale.** Nobody votes a packet ratio into
\(\int\|\omega\|_\infty\).

**Feynman.** Missable: \(R_{\mathrm{ang}}\) versus 1,
and whether \(R_D\ll 1\) is a bound. Both readable.
The a-priori slogan missed.

**Einstein.** The object stayed the classical field.
A tube budget is not a closed estimate.

**Operator.** Tube is scored. B5f is scored. Geometry
is scored (B14d). The budget is scored (B15e). The
net is scored (B16e). The blob is scored (B17e). Field occupation is not an a priori (B18e). Field glue is not an a priori (B19e). NS climb is not an a priori (B20e). Next: climb sketch (B21e).
Finer stays B22e. Do not spawn \(n=64\). B4c stands.
Do not cancel to \(\Phi\).

---

## Score

| id | Verdict | What it is |
|---|---|---|
| B5f | **fail** | angular piece closes \(X\) |
| B24 | **pass** | B4c and B5b readable together |
| B24a | **fail** | angular \(1/r^2\) closes \(X\) |
| B24b | **fail** | B4c packet budget is an a priori |
| B24c | **fail** | \(R_D\ll 1\Rightarrow X\in L^\infty\) |
| B24d | **fail** | revive Hardy or cancel to \(\Phi\) |
| B24e | **fail** | packet geometry closes \(X\) |
| B24f | **fail** | this retunes the PDE |
| domain B | **open** | climb-sketch leftover is B21e |

Tesla’s line: you asked the tube. Full \(D\) budgets
the packet. Angular loses. Neither reading is a bound.
