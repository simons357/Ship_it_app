# Application: high-speed missiles (QUEUED)

**Program:** [`turbulence-reduction`](README.md)  
**Status:** QUEUED — no DA study yet  
**Not a weapon design. Not a tank certificate. Clay is NOT CLAIMED.**

This slot exists so the program can take a second study after ships.
It is empty on purpose.

## Why it is not the ship stack

Ship riblets are sized to seawater \(u_\tau\) and a fouling-release
dry-dock interval. High-speed external flow brings compressibility,
heat, and a different viscous length. Copying \(s^+=15\)–\(17\) in
micrometres from a hull film is not a translation.

## When DA opens this slot

Decompose the compressible near-wall layer. Name a recognized setpoint
(\(x\to x^\star\) or a concrete \(C_f\) band). Select only mechanisms
that already exist for that regime. Do not invent a phononic layer as
the Cf mechanism. Do not copy `ship_package`.

Envelope: **not awarded**.
