# Leftover close as an a priori

`python3 scripts/track_b_close.py`  
`python3 scripts/da_machine.py trackb`

Dated 4 September 2026. The PDE is not being tuned.
B14d through B23e are scored. This write asks whether
that catalog closes \(X\). It does not.

---

## The knob on this write

Tesla: a leftover close is a knob on the check. It
does not write \(X\). Cashing a list of missed
slogans as continuation is a knob.

No \(Q_1\). No \(\varepsilon\). No Biot–Savart slogan.
Do not write \(c=8\) into the PDE. Do not spawn
\(n=64\).

---

## What the apparatus does

**B35, pass.** Finer-box miss, finer-DNS miss, and
the leftover catalog are readable together. Same
caches as B33 / B34. No new FFT.

**B35a, fail** of “a leftover close writes \(X\).”
A catalog of fails is not continuation.

**B35b, fail** of “scoring B14d through B23e is
continuation.” Continuation is an estimate, not a
list of missed slogans.

**B35c, fail** of “a stack of leftover fails is
still an NS a priori.” Failing a knob is not sitting
the packet.

**B35d, fail** of “leftover closes are
\(\int\|\omega\|_\infty\).” A leftover close is not
the max criterion.

**B35e, fail** of “classical regularity is decided
by leftover closes.” Scored as B36. Leftover knobs
do not decide \(X\). Regularity stays open.

**B35f, fail** of “this retunes the PDE.” Leftover
closes are knobs on the check.

**B34e, fail** of “a leftover close writes
regularity.” Scored here.

---

## They work it

**Tesla.** You asked if scoring leftovers writes the
bound. It does not. Sit down. The object is still
\(X\).

**Leray.** I own dissipation on these packets. I
did not hand you all data.

**Ladyzhenskaya.** Same weight, both sides. I still
will not give you \(\varepsilon\).

**Majda.** A catalog is not a class.

**Beale.** Nobody votes a list of fails into
\(\int\|\omega\|_\infty\).

**Feynman.** Missable: whether B22e and B23e are
scored, and whether a leftover close is a bound.
The first held. The a-priori slogan missed.

**Einstein.** The object stayed the classical field.
A desk is not an estimate.

**Operator.** Leftover close is scored. B34e is
scored. Regularity stays open. Do not spawn
\(n=64\). B4c stands. Do not cancel to \(\Phi\).
Do not write \(c=8\) into the PDE.

---

## Score

| id | Verdict | What it is |
|---|---|---|
| B34e | **fail** | a leftover close writes regularity |
| B35 | **pass** | finer-box miss, finer-DNS miss, leftover catalog readable |
| B35a | **fail** | a leftover close writes \(X\) |
| B35b | **fail** | scoring leftovers is continuation |
| B35c | **fail** | a stack of fails is an NS a priori |
| B35d | **fail** | leftover closes are \(\int\|\omega\|_\infty\) |
| B35e | **fail** | classical regularity is decided by leftover closes |
| B35f | **fail** | this retunes the PDE |
| domain B | **open** | regularity stays open |

Tesla’s line: a leftover close is a knob on the
check. It does not write \(X\).
