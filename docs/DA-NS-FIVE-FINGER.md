# Unaugmented NS — five-finger map (speculated whole)

**Class:** axisymmetric-with-swirl is only Finger 5. Fingers 1–4 are on the full
unaugmented 3-D NSE on \(\mathbb{R}^3\) or \(\mathbb{T}^3\). Quantity and remainder
are per finger. This page is a map. It is not a theorem. It does not weld
the three writings. Soft X stays silent.

The Cosmo / SM five-finger stack (`docs/DA-FINGERS.md`,
\(R=\exp(-\tfrac12\chi^2_{\mathrm{ext}})\exp(-\tfrac12\chi^2_{\mathrm{int}})\),
16 Cosmo types) is a different book. That hand as constitutive NS is **NO**.
This page is the B-hand on leftover regularity. Same word, different object.

A map is not a close. Naming a blank is not an estimate
(`docs/TRACK-B-RESIDUAL.md` B40*). A finite number is not \(C_0\) and not a kill.

NS is not solved. Lemma★ is OPEN. Need★ is MISSING as a bound. H1 is not a
theorem. The axisymmetric shell door is OPEN.

---

## What this is

The operator asked for a five-finger routine on the speculated estimation of
what the whole thing looks like: what sits after the spindle, what the fire
state is, what the realized state is, what the future state is, and for DA
to fill the blanks.

DA fills blanks that already have names. DA does not invent a sixth writing
that glues the three. DA does not put \(K(t)\) in the PDE. DA does not turn
A into B.

---

## The three writings stay three

| Writing | What it is | Status |
|---|---|---|
| Geometric path — H1 | Bad-pair \(A_{\mathrm{bad}}\) on \(Q_r\). Aimed leftover yes. Theorem no. | Write sits. Leftover 1 OPEN. |
| Energy-budget path — Lemma★ / Need★ | Locked \(\mathcal R_\star=(T_c)_+^2/(\mathcal D_s E Y)\). Signed dual on HH→L after gap-cancel. | Need★ MISSING. Leftover 4 OPEN. |
| Axisymmetric shell estimate | \(Z_j=\|\Delta_j\omega\|_2^2\). Remainder \(T_{j\leftarrow j}\). Occupancy \(55/56\) printed. | Leftover 5 OPEN. |

Do not weld. Closing one finger is not ordinary NS.

---

## Five fingers (B-hand)

### Finger 1 — identities (the palm)

**Quantity:** the named identities, not a remainder.

What sits:

- Leray energy. Enstrophy identity. BKM criterion as a criterion, not as a proof.
- Locked \(\mathcal R_\star=(T_c)_+^2/(\mathcal D_s E Y)\). Kill rule: \(\mathcal R_\star(v_n)\to\infty\).
- Gap-cancel on two shells \(\alpha>\beta\):
  \[
  T_c=-(\alpha-\beta)\frac{\alpha\beta E}{X}\,T_\beta,
  \qquad
  \mathcal R_\star=\frac{\alpha\beta E\,T_\beta^2}{X\,e_\alpha e_\beta Y}.
  \]
  The gap \((\alpha-\beta)\) cancels. Near-shell recovers aligned 9B \(K\).

Status: **YES as identities.** Do not redo. An identity is not a bound.

### Finger 2 — the spindle (geometric write)

**Quantity:** \(A_{\mathrm{bad}}\) on the cylinder \(Q_r\). Remainder: the three
shapes, not yet absorbed.

The spindle is the geometric tube. The axis the geometric path spins on.
H1 write sits (`docs/H1-WRITE.md`, `docs/H1-SOT.md`). P1-lowpass, P1-loc, PC
sit. None of those is H1 as a theorem.

Status: **YES as a write. NO as a theorem. Leftover 1 OPEN.**
Do not start H1 from ABC\(_\lambda\). Do not quote Ring as proved.

### Finger 3 — after the spindle

**What after the spindle is not:** Gronwall. BKM. The energy-budget dual.
\([\rho]\). Those are other fingers, or they follow a close that is not here.

**What after the spindle is:** the named geometric leftovers on the same
cylinder.

| Blank | Name | Status |
|---|---|---|
| H2 | Energy on the same cylinder | OPEN. Not started from ABC. |
| H3 | Absorb the bad term | OPEN. |
| Ring Lemma | \(\|\nabla\xi\|_{L^\infty(E_c)}\le C\,2^{j^*}\) | **REPAIR.** Do not quote as proved. |
| Shape 1 | High-high → low on the cylinder | OPEN. |
| Shape 2 | High-low → high | OPEN. |
| Shape 3 | Low-low → high | OPEN. |

Those six names are after the spindle. They are not Gronwall, not BKM,
and not Need★.

Path 1 in `docs/PATH-AND-DA.md` uses a different (6): the leftover
\(\int\mathcal R\), all-data A1, all-data A2, or a killing field.
That (6) is not the spindle and not WRITE (6). Do not rename it.
Gronwall, BKM, and bootstrap follow a close of Path 1 (6). They are
not Finger 3.

Status: **OPEN / REPAIR.** Naming these is not an estimate.

### Finger 4 — energy-budget dual (Need★)

**Quantity:** the signed HH→L transfer
\[
\mathcal S_\star=T_\beta^{\mathrm{HH}\to\mathrm{L}}
=\sum_{\substack{p+q=k\\|p|^2=|q|^2=\alpha\\|k|^2=\beta}}
\mathrm{Im}\bigl[(q\cdot v_p)(v_q\cdot\overline{v_k})\bigr].
\]
**Remainder:** the unsigned CS that hides \(s\),
\(\lvert T_\beta\rvert\le\sqrt{\beta}\,E\sqrt{s\,e_\beta}\).

Need★ (MISSING as a bound):
\[
\lvert\mathcal S_\star\rvert\le C\frac{\alpha}{\sqrt{\beta}}\,e_\alpha\sqrt{e_\beta}.
\]
Locked ratio \(\mathcal N_\star=\lvert\mathcal S_\star\rvert/((\alpha/\sqrt{\beta})e_\alpha\sqrt{e_\beta})\).
A finite sample is not the bound. Printed \(\mathcal N_\star\approx 0.099\)
is a sample. Grow \(s\) is secondary kill-lane pressure, not this sentence.
Attack 12's table is not Need★.

Status: **Need★ MISSING. Leftover 4 OPEN.** Write sits in
`docs/NEED-STAR-HH-L-DUAL.md`.

### Finger 5 — restricted class (axisymmetric shell)

**Class:** axisymmetric with swirl, unaugmented, \(\mathbb{R}^3\).
**Quantity:** \(Z_j=\|\Delta_j\omega\|_2^2\).
**Remainder:** \(T_{j\leftarrow j}\).

Identity + far-shell Young sit. Occupancy \(55/56\) is printed. That is not
\([\rho]\). \(\int\rho_j\) is OPEN.

Status: **OPEN.** Do not import 2-D \(\rho\). Do not bound the bad term by
\(\Lambda'\) or \(\dot Z_j\).

---

## Three states

### Fire state (what is live now)

The work that can move. Not Cosmo. Not A. Not Soft X.

- Need★ is the primary math target. Signed dual on HH→L after gap-cancel.
  MISSING as a bound.
- Grow \(s\) on 9B is secondary. Finite max \(K\approx 0.456\), max \(s=192\).
  Not a kill. Designed \(\Theta(m^2)\) 9D stays **NO**.
- H1 write sits. Theorem no. Do not start from ABC\(_\lambda\).
- Axisymmetric shell: occupancy printed, \([\rho]\) not written.
- Catalog B: pass 58, fail 192, open 1 (`B_regularity`).

Fire is the leftover, not the catalog pass count.

### Realized state (unaugmented NS as it sits)

This is B. Unaugmented 3-D Navier–Stokes. No \(Q_1\). No \(\Phi\)-cancel.
No \(K(t)\) in the PDE. No Q-stack.

- Ordinary NS is **OPEN**. Catalog B open 1.
- A is not B. Theorem A sits for the \(Q_1\)-augmented PDE at \(\varepsilon>0\),
  \(\beta\ge 1/2\). Uniform \(H^1\) as \(\varepsilon\to 0\) still open.
- RH is OPEN. Q is inverse-GCD only. U is DA process.
- The three writings are aimed leftovers, not theorems.

Realized = what the equation is, and that it is not closed.

### Future state (what would have to sit)

DA cannot invent this. DA can only name what a close would look like.

One of these, not a weld of all three:

1. Need★ bound sits, or a sequence with \(\mathcal R_\star(v_n)\to\infty\)
   kills the locked form. Leftover 4 moves.
2. An H1 shape sits, or \(\mathcal G(v_n)\to\infty\) kills the geometric
   write. Leftover 1 moves. Ring would have to leave REPAIR first if the
   shape uses it.
3. A class bound on \(\int\rho_j\) sits, or a sequence kills the shell
   identity. Leftover 5 moves.

Then Path 1 steps (7)–(9) can be written. Until one of (1)–(3) sits,
(7)–(9) are not next.

Future is not A. Future is not Cosmo. Future is not Soft X.
Future is unaugmented NS with one leftover moved, or a kill of that leftover.

---

## What DA will not fill

- A sixth writing that glues H1 to ★ to \(T_{j\leftarrow j}\).
- Cosmo / SM five fingers as constitutive NS.
- Soft X. Theorem H. SFE / UHF / DHFA. Harmonic Blueprint as unifier.
- \(K(t)\) in the PDE. \(Q_1\) on B. \(\Phi\)-cancel on B.
- Designed \(\Theta(m^2)\) 9D. Attack 12 table as Need★.
- Ring as proved. Occupancy \(55/56\) as \([\rho]\).
- Finite \(K\approx 0.641\), \(0.506\), \(0.456\), \(\sqrt{K}\approx 0.711\),
  \(\mathcal N_\star\approx 0.099\) as \(C_0\) or a kill.
- “Almost proved.” A⇒B. SND≡Bridge.

---

## Verdict

**MAP.** Five fingers named. Three states named. After the spindle named.
Does not move a row off `docs/ISSUES-SHEET.md`. Does not change Catalog B
open 1. Does not close leftover 1, 4, or 5.

Cosmo five fingers as NS: **STOP / out of this book.**

Machine: `scripts/da_ns_five_finger.py`.
`accepted_as_close` is always false.

---

## Pointers

- Geometric write: `docs/H1-WRITE.md`, `docs/H1-SOT.md`.
- After the spindle blanks: `docs/PATH-AND-DA.md`, `docs/NS-PROOF-CHAIN.md`.
- Need★: `docs/NEED-STAR-HH-L-DUAL.md`.
- ★ reason: `docs/LEMMA-STAR-REASON.md`.
- Grow \(s\): `docs/ATTACK-9D-GROW-S.md`.
- Axisymmetric shell: `docs/AXISYM-SHELL-DOOR.md`.
- Cosmo hand (other book): `docs/DA-FINGERS.md`.
- Tape: `docs/YES-NO-OPEN.md`.
- Leftovers: `docs/ISSUES-SHEET.md`.
