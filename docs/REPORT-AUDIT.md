# Report audit — GPT scoreboard and the other incoming notes

16 September 2026.
**Adjudication against the tape.
Not a close. NS not solved.**

Sources scored here:

1. GPT scoreboard / bottom line
   (phone screenshots, 16 Sep).
2. Grok / operator 16 Sep handoff
   (already seated as
   [`NS-STATUS.md`](NS-STATUS.md)).
3. “Rocks” classical-NS briefing
   (queued).
4. Theorem H audit summary (queued).
5. Pólya rebuild of a \(\rho\)-floor
   (queued).
6. Later GPT-style ceiling note
   (queued: “peak-fraction floor
   done”; “C10 died”; every lane
   is \(\|\nabla u\|_\infty\) from
   \(X\le M\)).

Tape: [`YES-NO-OPEN.md`](YES-NO-OPEN.md).
Handoff: [`NS-STATUS.md`](NS-STATUS.md).
Master plan: [`MASTER-PLAN.md`](MASTER-PLAN.md).

Four buckets only:
PROVED / CLAIMED / OPEN / DEAD.
“LIVE” is not a bucket. It is a
research order or a slogan.

---

## 1. GPT scoreboard, row by row

| GPT row | GPT word | Tape | Verdict |
|---|---|---|---|
| Old Theorem H | DEAD in displayed form | Withdrawn even under \(X\le M\). Shear ratio \(\to\infty\). | **CORRECT.** |
| Universal SND floor | Unsupported / old form killed | Equal-shell \(v_L\), \(\rho(0)=1/L\). | **CORRECT.** |
| SND diagnostic | LIVE | Instrument sits. No persistence. | **CORRECT as instrument.** Not a theorem. |
| SND + tail conditional | LIVE, secondary | Unwritten. Bernstein + assumed \(\gamma\) is BKM-adjacent. | **OVERCLAIM.** Useful later. Not seated. Not primary. |
| Exact-shell 9D \(K\le 16/9\) | CLAIMED | Designed \(\Theta(m^2)\) 9D is **NO.** Exact-shell bound is **CLAIMED.** | **WRONG NAME.** Do not write “9D is claimed.” |
| Three-shear \(K_{1,2}=2/3\) | Example / check | Floor of \(\sup K\). Not \(16/9\). | **CORRECT.** |
| Unrestricted Lemma★ | OPEN / separate | **KILLED** by \(v_n\). Replacement leftover 4 is OPEN. | **WRONG.** Most dangerous error on that board. |
| Near-scale obstruction | LIVE | \(b=0\) cancels; live piece is \(b\ge 1\) / enstrophy-side. | **CORRECT as localization.** |
| C10 depletion | LIVE — highest priority | Named leftover-5 candidate. (A) **not seated.** | **HALF.** Priority yes. Seated route no. |
| Centered spectral drift | LIVE — highest-value independent | Identities **PROVED.** Estimate **OPEN.** ★ stays dead. | **HALF.** Value as a target yes. Not a sitting estimate. |
| \(T_c\le\theta\nu\mathcal D_s+KX\) | OPEN | OPEN, and \(K\) must be useful, not tautological. | **CORRECT.** |
| Classical NS closure | OPEN | Catalog B open stays 1. | **CORRECT.** |

### GPT bottom line

“False bridges are gone; the leftover
is less vague.” **CORRECT.**

“Stop asking whether SND proves
regularity.” **CORRECT as strategy.**

“Does near-scale geometry force
depletion? Can centered drift be
absorbed?” **CORRECT as the two
questions.** Explicit failure
conditions: **CORRECT demand.**

“C10 first; centered independently;
Theorem H stays; independent
reproduction of \(16/9\).”
**CORRECT order.** That order is
not a proof.

---

## 2. Grok / 16 Sep handoff

Already the living handoff.
Mostly **CORRECT.** Two naming
fixes already on the tape:

- Do not title the claimed object
  “Exact-shell 9D.” The claimed
  object is the exact-shell bound.
- “Do not spend another cycle
  fighting H1/H” mixes two
  leftovers. Theorem H is
  withdrawn. Leftover 1 = H1 =
  WRITE (6) is a different
  integral and stays OPEN. Do
  not start it from this audit.

C10 as first priority is a
**research order**, not a seated
theorem. That distinction is now
on [`C10-CHAIN.md`](C10-CHAIN.md).

---

## 3. “Rocks” briefing

Correct facts:

- Official Statement (B) is
  unforced smooth data.
- 2-D is settled; 3-D unforced
  is open.
- OpenAI’s 8 Sep announcement
  is a **forced** construction,
  Fefferman (C)/(D) if it holds.
- Clay’s 11 Sep note is an
  **evaluation**, not an award.
- Vortex stretching and
  supercriticality name the
  difficulty. They are not a
  leftover close.

Wrong or off this desk:

- “That changed in September
  2026” as if unforced NS moved.
  It did not, on this desk.
- “Path forward = extend
  Córdoba–Martínez-Zoroa /
  Buckmaster–Alpöge to viscous
  unforced, or AI-search a
  self-similar blowup.”
  That is a **singularity
  program**. It is Clay (B)
  by counterexample if it
  ever sits. It is **not**
  Track B on this branch
  (a priori control from
  admissible data). Do not
  switch the living line to
  a blowup hunt.
- Euler blowup and Leray–Hopf
  non-uniqueness do not prove
  a smooth unforced
  singularity.

File already on the tape:
[`OPENAI-NS-CLAIM.md`](OPENAI-NS-CLAIM.md).

---

## 4. Theorem H audit summary

**CORRECT.** Already seated.
[`SND-H-REVIEW.md`](SND-H-REVIEW.md),
[`SND-H-REPAIR.md`](SND-H-REPAIR.md).

Do not draft a repaired Theorem H
from §8. Do not mail a panel.
\(A.2\) sits. Propagation unwritten.

---

## 5. Pólya \(\rho\)-floor rebuild

The proposed problem:

> Assume \(X\le M\) and
> \(\rho(0)\ge\rho_*\). Does \(A.3\)
> force a later floor, or can
> \(\rho\) collapse?

That is a **weaker** question than
regularity. Even a floor on \(\rho\)
leaves \(1-\rho\) free to sit at
arbitrarily high frequency. The
audit already named that hole.

\(A.3\) is a Dini **ceiling**, not
a floor. Indexed Bony plus a
comparison ODE is Theorem H
resurrection under another name.

**REJECT as primary.**
Do not write that next document
on this desk. If someone later
writes a conditional \(\rho\)
statement, it is a write, not
leftover 1, 4, or 5, and not
ordinary NS.

---

## 6. Later GPT-style ceiling note

| Claim | Tape |
|---|---|
| Exact shell budget, commutator, conditional \(F_j\) sit | **YES.** |
| “Corrected peak-fraction floor” sits unconditionally | **NO.** \(A.3\) is a ceiling. No floor. |
| 9D stays CLAIMED pending two reproductions | **YES** as the exact-shell bound. Designed 9D is **NO.** |
| Theorem H, \(M\)-free bound, \(t=0\) floor are dead with named kills | **YES.** |
| “C10 died” | **NO as a program.** (A) not seated. Depletion unwritten. Lane stays a named candidate. |
| Every live lane reduces to \(\|\nabla u\|_\infty\) from \(X\le M\) | **Circularity risk, not a theorem.** If a lane secretly uses that, it is BKM and dead as an a priori. C10 and centered drift are required to **avoid** that. |
| Honest ceiling = conditional theorem plus a catalog of kills | **YES as publication posture.** Not a close. |

---

## 7. What every report got right together

False bridges are gone:

- displayed Theorem H,
- universal \(\rho\)-floor,
- unrestricted \(\star\),
- energy-linear local \(R\),
- designed 9D \(\Theta(m^2)\),
- \(\Phi\)-cancel / SFE / HB /
  \(K(t)\) in the PDE.

The remaining mathematical
question is localized:

> Can near-scale nonlinear
> transfer beat viscosity
> and spectral spread?

Two independent target shapes
are the only ones on this desk
that could still carry unforced
regularity **inside the machinery
already built**:

- C10: dynamics \(\Rightarrow\)
  depletion of \(a_+\) \(\Rightarrow\)
  (A).
- Centered drift:
  \(T_c\le\theta\nu\mathcal D_s+K(t)X\)
  with a **useful** \(K\in L^1_{\mathrm{loc}}\).

They must not be forced to meet.

---

## Lock

GPT’s board is mostly inventory.
Its one lethal error: unrestricted
★ is **KILLED**, not OPEN.
C10 is not a sitting route.
A.3 is not a floor.
Blowup-search is not this line.
Do not repair H.
NS not solved.
