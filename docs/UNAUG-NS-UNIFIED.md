# Classical unaugmented NS — unified status and roadmap

16 September 2026.
**One page after the peer reports.**
Ordinary Navier–Stokes is not solved.
Clay Statement B is **OPEN**.
Catalog B open stays 1 (`B_regularity`).
A is not B. Soft X silent.

**Enough information?**
Yes to score the desk and order the
next writes. No to close the equation.
The reports name leftovers. They do
not seat an a priori bound on
\(X=\|\omega\|_2^2\).

Do not glue leftover 1, 4, and 5.
Do not start H1 from ABC_λ.
Do not weld \(\star\).
Do not repair displayed Theorem H.
Do not run more exact-shell \(K\)
sweeps. Do not write “9D is claimed.”

Tape: [`YES-NO-OPEN.md`](YES-NO-OPEN.md).
Four-bucket score:
[`NS-STATUS.md`](NS-STATUS.md).
How the desk already planned to move:
[`MASTER-PLAN.md`](MASTER-PLAN.md).
Incoming boards vs tape:
[`REPORT-AUDIT.md`](REPORT-AUDIT.md).
ChatGPT paste vs Grok desk vs
the C10 word:
[`INCOMING-WRITEUPS.md`](INCOMING-WRITEUPS.md).

---

## What was ingested

Peer writes used here, not RH and
not the magazine:

| Source | What it is | What this page takes |
|---|---|---|
| This desk, PR 100 | SND-to-regularity implication | Shell vs drift. Circular inputs named. |
| SND spectral, PR 101 | KEEP card | Displayed Theorem H **withdrawn**. Boxed \(\dot X_j\) in \(F_j\). Tests A–C. |
| Same-scale candidates, PR 102 | Survivor ranking | \(T_{j\leftarrow j}\) OPEN. “SURVIVES” means circular drafts were refused, **not** an inequality. |
| C10 chain, PR 103 | Depletion \(\Rightarrow\) (A) | Failed arrow recorded. Do not repair with \(\|\nabla u\|_\infty\). |
| H1 / centered summarize | Parallel tracks | H1 OPEN. Centered package is a separate book. |
| Unaugmented summarize | Two books | Φ-renorm algebra ≠ Lemma★ / SND. |
| NS-search summarize | Conditional toolkit | Ring + T2 Lemma 1 sit. Same-shell energy-linear \(R\) **dead**. |
| \(T_j\) estimate | Axisymmetric remainder | Identities sit. Requested Young line not seated. |

Older sentences that **do not win**:

- “Lemma★ is still OPEN” as the
  unrestricted box — that box is
  **KILLED** by \(v_n\).
- “SND-C is open at low Bony \(T\)”
  as if displayed H were waiting for
  a patch — displayed H is **false**
  even under \(X\le M\).
- “Attack 9D next” — freeze. Designed
  \(\Theta(m^2)\) 9D is **NO**.
- PR 102 “C10 SURVIVES” as a close —
  superseded by PR 103.

If a later sentence contradicts the
tape, the tape wins.

---

## Headline

Classical unaugmented 3-D NS on
\(\mathbb{T}^3\) or \(\mathbb{R}^3\),
no extra stress, keep \(1/r^4\),
no \(\Phi\)-cancel as the estimate:

**OPEN.** Not a hidden yes.

Two books stay two books:

1. **Swirl / Φ-renorm** — algebra
   \(\frac1{r^4}\partial_z(\Gamma^2)=\partial_z(\Phi^2)\)
   **KEEP**. Barrier
   \(\|u^r/r\|_\infty\) uniform in
   \(\varepsilon\) **OPEN**. Not Clay.
2. **Torus energy-budget / shells** —
   unrestricted \(\star\) **KILLED**.
   Displayed Theorem H **withdrawn**.
   Exact-shell \(K\le 16/9\) **CLAIMED**,
   not a regularity close.

The live question is not “does SND
persist?” It is whether the nonlinear
term can drive high-frequency
concentration faster than viscosity
can control it, **without assuming
the bound you want**.

---

## Four buckets

### PROVED (do not redo)

- Spectral identities
  \(X,Y,Z,\Lambda,T_c,\mathcal D_s\).
  \(\mathcal D_s\ge 0\).
  \((\log\Lambda)'=2(T_c-\nu\mathcal D_s)/Y\).
- T2 Lemma 1: div-free kills low
  self-flux. Not regularity.
- TJJ-Trans, TJJ-α, stretch
  \(\le a_+ Z_j\). Exact.
- Far-shell Young on the axisymmetric
  face. Does not touch \(T_{j\leftarrow j}\).
- Fallback \(F_j\) lemma under
  \(X\le M\) (Hölder \(6,2,3\)).
  Not a propagation theorem.
- Unrestricted
  \(\sup\mathcal R_\star<\infty\) is
  **false** (\(v_n\),
  \(\mathcal R_\star\gtrsim n/165888\)).
  Instantaneous class, not a blowup.
- Displayed Theorem H / SND-C as
  written **fails** even under
  \(X\le M\) (high-tail shears).
- Energy-linear remainder for (A)
  **dies** on \(u^\lambda\)
  (\(T\sim\lambda^{9/2}\) vs
  \(\sim\lambda^4\)).
- Designed \(\Theta(m^2)\) 9D **dead**.
- Theorem A for extra-stress NS
  (\(\varepsilon>0\)). Not ordinary NS.

### CLAIMED (freeze)

Exact-shell \(C=4/3\), \(K\le 16/9\).
Independent reproduction of the
projected identity and weighted
incidence. Sweeps are consistency
only. Three-shear \(K=2/3\) is a
**floor**, not the bound.
**Not** a continuation criterion.
**Not** a repair of \(\star\).

### OPEN (named leftovers)

Ordinary NS is **one** of these,
not all three glued:

| # | Leftover | What would move it |
|---|---|---|
| 1 | H1 = WRITE (6) | Shape 1, 2, or 3, or \(\mathcal G\to\infty\). Different integral. Do not start from this page. |
| 4 | Replacement energy-budget | A different estimate that \(v_n\) does not kill. Pathwise useful \(K\) for \(T_c\) is a candidate, not a theorem. |
| 5 | Axisymmetric \(T_{j\leftarrow j}\) / \(\int\rho_j\) | Class bound, or a field with \(\int\rho_j=\infty\). C10 is the named candidate. (A) is **not seated**. |

Also OPEN, not a thirteenth row:

- Frequency-drift law for \(j_*\) or
  \(\Lambda\) from the field
  ([`SND-TO-REGULARITY.md`](SND-TO-REGULARITY.md)).
- Uniform \(\|u^r/r\|_\infty\) on the
  swirl book.

### DEAD (do not rebuild)

- Ordinary NS solved.
- Unrestricted \(\star\) as a bound.
- Displayed Theorem H as a bridge.
- Theorem G; universal \(t=0\) SND
  floor.
- C10 as a seated theorem.
- Merge C10 with \(T_c\) or with
  \(\star\) without an inequality.
- Energy-linear \(R\) for (A).
- T2 Lemma 2 (\(H^{2.3}\) ball) as
  large-data input.
- \(\Phi\)-glue into H.
- Occupation clock as a bound on \(X\).
- Detector occupation decay.
- Triple Lock. Inverse-GCD as NS.
- SFE / \(Q_1\) / HB as this PDE.

---

## Three lanes, not a series

They sit **beside** each other.
None currently proves that the
dynamics cooperate.

| Lane | Target | Status |
|---|---|---|
| **C10 / leftover 5** | \((T_{j\leftarrow j})_+\le\theta\nu D_j+R_{\mathrm{allowed}}\) | Identities sit. First new arrow (template \(\to a_+\) estimate) is **empty**. Controlling \(a_+\) in \(L^\infty\) already controls a piece of strain. Not weaker than BKM. |
| **Centered drift / leftover 4 packaging** | \(T_c\le\theta\nu\mathcal D_s+K(t)X\) with **useful** \(K\in L^1_{\mathrm{loc}}\) | Identities sit. Estimate OPEN. Unrestricted \(\star\) stays killed. Tautological \(K=(T_c-\theta\nu\mathcal D_s)_+/X\) is not content. |
| **SND** | Instrument: \(\rho\), \(j_*\), tail, \(F_j\) | Diagnostic. No persistence. Boxed \(\dot X_j\) identity **KEEP**. Displayed H **withdrawn**. |

Exact-shell 9D is a **fourth object**,
frozen CLAIMED, not a lane into
regularity.

Shell condition vs drift, written
out: a shell bound controls
instantaneous mass, not how the
peak moves. Even true Ring plus a
true flux bound still need a drift
law. That implication assumed the
desired bound in named places
(T2 Lemma 2, Theorem F, Theorem E,
G’s uniform \(C_*\)).

---

## Roadmap — where to go from here

### Freeze (do not spend primary effort)

- Repairing displayed Theorem H.
- Universal \(\rho\)-persistence.
- More exact-shell \(K\) sweeps.
- BKM by assuming a geometric tail.
- Restoring unrestricted \(\star\).
- Starting H1 from ABC_λ.
- Welding C10 to \(T_c\) with no
  displayed inequality.
- Treating PR 102 “SURVIVES” as a
  theorem.
- Magazine, RH, SFE, \(Q_1\).

### Next writes, in order

**1. Route A — kill or write, one page.**
Dynamical bound on \(a_+\) that does
**not** use \(H^1\), \(L^\infty\), or
BKM. The later C10 write already
says that line is empty and **is
(A)** for the main stretch.

If that page cannot be written
without the forbidden control,
**name the death** and stop
patching. Leftover 5 stays OPEN.
Do not insert \(\|\nabla u\|_\infty\le\Gamma\)
and call it depletion.

**2. Route B — primary living estimate
after that gate.**
Keep \(T_c-\theta\nu\mathcal D_s\) as
one pairing. Hunt a **useful** \(K\),
not a tautology. Test on \(v_n\),
amplitude, shears, and single shells.
Do not restore
\(\sup\mathcal R_\star<\infty\).

If Route B sits with useful \(K\),
then \(\Lambda'\le 2K\) and
\(X\le\|u_0\|_2^2\Lambda\) stays
finite **in this packaging**.
One direction. No converse.

**3. SND stays an instrument.**
Print \(\rho\), \(j_*\), tail,
\(F_j\), \(\Delta j_*\). Any later
conditional criterion must survive
Tests A–C on the KEEP card and must
not smuggle \(M=X_\infty\). That is
not the close.

**4. Exact-shell bound stays CLAIMED.**
Independent reproduction only.
Not this desk’s next write.

**5. Leftover 1 (H1) stays OPEN.**
Different integral, different class.
Do not start it from this roadmap.
If both Route A and Route B die,
the geometric cylinder path is still
there — as itself, not as glue.

**6. Crossover** only if (A) sits.
Then display an actual inequality
from \(a_+\) to \(T_c\). None sits.
Do not draw a conceptual arrow.

### What winning would look like

A seated inequality, every arrow
marked, that

- does not assume \(H^1\), \(L^\infty\),
  BKM, or the conclusion,
- survives \(v_n\), shears, amplitude,
  equal-shell \(v_L\), mixed swirl,
- and implies an a priori bound on
  \(X\) (Route B) or on the local
  block **plus a written passage to
  \(X\)** (Route A plus a later global
  step that does not exist today).

Until that page exists, talking is
not the close.

### If both routes die

Leftover 4 needs a **different**
estimate that \(v_n\) does not kill.
Need★ cannot repair the dead box.
Leftover 5 stays OPEN until a class
bound or a diverging \(\int\rho_j\).
Leftover 1 stays the cylinder write.
That map is honest. It is not a
third fake bridge.

---

## Lock

Enough for this score and this
order. Not enough for a close.
Displayed H withdrawn.
Unrestricted \(\star\) killed.
C10 not seated.
Centered useful \(K\) OPEN.
SND is an instrument.
Exact-shell \(K\le 16/9\) CLAIMED,
frozen, not regularity.
Frequency drift still missing.
Do not glue 1, 4, and 5.
NS not solved.
