# Signed Assembly Gate

24 September 2026.
**The first missing implication, named
as a gate. Not a theorem. Not Gate 5.
Not a close. NS not solved.**

The screenshot canon is already
reconstructed:
[`FOURIER-TRIANGLE.md`](FOURIER-TRIANGLE.md).
This page does not redo those
identities. It **inserts one object**
between the seated triangle geometry
and any \(T_c\) / \(\Lambda'\) bound.

\[
\boxed{\textbf{SIGNED ASSEMBLY GATE}}
\]

sits **before** old Gate 5
(the \(T_c\) bound / time evolution /
Gronwall). Do not jump from a
triangle to Gronwall.

Unaugmented NS on \(\mathbb{T}^3\).
No \(Q_1\). No \(\Phi\)-cancel. No
SND persistence. No Theorem H. No
Route A weld. Unrestricted
\(\sup\mathcal R_\star<\infty\) stays
**KILLED** by \(v_n\). Claimed
\(K\le 16/9\) stays CLAIMED, one
shell, not global closure. Prime
masks stay optional selectors.

Need★ is this gate on **two shells**.
The one-shell \(16/9\) program is
this gate on **\(Aw=\alpha w\)**.
Attack 8’s three-shell family is
the first counting probe of the
**unequal-length** side. None of
those is this page as a theorem.

---

## What the screenshots seated

Do not reopen. Do not weaken.

\[
\boxed{\text{The Fourier triangle is exact at the level of one triad.}}
\]

\[
\boxed{
\text{per-triangle geometry}
\not\Rightarrow
\text{a signed bound on the sum}.
}
\]

That second arrow **is** the first
missing implication. It is this gate.

Seated chain of identities (EXACT):

\[
\lambda_k=\lvert k\rvert^2,
\qquad
p+q=k,
\qquad
p\cdot v_p=0,
\]

\[
\widehat B_k
=i\,P_k\sum_{p+q=k}(k\cdot v_p)\,v_q.
\]

Leray does not enlarge the vector.

Equal input lengths
\(\lvert p\rvert^2=\lvert q\rvert^2=\alpha\),
\(\lvert k\rvert^2=\beta\):

\[
k\cdot v_p=k_\perp\cdot v_p,
\qquad
\lvert k_\perp\rvert^2
=\beta\Bigl(1-\frac{\beta}{4\alpha}\Bigr).
\]

Two structurally different
cancellations, both EXACT:

\[
\boxed{\text{transverse / flat-triangle: }\beta=4\alpha\Rightarrow k_\perp=0}
\]

\[
\boxed{\text{two-shell spectral-gap: }(\alpha-\beta)\text{ drops out of }\mathcal R_\star}
\]

Multiple shells, or \(\lvert p\rvert\neq\lvert q\rvert\),
kill **both**. That is the
unequal-length defect.

Signed transfer (keep \(\mathrm{Im}\)):

\[
T_k
=\sum_{p+q=k}
\mathrm{Im}\bigl[(q\cdot v_p)(v_q\cdot\overline{v_k})\bigr],
\qquad
\sum_k T_k=0,
\]

\[
T_c
=\sum_k\lambda_k(\lambda_k-\Lambda)T_k.
\]

Phases enter **only** through that
product. Replacing it by
\(\lvert\widehat B_k\rvert\) throws
away the cancellation the sum might
still have.

Clock (EXACT, not a bound):

\[
\Lambda'
=\frac{2(T_c-\nu\mathcal D_s)}{X}.
\]

\[
\mathrm{NSE}
\to
\text{Fourier triads}
\to
T_k
\to
T_c
\to
\Lambda'.
\]

The missing mathematics is not the
clock. It is control of the **positive
signed** nonlinear term **after
assembly**.

\(I_3\) is the cubic lattice
\((\mathbb{Z}^3,x_1^2+x_2^2+x_3^2)\),
not \(\sum_k T_k=0\). The older
energy-conservation reading is
withdrawn
([`FOURIER-TRIANGLE.md`](FOURIER-TRIANGLE.md),
(12)). Hilbert symbols decide
which integer triangles exist.
They do not produce amplitudes,
phases, \(k_\perp\), gap-cancel,
\(\mathrm{Im}\) control, or
integrable \(K(t)\). Energy
conservation of \(B\) remains
\(\sum_k T_k=0\), under that
name. The remaining sufficient
theorem on the Fourier-triangle
desk is (17), not this gate.

\(16/9\neq\) global closure, even if
the one-shell proof later checks.

---

## Where the gate sits

Old Gate 5 was the jump to a
\(T_c\) bound and then to time.
That jump skipped the missing
implication. The reconstruction is
now:

```
exact Fourier triad                         EXACT
        |
        v
DF + Leray geometry                         EXACT
        |
        v
equal-length / gap cancellations            EXACT
        |
        v
unequal-length defect                       EXACT as a hole
        |
        v
   ┌─────────────────────────┐
   │  SIGNED ASSEMBLY GATE   │   FIRST MISSING
   │  keep k_⊥, gap, Im      │
   │  do not use |B̂_k| early │
   └─────────────────────────┘
        |
        |  only if the gate writes
        v
T_c bound (snapshot)                        OPEN
        |
        v
Λ' = 2(T_c − ν D_s)/X                     EXACT
        |
        v
time-dependent K(t) ∈ L¹                    LATER (Loss B)
```

Loss A is this gate.
Loss B is evolution: a frozen
one-shell estimate does not control
\(K(t)\). Do not build Loss B
before Loss A has a write or a
named death.

---

## Five Fingers / CRSPR on this gate

This is the **B-hand** role map on
the seated triad objects. It is
**not** Cosmo / SM five fingers
(`DA-FINGERS.md`). That hand as
constitutive NS is **NO**.

Domain Architect / CRSPR may
**classify** these roles. It may
not derive \(T_c\) from SFE, and
it may not glue leftover 1, 4, 5.

| Role | Occupant on this gate | Seated object |
|---|---|---|
| **Permission** \(P\) | which triads are actually present | support of \((p,q,k)\) with \(p+q=k\) and \(v_p,v_q,v_k\neq 0\) |
| **Coupling** \(H\) | Leray / divergence-free interaction coefficient | \(k\cdot v_p=k_\perp\cdot v_p\) after \(P_k\); one-mode factor \(\lvert k_\perp\rvert\) |
| **Phase** \(\psi\) | imaginary signed product | \(\mathrm{Im}[(q\cdot v_p)(v_q\cdot\overline{v_k})]\) |
| **Scale** \(\lambda\) | centered spectral weight | \(\lambda_k(\lambda_k-\Lambda)\) |
| **Realized output** \(\Phi\) | signed assembled transfer | \(T_c=\sum_k\lambda_k(\lambda_k-\Lambda)T_k\) |

This decomposition is classification
of identities that already sit
(evidence Level 0). It is not a
proof that the roles close. The
blank is the **sum**: Permission
selects a set of triangles;
Coupling and Phase live on each
triangle; Scale weights the output;
the gate must assemble those
without replacing Phase by an
absolute envelope.

Finger 1 of `DA-NS-FIVE-FINGER.md`
is the palm of identities. This
gate is the missing estimate on
**Finger 4** (Need★ / energy-budget
dual), named so Gate 3/4
reconstructions must recover the
two cancellations rather than
contradict them.

---

## What the gate must produce

A write that **sums the exact signed
triangle identity** while retaining
enough of

1. transverse / flat-triangle
   geometry (\(k_\perp\),
   \(\beta=4\alpha\Rightarrow 0\)),
2. two-shell gap-cancel
   (\((\alpha-\beta)\) out of
   \(\mathcal R_\star\)),
3. phase cancellation
   (\(\mathrm{Im}\), not
   \(\lvert\widehat B_k\rvert\)),

on a **named class**:

| Class | Already-named leftover | Status |
|---|---|---|
| Two shells, HH→L | Need★: \(\lvert\mathcal S_\star\rvert\le C(\alpha/\sqrt{\beta})\,e_\alpha\sqrt{e_\beta}\) | **MISSING** |
| One input shell \(Aw=\alpha w\) | Claimed \(K\le 16/9\) | **CLAIMED**, frozen, not seated |
| Three-shell closing triad | Attack 8 family \(k_0\), \(k_0+e\), \(2k_0+e\) | **NUMERICAL probe**, not a bound |
| General multi-shell | Replacement leftover 4 after \(v_n\) | **OPEN**; unrestricted box **DEAD** |

Name the class on the write.
A bound proved on two shells is
not a bound on \(v_n\). A claimed
one-shell \(16/9\) is not a
three-shell bound.

---

## Death conditions (do not patch)

The gate is **killed as a close**
if the write does any of:

- replaces
  \(\mathrm{Im}[(q\cdot v_p)(v_q\cdot\overline{v_k})]\)
  by \(\lvert\widehat B_k\rvert\)
  or another absolute envelope
  **before** the sum
  (throws away Phase);
- uses cheap CS
  \(\lvert\widehat B_k\rvert\le\lvert k\rvert E\)
  and returns occupancy \(s\)
  as the remainder
  (Loss A, already mapped);
- assumes \(\|\nabla u\|_\infty\),
  \(H^1\) continuation, BKM, or
  Prodi–Serrin
  (circular);
- treats \(K\le 16/9\) as global
  closure or as a bound on \(v_n\);
- feeds \(P=\mathbf 1_{\mathbb P}\)
  or Q6 / Bridge* as the assembly
  mechanism;
- restores unrestricted
  \(\sup\mathcal R_\star<\infty\);
- jumps to \(\int K(t)\,dt<\infty\)
  without a snapshot inequality
  (Loss B before Loss A);
- welds this gate to leftover 1
  (H1) or leftover 5
  (\(T_{j\leftarrow j}\)).

If every attempted sum reintroduces
\(s\) or \(\|\nabla u\|_\infty\),
**name that death** and stop
patching. The leftover is then a
new estimate shape, not a hybrid
of wreckage.

---

## What this does not reopen

| Object | Status that stays |
|---|---|
| Unrestricted \(\star\) | **KILLED** by \(v_n\) |
| Displayed Theorem H | **Withdrawn** |
| Route A / \(\int a_+\) | **Parked** (UNRESOLVED) |
| Designed \(\Theta(m^2)\) 9D | **DEAD** |
| Exact-shell \(K\le 16/9\) | **CLAIMED**, one shell, freeze sweeps |
| \(K=0\) absorption | **DEAD** |
| Triple Lock / SFE / HB as this PDE | **RETIRED** |
| Prime mask as a dynamical estimate | **NO** |
| Cosmo five fingers as constitutive NS | **NO** |

Gate 3/4 reconstructions should
**recover** transverse cancellation
and gap-cancel. If a Gate-3 formula
contradicts
\(\lvert k_\perp\rvert^2=\beta(1-\beta/(4\alpha))\),
the Gate-3 formula is the one that
moves.

---

## Next target

That blank is now split as SAG-5:
[`SAG-5-COMPATIBILITY.md`](SAG-5-COMPATIBILITY.md).

\[
\boxed{\text{SAG-5 = GLOBAL COMPATIBILITY DEFECT}}
\]

\[
\boxed{\text{5A: shared-input rank defect}}
\qquad
\boxed{\text{5B: partner/output cocycle compatibility}}
\]

Do not stamp SAG-5 from
\(\mathrm{rank}_{\mathbb{C}}L_p\le 2\)
alone. One shared input does not
obstruct two triangles; it can
obstruct three or more. The
obstruction that can already
appear at two triangles is
output / partner identification.

\[
\boxed{
\begin{aligned}
&\text{Can we sum the exact signed triangle identity}\\
&\text{while retaining enough of }k_\perp\text{, gap-cancel, and }\mathrm{Im},\\
&\text{without replacing it prematurely by }\lvert\widehat B_k\rvert\text{?}
\end{aligned}
}
\]

First experiment: \(\rho_2\) on the
smallest coefficient-sharing pair.
No \(\lvert\sum S\rvert\to\sum\lvert S\rvert\).
No occupancy bound.

That is more precise than “close
Navier–Stokes.” It is the identifiable
bottleneck connecting

- the old Fourier-triangle canon,
- Need★,
- Attack 8 / occupancy counting
  (Attacks 6–11: local geometry can
  look excellent; global assembly
  returns a counting loss),
- the centered clock \(\Lambda'\).

A successful write is a signed
assembly estimate on a named class,
every arrow marked EXACT / STANDARD /
NEW / NUMERICAL, tested on
\(v_n\), shears, amplitude,
equal-shell \(v_L\), and mixed swirl.

SAG-5 is that page’s name, not
its theorem. \(\rho_2=1/\sqrt{2}\)
is stamped finite constant
depletion. SAG-6
([`SAG-6-LATTICE-CIRCLE.md`](SAG-6-LATTICE-CIRCLE.md))
is the bisector stack. 6D:
\(\rho_k(N)=1\) on a legal
coherent family. That route is
not the missing power.

Static shared-output rescue of
charge-only coercivity is
**KILLED** by the scalene
two-triad witness
([`JOINT-EPOCH-BUDGET.md`](JOINT-EPOCH-BUDGET.md)).
The next Gate is the Joint
Gap–Charge Epoch Budget, not
another SAG-6. Until that
budget or a named death exists,
talking is not the gate.
Seated ledger:
[`CENTERED-MASTER-LEDGER.md`](CENTERED-MASTER-LEDGER.md).
Residual / primitive test:
[`NARROW-HET-RESIDUAL.md`](NARROW-HET-RESIDUAL.md).

---

## Lock

Triangle EXACT at one triad.
Two cancellations EXACT.
Unequal-length defect EXACT as a hole.
\(\sum T_k=0\), \(T_c\), \(\Lambda'\) EXACT.
Phases live in \(\mathrm{Im}\).
Per-triangle geometry \(\not\Rightarrow\) signed bound on the sum.

\[
\boxed{\textbf{SIGNED ASSEMBLY GATE}}
\]

before old Gate 5.
Need★ = this gate on two shells.
\(16/9\) = this gate on one shell, CLAIMED.
Attack 8 = counting probe, not a bound.
SAG-5 = global compatibility defect
(5A rank, 5B cocycle). \(\rho_2=1/\sqrt{2}\)
stamped, constant, not \(N^{-\delta}\).
SAG-6A EXACT. 6B sparse. 6D
\(\rho_k(N)=1\), coherent family.
One-\(k\) equal-input stack is not
the missing power.
Static shared-output rescue of
charge-only coercivity **KILLED**.
Next Gate = Joint Gap–Charge
Epoch Budget.
Master ledger:
[`CENTERED-MASTER-LEDGER.md`](CENTERED-MASTER-LEDGER.md).
CRSPR roles classify; they do not close.
Do not use \(\lvert\widehat B_k\rvert\) early.
Do not evolve a bound that was never written.
Do not reopen a dead route by declaration.
Fourier-triangle (17) is the
remaining sufficient scalene
time estimate; this gate is not
a substitute for it.
NS not solved.
