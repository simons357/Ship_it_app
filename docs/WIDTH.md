# Relative width and the narrow residual

24 September 2026.
**The width identities sit.
The crossover is not a useful K.
No primitive for \(S_\Gamma\) is on this desk.
Not a close. ★ stays killed.
Catalog B open stays 1.**

Incoming ledger:
[`CENTERED-LEDGER.md`](CENTERED-LEDGER.md).
Chart reset:
[`RESET.md`](RESET.md).
Overnight page:
[`DA-NS-2.md`](DA-NS-2.md).
Target:
[`CENTERED-DRIFT.md`](CENTERED-DRIFT.md).
SBP / \(\Phi_e\):
[`SBP.md`](SBP.md).
Press (Lemma A sits;
Lemma B OPEN):
[`PRESS.md`](PRESS.md).
Sign gate:
[`SIGN-GATE.md`](SIGN-GATE.md).
Machine: `python3 scripts/width.py`.
Does not overwrite `stokes_moments.py`.
Do not start leftover 1.
Do not weld \(\star\).
Do not cash B★ as G5.
Do not seat JGC.
Do not invent a bridge.
This is the DA job from the
incoming board: classify
BROAD / NARROW independently,
check the identities, hunt
an existing primitive for
\(S_\Gamma\). Stop there.

---

## Three kinds of sentence

**Exact.** \(r^2=\mathcal D_s/(\Lambda Y)\),
\(\mathcal D_s/Y=\Lambda r^2\),
and the three writings of the
named threshold. Conversion
\(\delta\approx r\kappa/2\).

**Named comparison.**
\(r\sim\kappa^{-1/2}\) as a
filing line. Not a payment.

**Not on this desk.**
\(\mathsf B^{\mathrm{prim}}w=b\),
loop-network rank, Vandermonde
cubic depletion as a remainder,
solver \(J_{\theta,n}\),
\(\alpha_{c,\kappa}^2\chi_\kappa^2\kappa\).
MASTER dossier is not in this
repo.

---

## Width — EXACT

On this desk \(\Lambda=Y/X\) is
a convex combination of
eigenvalues of \(A=-P\Delta\).
Those eigenvalues are
\(\lvert k\rvert^2\). So
\(\kappa=\sqrt{\Lambda}\) is
an \(\lvert k\rvert\)-scale.
Do not read \(\Lambda\) as
\(\lvert k\rvert\).

Let \(\sigma^2=\mathcal D_s/X\).
Then \(\sigma\) is the absolute
width of the \(\lvert k\rvert^2\)
measure. Define

\[
r^2
=
\frac{\mathcal D_s}{\Lambda Y}
=
\frac{\mathcal D_s}{X\Lambda^2}
=
\Bigl(\frac{\sigma}{\Lambda}\Bigr)^2.
\]

\(r\) is the relative
\(\lvert k\rvert^2\)-width.
Dimensionless.

\[
\frac{\mathcal D_s}{Y}
=
\Lambda r^2.
\]

Locked today.

---

## The named threshold —
equivalence only

\(\kappa=\sqrt{\Lambda}\). The
three writings

\[
r\gtrsim\kappa^{-1/2},
\qquad
\frac{\mathcal D_s}{Y}\gtrsim\kappa,
\qquad
\Lambda r^2\gtrsim\sqrt{\Lambda}
\]

are the same comparison.
Exact as an equivalence.
Not a theorem that anything
is paid.

They call the first side
BROAD. That is a name for
the region
\(\mathcal D_s/Y\gtrsim\sqrt{\Lambda}\).
It is not viscosity winning.

The clock remains

\[
(\log\Lambda)'
=
\frac{2}{Y}(T_c-\nu\mathcal D_s)
=
2\Bigl(\frac{T_c}{Y}-\nu\frac{\mathcal D_s}{Y}\Bigr).
\]

BROAD makes the viscous
summand \(\gtrsim\nu\kappa\).
The remainder is still
\([T_c-\theta\nu\mathcal D_s]_+/Y\).
A large \(\mathcal D_s/Y\)
does not bound \(T_c/Y\).
Amplitude still kills
\(T_c\le c\nu\mathcal D_s\).
[`DA-NS-2.md`](DA-NS-2.md).

B★ already spent a
\(\Lambda^{1/2}\). That box
has no universal \(C\).
Do not glue B★ to this
threshold.
[`BSTAR.md`](BSTAR.md).

---

## Do not stamp
\(r\sim\kappa^{-1/2}\)

Scale wave-numbers by
\(\ell>1\). Eigenvalues
\(\lvert k\rvert^2\) become
\(\ell^2\lvert k\rvert^2\).

\[
r\mapsto r,
\qquad
\kappa\mapsto\ell\kappa,
\qquad
\kappa^{-1/2}\mapsto\ell^{-1/2}\kappa^{-1/2}.
\]

\(r\) is invariant. The
threshold moves. Locked
today on a three-mode
ladder.

On the unit torus the
comparison is a number.
On \(\mathbb{R}^3\), or
under rescaling, it is
not a universal region
boundary. The
“missing-half-derivative”
slogan is that number
\(\sqrt{\Lambda}\). It is
not a seated Sobolev
estimate.

---

## Conversion, not a mix

If \(\delta\) is an absolute
\(\lvert k\rvert\)-gap, then
\(\mathrm{d}(\lvert k\rvert^2)\approx 2\kappa\,\delta\),
so

\[
\delta\approx\frac{r\kappa}{2},
\qquad
\kappa^2\delta=\tfrac12\kappa^3 r.
\]

Thus the older
\(R_\Lambda-2\kappa^3=O(\kappa^2\delta)\)
and the newer
\(O(\kappa^3 r)\) are the
same leading order if
\(r\) is this relative
\(\lvert k\rvert^2\)-width
and \(\delta\) is the
\(\lvert k\rvert\)-gap.
They did not silently mix
those two writings.

They would mix them if
\(r\) were reused for
\(\delta/\kappa\) without
the factor \(1/2\), or if
\(\Lambda\) were read as
\(\lvert k\rvert\). This
desk does not.

The \(O(\,\cdot\,)\) is
not a remainder and not
a useful \(K\).

---

## BROAD is not a payment

A growing equal-mass
ladder on
\(\lvert k\rvert^2=1,\dots,n^2\)
keeps \(r=\Theta(1)\) while
\(\kappa^{-1/2}\to 0\).
It is BROAD by their
threshold for large \(n\).

The growing-layer family
\(v_n\) has the same
support idea, and it is
the named kill of
unrestricted ★. A state
can be BROAD and still
leave a cubic remainder
that viscosity does not
catch.

Do not file
“BROAD \(\to\)
\(\mathcal D_s\) / viscosity”
as a seated door.

Homochiral Vandermonde
\((x-y)(y-z)(z-x)\) is
**not locked** here.
Do not seat cubic
depletion.

Narrow heterochiral
radial \(O(\kappa^3 r)\)
is the conversion above,
not a payment.

---

## The residual — already
named, not paid

For a frozen epoch they
write

\[
T_{c,\Gamma}^{\mathrm{het}}
=
2\kappa_e^3 Q_{a,\Gamma}
+\rho_{\Gamma,e}^{\mathrm{rad}}
+\rho_{\Gamma,e}^{\mathrm{mov}},
\qquad
\rho^{\mathrm{mov}}
=
-(\Lambda-\kappa_e^2)S_\Gamma.
\]

Charge-only is already
dead: \(Q_{a,1}+Q_{a,2}=0\)
and \(T_c^{\mathrm{het}}>0\).
Machine:
`scripts/centered_barycenter.py`.

\(S_\Gamma=\sum_\gamma A_\gamma Q_{a,\gamma}\)
is their compression of
the \(\Lambda\)-derivative
of the heterochiral
coefficient. Algebra on
their network. Not a
remainder.

The two hard pieces
\(2\kappa^3 Q_a\) and
\(-(\Lambda-\kappa_e^2)S_\Gamma\)
are the same leftover
already on
[`DA-NS-2.md`](DA-NS-2.md)
and
[`CENTERED-LEDGER.md`](CENTERED-LEDGER.md).
Filing them as
“NARROW HET CHARGE +
MOVING CENTER” does not
pay DA-NS-2.

---

## Primitive hunt —
nothing sits under
another name

The question: is there a
quadratic
\(\mathcal H_w=\sum_m w_m\lvert a_m\rvert^2\)
whose nonlinear
derivative is \(S_\Gamma\)?

On this desk:

- \(W_K=\mathcal D_s+X(\Lambda-K)^2\)
  is frozen variance.
  Chart object.
  [`RESET.md`](RESET.md).
  Not charge.
- \(H^\varepsilon=\sum_{k,s}\varepsilon s\,\lambda_k\lvert a_k^s\rvert^2\)
  is quadratic
  helicity-weighted
  intensity. Phase twins
  kill it as a sign
  predictor.
- \(\Psi_\lambda^\varepsilon=\frac12\sum_{k,s}\varepsilon s\,\lvert k\rvert^2(\lvert k\rvert^2-\lambda)\lvert a_k^s\rvert^2\)
  is the signed-helicity
  potential from the
  incoming ledger §13.
  Its nonlinear
  derivative produces
  the \(\eta\)-side
  (oriented radial
  transfer), not
  \(S_\Gamma=\sum A_\gamma Q_{a,\gamma}\).
  Charge is not transfer.

No other quadratic
capacity on this branch
is named as a primitive
for \(S_\Gamma\).

The finite test
\(\mathsf B^{\mathrm{prim}}w=b\),
\(c^Tb=0\) on
\(\ker(B^T)\), is
**not implemented**
here. The loop networks
are not on this branch.
Do not invent the
matrix. Do not invent
a holonomy. Do not
run another
optimizer and call it
this test.

If Heavy later prints
rank, left-nullspace,
and \(c^Tb\), that print
is a finite obstruction
or a finite \(w\). It is
not a uniformly
conditioned
translation-invariant
primitive, and it is
not DA-NS-2.

---

## Endpoint — still OPEN

\[
\sup_n\int_0^T
\frac{[T_c^{(n)}-\theta\nu\mathcal D_s^{(n)}]_+}{Y^{(n)}}\,dt
<\infty
\]

is the Y-cousin target.
Tautological if \(K\) is
defined as that ratio.
Leftover 4 / G4. Not
leftover 1. Not leftover
6.
[`CENTERED-LEDGER.md`](CENTERED-LEDGER.md).

The schematic map
DANGEROUS \(\to\)
BROAD / NARROW HOMO /
NARROW HET RADIAL /
NARROW HET CHARGE +
MOVING CENTER
is a filing system.
It is not a close.

Things this page will
not spend compute on:
single-circle searches,
isolated stars, constant
coherence, generic phase
rotation, homochiral
optimization, exact-shell
counting, solver
reruns, \(\alpha_{c,\kappa}^2\chi_\kappa^2\kappa\).

---

## What this page is not

- A close. Catalog B open
  stays 1.
- A seating of DA-NS-2.
- A restoration of ★.
- A seating of B★, LX,
  SAG, JGC, or
  \(\mathsf B^{\mathrm{prim}}\).
- A named death of G4.
- Leftover 1. Do not
  start H1.
- A bridge from
  \(\Psi\) to \(S_\Gamma\).
- [`PATH-TO-CLOSE.md`](PATH-TO-CLOSE.md).

---

## Lock

The width identities sit.
The crossover is not a useful K.
No primitive for \(S_\Gamma\) is on this desk.
G4 stays OPEN. ★ stays killed.
NS not solved.
