# Process Console v5

Engineering lane for Domain Architect. The console represents process
state. It does **not** depend on Q4’s scientific outcome.

Q4-1 v1 displaying **INCONCLUSIVE** is an acceptance test.

## Two lanes

| Lane | Object | Status |
|---|---|---|
| Engineering | F-X2 relabels + Process Console against schema v5 | live |
| Engineering | Q4-1 v1 (defective historical run) | **INCONCLUSIVE** — failed consistency check; promotion prohibited |
| Research | Q4-1 v2 preregistered run | not executed; not evidence |

v2 uses the direct \(D_s\) form, a scale-aware residual, locked
code/hashes, and the same decision window unless DA changes it
*beforehand*. It does not inherit the old 3,915/663 diagnostic
imbalance as evidence.

## Stamp kinds (schema, not prose)

| Kind | Meaning |
|---|---|
| **PROVED** | derivation independently checked |
| **REPRODUCED** | independent computation matched |
| **CONSISTENCY CHECK** | implementation agrees with stipulated formula/assumptions |

`DA-STAMPED` is not a stamp kind. F-X2 refuses it unless an intended
kind is supplied.

## Commands

```bash
python -m domain_architect --console
python -m domain_architect --console --json
python scripts/serve_process_console.py --port 8765
python scripts/sweep_withdrawn_taylor_green.py
python -m unittest tests.test_process_console tests.test_atlas_and_errata
```

## Taylor–Green live values

\[
r^2=2.2291,\qquad \frac{T_c}{\nu D_s}=1.2259
\]

The old fraction \(3915/663\) is **WITHDRAWN**. It may appear only as
historical provenance.

## Atlas

Closed shortcut, not bad news:

\[
\boxed{\text{Broad spectral state}\not\Rightarrow
\text{pointwise viscous payment of the dangerous term}.}
\]

An error does not disappear. It produces an erratum, a regression
test, a process rule, and a cleaner scientific statement.

Ordinary 3-D Navier–Stokes is **not solved**.
