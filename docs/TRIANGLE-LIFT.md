# First lift — what the triangles still imply

23 September 2026.
**The identities lift through any
number of triangles on the same
two shells. They stop at a third
eigenvalue, and they stop when
\(B\) leaves the triangle.
\(K\sim\sqrt{E}\) is dead.
Not a close. ★ stays killed.
Catalog B open stays 1.**

Geometry:
[`FOURIER-TRIANGLE.md`](FOURIER-TRIANGLE.md).
Energy-class ladder (Y-door
and mid door die with
\(K\sim\sqrt{E}\)):
[`ENERGY-K.md`](ENERGY-K.md).
Pairing CS:
[`L-DOOR.md`](L-DOOR.md).
Target:
[`CENTERED-DRIFT.md`](CENTERED-DRIFT.md).
Machine: `python3 scripts/triangle_lift.py`.
Does not overwrite `stokes_moments.py`.
Do not start leftover 1.
Do not weld \(\star\).
Do not cash B★ as G5.

---

## Three kinds of sentence

**Exact.** Gap-cancel is about
eigenvalues, not about one
triangle. Leak of \(B\) is a
support fact.

**Numerical.** Printed \(T_c\)
on named sums. Residuals.

**Illustrative.** None on this
page. The leak is exact. Do
not call it motion.

---

## What still sits

One two-shell triangle. Gap
formula sits. Already locked.

Two triangles on the *same*
two eigenvalues — here
\((1,1,0)+(1,-1,0)=(2,0,0)\)
and
\((0,1,1)+(0,-1,1)=(0,0,2)\),
shells \(2\) and \(4\),
disjoint keys. The sum still
has exactly two shells. The
gap formula still matches
\(T_c\) at \(10^{-9}\).

So: **more triangles is not
the first loss.** Same two
shells keep
\[
T_c
=
(\alpha-\beta)\frac{\alpha\beta E}{X}\,T_\alpha.
\]
Need★ on that \(T_\alpha\) is
still MISSING. The reduction
is not a bound.

\(T_c(u+v)\) is not claimed
equal to \(T_c(u)+T_c(v)\).
\(\Lambda\) of the union can
move. On the balanced pair
the sum matched. On an
unbalanced pair the residual
printed \(\approx 0.017\).
That is a reading, not the
identity. The identity is
the gap formula.

---

## First loss at the sum

Add a second two-shell
packet on *different*
eigenvalues:
\((2,1,0)+(1,2,0)=(3,3,0)\),
shells \(5\) and \(18\).

The union has four
eigenvalues. The two-shell
formula does not apply.
Printed

| Object | \(T_c\) |
|---|---|
| \(2,4\) triangle | \(+3.66\) |
| \(5,18\) triangle | \(-80.9\) |
| Sum of those two numbers | \(-77.2\) |
| \(T_c\) of the union | \(-106.4\) |

Cross vertices fire.
The pairing of the union is
not the sum of the pairings.
That is the first place the
one-triangle geometry is
lost under summation.

A three-shell unequal
triangle is the same loss
with one object: no
single-gap reduction.

---

## First loss at the evolution

An isolated conjugate-closed
triangle is **not** closed
under \(B\).

On the \(2,2,4\) triangle:
six supported keys, eighteen
pairwise landings, four
off-support landings live.
\[
\frac{\|B_{\mathrm{off}}\|_2^2}{\|B\|_2^2}
\approx 0.335.
\]

The identity that lives on
those six keys does not
govern the next instant.
New keys light. Two
same-shell triangles leak
harder (\(\approx 0.84\)).
This is exact, not a phase
cartoon.

---

## First energy-class remainder

After the identities, the
first remainder that would
have been useful for Route B
is
\[
\lvert T_c\rvert
\le
C\sqrt{E}\,X
\qquad\Rightarrow\qquad
K\sim\sqrt{E}.
\]
Amplitude matches (\(A^3\)
over \(A\cdot A^2\)). \(E(t)\)
is bounded by the energy
inequality, so a true
uniform \(C\) would give
\(\Lambda'\le 2C\sqrt{E}\)
and close \(X\) on the
torus.

**It does not sit.**
Ratio \(R_E=\lvert T_c\rvert/(\sqrt{E}\,X)\):

| Family | \(R_E\) |
|---|---|
| \(v_1\) | \(0.114\) |
| \(v_4\) | \(13.0\) |
| \(v_8\) | \(144\) |
| imag \(\lvert k\rvert^{-2}\), \(K=4\) | \(5.90\) |
| imag \(K=6\) | \(28.3\) |
| imag \(K=8\) | \(86.7\) |

\(v_n\) kills this \(C\).
That family did **not** kill
B★. Different box. Do not
glue. Imag climbs too.
Finite \(144\) is not \(C\).
Do not AM-GM a false line.

B★ stays not a universal
\(C\). This page does not
restore it and does not
replace it.

---

## The missing implication, now as a lift

Preserve the identities.

They imply the two-shell
gap formula on every field
supported on two
eigenvalues, however many
triangles sit there.

They do **not** imply

1. a gap formula after a
   third eigenvalue,
2. a closed evolution on
   one triangle,
3. a useful \(K\) in
   \(T_c\le\theta\nu\mathcal D_s+K(t)X\).

(3) is G4. The first
energy-class candidate for
that \(K\) is dead. G4 stays
OPEN. G5 is not this page.
Need★ cannot repair the
dead unrestricted box.
Tautological \(K\) is not
this arrow.

---

## What this page is not

- A close. Catalog B open
  stays 1.
- A seating of B★.
- A restoration of ★.
- Leftover 1. Do not start
  H1.
- An NSE trajectory.

---

## Lock

Same two shells: gap-cancel
sits. Third eigenvalue:
gap-cancel dies. \(B\)
leaks off an isolated
triangle. \(K\sim\sqrt{E}\)
is killed by \(v_n\).
The identities still do
not give a useful \(K\).
★ stays killed.
NS not solved.
