# Chart reset of the frozen variance

24 September 2026.
**The jump is exact.
The remainder does not jump.
A reset is not a useful K.
Not a close. ★ stays killed.
Catalog B open stays 1.**

Incoming ledger:
[`CENTERED-LEDGER.md`](CENTERED-LEDGER.md).
Overnight page:
[`DA-NS-2.md`](DA-NS-2.md).
Target:
[`CENTERED-DRIFT.md`](CENTERED-DRIFT.md).
Short interval:
[`INTERVAL.md`](INTERVAL.md).
Machine: `python3 scripts/reset.py`.
Does not overwrite `stokes_moments.py`.
Do not start leftover 1.
Do not weld \(\star\).
Do not cash B★ as G5.
Do not seat JGC.
This is **not** a reconstruction
of the truncated §17 paste.
It is the unique algebra that
follows from the sitting \(W_K\).

---

## Three kinds of sentence

**Exact.** \(W_K\) and \(\Delta W\)
with the physical state held
fixed. Chart-invariance of
\(K_{\min,\theta}\).

**Numerical.** Printed two-shell
samples. Finite is not \(C\).

**Not on this desk.** Their
boxed §17 text after the break.
JGC as a seated epoch budget.

---

## Frozen variance — already
PROVED

\[
W_K
=
\mathcal D_s+X(\Lambda-K)^2
=
Z-2KY+K^2X
=
\lVert A^{1/2}(A-K)u\rVert_2^2
\ge 0.
\]

Locked on
[`CENTERED-LEDGER.md`](CENTERED-LEDGER.md).
An identity is not a close.

Continuous tracking \(K=\Lambda\)
makes \(W_\Lambda=\mathcal D_s\).
That is a rename, not an
estimate.

---

## Exact jump — locked today

At a chart reset
\(K_e\to K_{e+1}\), the
physical field is fixed.
So \(X,Y,Z,\Lambda,\mathcal D_s,T_c\)
do not jump. Only the chart
does.

\[
\Delta W
=
W_{K_{e+1}}-W_{K_e}
=
X\bigl[(\Lambda-K_{e+1})^2-(\Lambda-K_e)^2\bigr]
=
X(K_e-K_{e+1})(2\Lambda-K_e-K_{e+1}).
\]

Reset to the live barycenter
\(K_{e+1}=\Lambda\):

\[
\Delta W
=
-X(\Lambda-K_e)^2
\le 0,
\qquad
W_\Lambda=\mathcal D_s.
\]

Reset away from \(\Lambda\) can
raise \(W\). The sign is a chart
choice.

The incoming paste broke at
\(\Delta W\) / \(X[(\Lambda-K_{e+1})^\,\).
That fragment is consistent
with the sitting squares
formula. We do not fill their
box. We lock the algebra that
already follows from \(W_K\).

---

## The remainder does not jump

The Y-cousin integrand is

\[
K_{\min,\theta}
=
\frac{[T_c-\theta\nu\mathcal D_s]_+}{Y}.
\]

It depends on
\((T_c,\mathcal D_s,Y,\nu,\theta)\)
only. The chart \(K\) is not
in the formula.

Therefore

\[
\Delta K_{\min,\theta}=0
\]

at every reset. Chart jumps
do not pay DA-NS-2. They do
not pay Route B
\(T_c\le\theta\nu\mathcal D_s+K(t)X\)
either. The pairing is physical.

Locked today on the printed
two-shell samples: \(K_{\min}\)
is identical before and after
the jump, including when \(W\)
drops.

---

## Frequent resets are circular
for \(\mathrm{TV}(\Lambda)\)

Reset to each successive
barycenter \(\Lambda_i\). Then

\[
\sum_i\lvert\Delta W_i\rvert
=
X\sum_i(\Lambda_i-\Lambda_{i-1})^2.
\]

That is not \(X\cdot\mathrm{TV}(\Lambda)\).
A mesh of size \(\delta\) makes
each jump \(X\delta^2\) and
\(\sum(\Delta\Lambda)^2\le\delta\cdot\mathrm{TV}(\Lambda)\).
A bound on the jump ledger is
then weaker than a bound on
\(\mathrm{TV}(\Lambda)\), which is
the thing G4 is trying to pay
(\(\int\lvert\Lambda'\rvert\)).

If the last line is “resets
are summable, therefore
\(\Lambda\) stays finite,” stop.
That assumes the leftover.
JGC stays a target, not a
theorem.
[`DA-NS-2.md`](DA-NS-2.md).

Do not pointwise-estimate
\(\dot S_\Gamma\). Keep that.
A Stieltjes rewrite of the
moving-center residual is
not a remainder.

---

## What this page is not

- A close. Catalog B open
  stays 1.
- A seating of DA-NS-2.
- A restoration of ★.
- A seating of B★, LX, SAG,
  or JGC.
- A named death of G4.
- Leftover 1. Do not start
  H1.
- Their missing boxed text.
- [`PATH-TO-CLOSE.md`](PATH-TO-CLOSE.md).

---

## Lock

The jump sits. The remainder
does not jump. A reset is not a useful K.
G4 stays OPEN. ★ stays killed.
NS not solved.
