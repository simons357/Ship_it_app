# Energy-class remainder ladder — dead

23 September 2026.
**The only amplitude-legal energy
remainders are \(K\sim\sqrt{E}\)
and \(K_Y\sim\sqrt{E}\).
Both die on \(v_n\). The mid
door dies with them.
Not a close. ★ stays killed.
Catalog B open stays 1.**

Geometry:
[`FOURIER-TRIANGLE.md`](FOURIER-TRIANGLE.md).
First lift:
[`TRIANGLE-LIFT.md`](TRIANGLE-LIFT.md).
Target:
[`CENTERED-DRIFT.md`](CENTERED-DRIFT.md).
Machine: `python3 scripts/energy_k.py`.
Does not overwrite `stokes_moments.py`.
Do not start leftover 1.
Do not weld \(\star\).
Do not cash B★ as G5.
Do not cash Attack-2 \(C_*\)
as this death.

---

## Three kinds of sentence

**Exact.** Homogeneity, and the
closed forms for \(E_n,X_n,Y_n,T_c\)
on the growing-layer family.

**Numerical.** Printed ratios on
named fields. Imag cutoff is a
reading, not the boxed kill.

**Illustrative.** None. Amplitude
is exact scaling, not a cartoon.

---

## Homogeneity (exact)

\(T_c\) is cubic. \(E,X,Y,\mathcal D_s\)
are quadratic. \(\Lambda\) is
scale-invariant.

An X-remainder
\(\lvert T_c\rvert\le C E^p X\)
is amplitude-legal only at
\(p=1/2\):
\[
\lvert T_c\rvert
\le
C\sqrt{E}\,X
\qquad\Rightarrow\qquad
K\sim\sqrt{E}.
\]
If that \(C\) sat,
\(\Lambda'\le 2C\sqrt{E}\)
and \(X\) would close on the
torus.

A Y-remainder
\(\lvert T_c\rvert\le C E^q Y\)
is amplitude-legal only at
\(q=1/2\):
\[
\lvert T_c\rvert
\le
C\sqrt{E}\,Y
\qquad\Rightarrow\qquad
K_Y\sim\sqrt{E}.
\]
If that \(C\) sat,
\((\log\Lambda)'\le 2C\sqrt{E}\)
and \(\log\Lambda\) would close.

The unique amplitude-matched
interpolation is
\[
\lvert T_c\rvert
\le
C\sqrt{EXY}
\qquad\Rightarrow\qquad
K\sim\sqrt{E}\,\Lambda^{1/2}.
\]
If that \(C\) sat,
\((\sqrt{\Lambda})'\le C\sqrt{E}\).

Locked on the live \(2,2,4\)
triangle: \(R_E\), \(R_Y\), and
\(R_{\mathrm{mid}}\) stay flat
from amplitude \(0.05\) to \(4\).
The illegal doors do not:
\(p=1\) blows as \(A\to 0\);
\(p=0\) blows as \(A\to\infty\).

The already-dead line
\(\lvert T_c\rvert\le C\|u\|_2 X^{3/2}\)
is \(a^3\) versus \(a^4\). It is
not this ladder and not
Attack-2 \(C_*\).

---

## The X-door is already dead

\(R_E=\lvert T_c\rvert/(\sqrt{E}\,X)\)
grows on \(v_n\)
(\(0.114\to 144\)) and on imag
cutoff. Locked in
[`TRIANGLE-LIFT.md`](TRIANGLE-LIFT.md).
No other \(p\) is amplitude-legal.
The X-ladder has one rung, and
it is gone.

---

## The Y-door and the mid door

Closed forms on \(v_n\), same
family that killed ★:

\[
E_n=4(2n+1),
\quad
T_c=3n^5(3n^2+3n+1),
\]

\[
X_n=\frac{n(2n+1)(49n+4)}{3},
\]

\[
Y_n=\frac{n(2n+1)}{15}
\bigl(1167n^3+174n^2+8n-4\bigr).
\]

Then
\[
R_Y
=\frac{\lvert T_c\rvert}{\sqrt{E}\,Y}
\sim c\,n^{3/2}\to\infty,
\qquad
R_{\mathrm{mid}}
=\frac{\lvert T_c\rvert}{\sqrt{EXY}}
\sim c\,n^{5/2}\to\infty.
\]

Printed:

| Family | \(R_E\) | \(R_{\mathrm{mid}}\) | \(R_Y\) |
|---|---|---|---|
| \(v_1\) | \(0.114\) | \(0.051\) | \(0.0225\) |
| \(v_4\) | \(13.0\) | \(1.48\) | \(0.168\) |
| \(v_8\) | \(144\) | \(8.22\) | \(0.469\) |
| imag \(K=4\) | \(5.90\) | \(1.70\) | \(0.489\) |
| imag \(K=6\) | \(28.3\) | \(5.77\) | \(1.17\) |
| imag \(K=8\) | \(86.7\) | \(13.6\) | \(2.15\) |

\(v_n\) kills both remaining
doors. Same quality as the ★
kill: closed form, not a
finite sample. Imag climbs
too. Separated triads do
**not** kill \(R_Y\) (it
falls). Different family.
That family did **not** kill
B★. Different box. Do not
glue.

Finite \(0.469\) is not \(C\).
The arrow is \(n^{3/2}\).

---

## What this does not kill

Pathwise \(K(t)\) that is not
a function of energy-class
norms of the snapshot. The
tautological coefficient is
still forbidden.
B★ is a \(\mathcal D_s\)
door and still not a
universal \(C\).
Attack-2 \(C_*\) is
\(\lvert T_c\rvert\le C_* X^{3/2}\Lambda\),
amp-invariant, not this
ladder.

G4 stays OPEN. G5 is not
this page. The energy-class
door for a useful \(K\) is
closed. A later write must
produce cancellation in
\(T_c=M-\Lambda N\) itself,
or a named death of G4
that is not this ladder.

---

## What this page is not

- A close. Catalog B open
  stays 1.
- A seating of B★.
- A restoration of ★.
- A death of Attack-2 \(C_*\).
- Leftover 1. Do not start
  H1.
- An NSE trajectory.

---

## Lock

Only \(p=q=1/2\) matches
amplitude. \(K\sim\sqrt{E}\)
was already dead. \(K_Y\sim\sqrt{E}\)
and the mid door die on \(v_n\)
by closed form,
\(R_Y\sim n^{3/2}\).
The energy-class ladder is
dead. G4 stays OPEN.
★ stays killed.
NS not solved.
