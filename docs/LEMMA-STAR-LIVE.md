# Lemma★ — two live writes

12 September 2026. Desk lock.
**NS not solved. Lemma★ OPEN.**

Phone: stay in this chat.

Everything else is scored or refused.
Kill lane is still live.

---

## 1. Attack 9D — remaining packet falsifier

Growing input and output supports.
Full complex polarizations.
Frequency factors kept.

Target
\[
\|\Pi_\beta B(w,w)\|_2
\le
C\alpha\beta^{-1/2}\|w\|_2^2
\qquad\Longleftrightarrow\qquad
\sup K_{\alpha,\beta}<\infty.
\]
\(B=B(w,w)\) is the same exact-shell bilinear.
It is not a new field.

Fixed-output \(\Theta(m^2)\) is excluded
(\(K\le 16s\)). Freiman-AP is dead.
Do not rebuild either.

Do not redo finite 9B (\(K\approx 0.641\),
linear pol). Bounded samples are not a
proof. A diverging family would kill ★.
A larger finite number only raises
\(C_{\mathrm{geom}}\).

Probe: `python3 scripts/ns_attacks/attack9d_growing.py`

First full-complex sweep (seed 1390, 225
fields): max \(K\approx 0.612\) at \((4,8)\).
Did not beat finite 9B \(0.641\). Did not
diverge. Not a proof. Kill lane LIVE.
`results/attack9d_growing/`

Linear-pol growing-\(s\) samples (scored,
not this close):
[`LEMMA-STAR-9B-GROWING.md`](LEMMA-STAR-9B-GROWING.md).

---

## 2. The ★ reason — not written

Why signed stretching cannot outrun
spectral spread. HH→L is the dangerous
channel.

That sentence is not written.
Until it is, or until
\(\mathcal R_\star\to\infty\) on a family,
Lemma★ stays OPEN.

Do not invent it.
[`LEMMA-STAR-REASON.md`](LEMMA-STAR-REASON.md).
[`LEMMA-STAR-R.md`](LEMMA-STAR-R.md).

---

## Optional, not next by default

- Recompute user-reported 9C
  (\(0.11\to 0.031\)) with locked
  \(\mathcal R_\star\) (never done here).
- Exact-core ABC at \(\lambda=8\) and \(16\)
  on one field definition, plus a fixed-\(\lambda\)
  resolution study. Clue only. Do not stop
  patching ★ from more finite rows.
- H1 on the cylinder — started on shape 3
  (predictive CF ODE; fold/sheet/gap).
  Alignment frozen. Not a theorem.
  Do not start it from ABC screenshots.
  [`H1-PREDICTIVE.md`](H1-PREDICTIVE.md).

---

## Do not redo

Five lanes 1–5, 9A, natural 9C, finite 9B
(\(K\approx 0.641\)), small-lattice truncated
ABC, SuperGrok falsifier stamp, Dream Team
vote.

---

## Out of this book

Q-stack, SND, Theorem H, five fingers,
augmented NSE.

NS not solved. Lemma★ OPEN.
