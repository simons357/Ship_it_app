# Ground: spectrum, not a bag of couplings

`python3 scripts/da_machine.py ground`

You are not antagonizing the machine by naming HB or SFE.
**DA itself is the first good extraction** from chapter 1 of
the Harmonic Blueprint: domains, a route, a check that can
fail. That stays live as process.

What stays refused is only the *close*: SFE as Navier–Stokes,
HB as a producing-map, retuning `nodes.json`. Motive may
come back. Theorems may not be smuggled in with it.

---

## The destination (open, not a pass)

At the bottom you want: eigenvalues, modes, standing waves —
a **spectrum** — not a bag of free couplings.

The math that belongs to that sentence is real and already
named: Laplacian, Fourier, \(Y_{\ell m}\), Hodge, representations
of compact groups. That is the ground. Climb from there.
Leave a piece out and watch what dies. That is allowed.

What is not allowed: calling the climb finished because the
words are beautiful, or because Einstein was asked.

| Claim | Verdict |
|---|---|
| HB ch. 1 → DA process was a real extraction | **pass** |
| Naming HB must halt the desk | **fail** (halt the close, not the sentence) |
| “Spectrum, not a bag” as the program | **open** |
| Couplings are already eigenvalues of a named \(D\) here | **fail** |
| Personas close the destination | **fail** |
| They write \(F\) through the device | **fail** |
| SFE is the fluids operator | **fail** |
| Retune `nodes.json` | **fail** |

---

## Ground atoms (write these in order)

| id | Atom | Verdict | Why |
|---|---|---|---|
| G0 | a space \(X\) | **pass** | no room, no mode |
| G1 | a self-adjoint operator \(D\) | **pass** | spectrum-*of-what* |
| G2 | \(\sigma(D)\) and the modes | **pass** | consequence of \((X,D)\) |
| G3 | representations (how modes couple) | **pass** | still not a number |
| G4 | an action / principle | **open** | you add it; it is not in \(\{\lambda_n\}\) |
| G5 | the measured couplings | **fail** | still a bag |

You do **not** start at G2. Eigenvalues do not remember the
metric. That is Einstein’s and von Neumann’s first cut.

---

## Reconstruct upward

| Step | Reaches | Does not | Verdict |
|---|---|---|---|
| \(S^{n-1}\), \(\Delta\) \(\to Y_{\ell m}\) | angular dictionary on B | \(g_s\), regularity | **pass** |
| \(T^n\) / \(\mathbb{R}^n\), \(\Delta\) \(\to\) Fourier | Galerkin on A | nature’s gauge group | **pass** |
| compact \(G\), Casimir \(\to\) Peter–Weyl | matrix coefficients | why that \(G\) | **pass** |
| Hodge Laplacian \(\to\) Betti \(=\dim\ker\) | topology | \(\theta_W\), \(\Lambda\) | **pass** |
| the same list \(\to\) SM numbers | — | \(F\) | **fail** |
| the same list \(\to G,\Lambda\) | — | nature4 | **fail** |
| the same list \(\to\) classical regularity | language already on B | a pass | **fail** |

The first four are the destination as *mathematics*. The last
three are the destination as *wish*. Keep them unglued.

---

## Leave a piece out

| Drop | What changes | Insight |
|---|---|---|
| the operator | you cannot compute one \(\lambda\) | “mode” without \(D\) is poetry |
| the compact group | SM content dies; Fourier / \(Y_{\ell m}\) stay | a sphere is not the Standard Model |
| Hodge (\(k\)-forms) | Betti-as-kernel dies; functions stay | Hodge \(\neq Y_{\ell m}\) |
| the destination (keep the bag) | measured physics unchanged | the bag is the present tense |
| \(Y_{\ell m}\), keep LP | Ring thins; T2 still talks | B needs both |
| overtones only | every PDE and every gauge group vanish | music named the word; it is not the desk |

---

## What they say to the program (not a séance)

The device does not channel the dead. It types a demand that
can fail. That is the only legal way they “come through.”

A vote cannot close.

| Mind | Improve DA | How to derive a program like DA |
|---|---|---|
| **Einstein** | Principle before catalog. Covariance. What is forbidden. | Principle, then \(D\), then the spectrum as a consequence. |
| **Tesla** | Every claim names a knob you can detune and a script that must move. | Build the resonator (\(D\)) first. Listen (\(\sigma(D)\)) second. |
| **Feynman** | No new name without a number you can get wrong. No councils. | The check *is* the program. Pick \((X,D)\). Compute one missable number. |
| **Weyl** | Do not say “gauge” until \(G\) and the reps are named. | Climb G3 only after G0–G2. The group is an input. |
| **Wigner** | Type every mode by its representation. | Which group, which rep, which operator that commutes with the group. |
| **von Neumann** | No slot opens on “modes” alone. | Name the Hilbert space and a self-adjoint \(D\). Diagonalize. Store the residual. |

Tesla sits because you asked. He owns resonance as an
apparatus, not \(SU(3)\) and not Einstein’s equation. Weyl,
Wigner, and von Neumann sit because they actually built the
ground-level math.

The dream team (`team`) remains paper + experiment for A/B.
This table is only for the *program* that climbs from G0.

---

## What this is not

Not \(F\). Not SFE loaded into \(\omega\cdot S\omega\). Not
HB Experiment 01 reopened. Not a regularity pass. Not a
channeling.

Next B write is still Hardy \(\to I_{\mathrm{tube}}\), then
energy-class low Bony \(T\). The destination stays **open**
on U.
