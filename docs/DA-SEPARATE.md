# Each candidate, alone

`python3 scripts/da_machine.py separate`

You asked to run each one separately. DA did. Four decks.
No bundles. Isolation did not promote anyone to a unifier.

## GQ — gravity + quantum, one pair at a time

| Alone | Verdict |
|---|---|
| Einstein \(G_{\mu\nu}\leftrightarrow T_{\mu\nu}\) | **pass** |
| Equivalence (same metric in every species) | **pass** |
| \(\Lambda\) | **open** |
| QFT on curved | **pass** |
| Vacuum \(\to\) gravity | **fail** |
| Hierarchy \(G v^2\) | **open** |
| Graviton \(\leftrightarrow\) SM | **open** |
| gauge3 \(\leftrightarrow G\) | **fail** |

## PUB — each published program alone

gauge3 and nature4 are **not** glued. MSSM-class is open as
a 3-gauge meet and fail as four-force unification, even
when it is the only claim on the table. Minimal SU(5) and
the SM fail alone. This repo’s \(R\) fails alone.

## SIX — each of the reconstructed 16 alone

Singleton lock-\(R\) only. Vacuum and Planck still move
\(R\). \(S_c\) and \(\delta\) still move \(R\). The
couplings stay must-hit and decorative. \(R\) alone is
still circular. Isolation does not write \(F\).

## COSMO — each official slot alone

Produce fails alone for all sixteen. Koide is one 1981
relation. \(G\) and \(\ell_P\) are one leftover. The
manifold \(\lambda_1/\lambda_2=\cos\theta_W\) test already
failed in the app. Isolation does not turn 16/16 into \(F\).

Stay on the GQ leftovers. Do not re-bundle this into a
sixteen-slogan. Official table:
[`docs/COSMO-SIXTEEN.md`](COSMO-SIXTEEN.md).
