# The leftover object (app)

10 September 2026. Phone lock. **Not a proof.
NS not solved.**

You asked for pictures and an app that shows
the object. The object is the boxed Lemma★
shape, not a close.

Folder to release:
`apps/the-object/`

Run:
`python3 -m http.server 8765 --directory apps/the-object`

Stills:
`results/the_object/`
`apps/the-object/stills/`

Builder:
`scripts/ns_attacks/build_object_gallery.py`

The page says OPEN. It does not announce a
solve. A finite sample is not \(C_0\). Stay
in this chat.
