# Sign gate — first variation

24 September 2026.
**The gate is \(L_{1,N}\).
Do not substitute \(T^{(0)}\)
into the finite-gap identity.
No sign verdict sits.
Not a close. ★ stays killed.
Catalog B open stays 1.**

Lemma A (unaltered):
[`PRESS.md`](PRESS.md).
SBP identity:
[`SBP.md`](SBP.md).
Triangles:
[`FOURIER-TRIANGLE.md`](FOURIER-TRIANGLE.md).
Incoming ledger:
[`CENTERED-LEDGER.md`](CENTERED-LEDGER.md).
Relative width:
[`WIDTH.md`](WIDTH.md).
Target:
[`CENTERED-DRIFT.md`](CENTERED-DRIFT.md).
Machine: `python3 scripts/sign_gate.py`.
Does not overwrite `stokes_moments.py`.
Do not start leftover 1.
Do not weld \(\star\).
Do not cash B★ as G5.
Do not seat JGC.
Do not invent a bridge.
Do not run Taylor–Green.
Do not run a Heavy search
on this desk.
Do not mix scalene
homochiral loops into this
heterochiral sign question.
Do not alter Lemma A.
No more potentials.
No more reinterpretation
of the same clock.

---

## Three kinds of sentence

**Exact.**
\(T_c=\Lambda\langle\delta,T\rangle+\sum\delta_m^2 T_m\).
\(R_{2,N}\) is higher order when
\(T=T^{(0)}\) or
\(T-T^{(0)}=O(\mathrm{gap})\).

**Named comparison.**
The gate \(L_{1,N}\).
The four outcome buckets.
\(\Theta_N=\nu\kappa_N^2\tau_{U,N}\).
The seed schema \(\mathcal A_N^{+}\).

**Not on this desk.**
A Heavy print. BOTH SIGNS.
ONE SIGN as a seated
\(I_3\) bridge. Vandermonde
as a remainder. Lemma B.

---

## Exact expansion — do not
alter the gate

Sitting
\(T_c=\sum_k\lambda_k(\lambda_k-\Lambda)T_k\).
Set \(\delta_k=\lambda_k-\Lambda\).
Then, exactly,

\[
T_c
=
\Lambda\langle\delta,T\rangle
+
\sum_m\delta_m^2 T_m.
\]

Locked today on the two-shell
gap formula. The \(T_m\) on
the right is the transfer of
the **actual** neighboring
state. It varies with the
deformation.

The first-variation object,
on an asymptotic
neighboring-shell family, is

\[
L_{1,N}
=
\Lambda_N\langle\delta_N,T_N^{(0)}\rangle.
\]

Do not alter that gate.
Transfer variation is the
next order. Heavy keeps the
actual neighboring \(T_m\)
as a remainder check. Do
not silently put \(T^{(0)}\)
into the finite-gap identity.

---

## Remainder check

\[
R_{2,N}
:=
T_{c,N}
-
\Lambda_N\langle\delta_N,T_N^{(0)}\rangle.
\]

If \(T=T^{(0)}\), then
\(R_{2,N}=\sum\delta_m^2 T_m\),
quadratic in the gap.

If \(T-T^{(0)}=O(\mathrm{gap})\),
\(R_{2,N}\) stays higher order.

If the deformation changes
polarization, amplitudes, or
topology by \(O(1)\), then
\(R_{2,N}\) is only first order
in the gap. That sequence is
not the intended gate.

Locked today on a shrinking
two-shell family.

---

## Outcome tree — named,
not printed

Four buckets. No fifth.

**BOTH SIGNS**, heterochiral,
\(\lvert\rho_N^\pm\rvert\ge c>0\),
persistent.

Then universal first-order
one-sided narrow depletion
is **false**. Stop static
closure on this question.
The positive branch goes to
evolution. Then

\[
\Theta_N=\nu\kappa_N^2\tau_{U,N}
\]

is a named number, not
computed here.

Save the actual integer
vectors, helicity labels,
polarizations, and
amplitudes — not merely
\(\delta\) and \(T^{(0)}\).
That field is the seed

\[
\mathcal A_N^{+}.
\]

Do not invent it. Heavy
has not printed.

**ZERO ONLY** in homochiral
families.

Consistent with a
Vandermonde delay. That
does not rescue the full
NSE. Heterochiral stays
separate. Cubic depletion
is **not locked** here.
[`WIDTH.md`](WIDTH.md).

**ONE SIGN** heterochirally.

Then ask whether the
restriction is a Gram /
lattice realization
problem. That would be the
first serious bridge from
\(I_3\) arithmetic into
centered dynamics, not
merely occupancy.
[`FOURIER-TRIANGLE.md`](FOURIER-TRIANGLE.md).
It is **not seated**.
Do not invent the bridge.

**NO NEIGHBOR** stays its
own category.

Arithmetic rigidity is not
sign depletion. Do not
glue the two.

---

## Two frontiers

**Static frontier.**
Arithmetic sign
realizability. This gate.

**Dynamic frontier.**
Dangerous-state
persistence. Only after
BOTH SIGNS, and only as
evolution. The NSE gets
the next move.

No more potentials.
No more clocks under a
new name. Lemma A stays
as written.
[`PRESS.md`](PRESS.md).
Lemma B stays OPEN.

---

## What this page is not

- A close. Catalog B open
  stays 1.
- A seating of DA-NS-2.
- A restoration of ★.
- A seating of B★, LX,
  SAG, JGC, or Lemma B.
- A named death of G4.
- A Heavy search.
- A sign verdict.
- An \(I_3\) bridge.
- Leftover 1. Do not
  start H1.
- A Taylor–Green run.
- [`PATH-TO-CLOSE.md`](PATH-TO-CLOSE.md).

---

## Lock

The gate is \(L_{1,N}\). Do not put \(T^{(0)}\) in the finite-gap identity.
No sign verdict sits.
G4 stays OPEN. ★ stays killed.
NS not solved.
