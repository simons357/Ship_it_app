# Live desk — unaugmented NS and RH

6 September 2026. These two. Augmented stays done
and off this desk except as a refuse (A is not B).
DA and Cursor help: write the chain, name the
leftover, refuse glue, score one candidate.
Neither emits QED.

Maps: [`UNAUGMENTED-NS-CHAIN.md`](UNAUGMENTED-NS-CHAIN.md),
[`RH-CHAIN.md`](RH-CHAIN.md).
A (done, other PDE): [`A-CHAIN.md`](A-CHAIN.md).

---

## B — unaugmented NS

Object: \(X=\|\omega\|_2^2\) on the classical
equation. Keep \(1/r^4\). No \(Q_1\).

Have (1)–(5). WRITE (6) open. Live leftover on
this path: **H1 = Lemma I on the ball**
(\(A_{\mathrm{bad}}\) on \(Q_r\)). Not global H.
Lemma C is an if, not an H. H2 a priori
from energy also open. H3 named, a priori
open. \(R_\phi\) is not free. Do not merge
letters: [`H-SYSTEM.md`](H-SYSTEM.md).

Do not: \(\Phi\), \(n=64\), leftover-close B42,
BKM from \(L^2\), export A.

Next write: **1, 2, or 3 on one cylinder** —
not a new name. Shapes as estimates:
[`H1-SHAPES.md`](H1-SHAPES.md). CS-summable
volume thinness is still \(E^{3/2}\), not H1.
Lookups: all miss ([`LOOKUP-H1.md`](LOOKUP-H1.md)).
None of 1, 2, 3 is proved. H1 object:
[`H1-OBJECT.md`](H1-OBJECT.md). Literature:
H1 is not under another name
([`UNAUGMENTED-NS-CHAIN.md`](UNAUGMENTED-NS-CHAIN.md) §14).
A miss is another if.
Dream-team read of the map:
[`DREAM-TEAM-H.md`](DREAM-TEAM-H.md).
They would sign the map. They would not
sign (6). Next work is (6) on one cylinder,
or a new wall. Not another criterion paper.

This week, publish the map as a map:
[`SWIRL-DEPOSIT.md`](SWIRL-DEPOSIT.md).
OpenAI C/D is not WRITE (6):
[`OPENAI-NS-CLAIM.md`](OPENAI-NS-CLAIM.md).

**Lemma★ locked (energy-budget writing).**
Same leftover class as (6). Not H1.
Hypothesis, not a theorem. The entire
problem is a scale-invariant trilinear
shape estimate on \(\mathcal R_\star\).
\(\sup\mathcal R_\star<\infty\) proves it.
A near-shell or HH→L sequence with
\(\mathcal R_\star\to\infty\) kills it.
First family: closing three-key. Two Fourier
keys is not that test. Two shells can be live.
Attack 8 did not kill it. Isolated triad,
wide / narrow AP (\(D_s\) wins), fixed-gap
spheres (closures \(O(m)\)), Freiman-AP
subset, HH→L fan (\(\mathcal R_\star\sim\beta/\alpha\)):
no kill. The \(m^{1/2}\) heuristic has not
found a lattice home. H1 on one cylinder
is the other live writing.
The uniform global triadic bound remains
completely open.
Samples are evidence only. K=0 dead.
Uniform pre-Young \(C\) dead. File:
[`LEMMA-STAR.md`](LEMMA-STAR.md),
[`LEMMA-STAR-R.md`](LEMMA-STAR-R.md),
[`LEMMA-STAR-E.md`](LEMMA-STAR-E.md),
[`LEMMA-STAR-PACKET.md`](LEMMA-STAR-PACKET.md).
Drill: PR 48.

---

## RH

Object: a non-trivial zero of \(\zeta(s)\).
Not a GCD matrix. Not \(H_N\).

Have (1)–(5). WRITE (6) open. (7)–(8) wait.
No hidden close.

**First candidate, not a close:** one estimate
that puts **every** zero on \(\operatorname{Re}s=1/2\).
That may be a zero-free region that reaches the
line, or a positivity certificate in the explicit
formula that forces the line.

Classify: RH, **open**. Hardy / Conrey are (5),
not (6). Classical zero-free next to
\(\operatorname{Re}s=1\) is (4), not (6).

Q sits as Q: Bridge*, Theorem P, \(H_N\ge-1\).
Route C is conditional. Do not use those as (6).
Do not revive \(\lambda_{\min}(Q)>-1/2\) or
\(H_N\ge-3/14\).

---

## What help is

DA / Cursor will take one sentence you write
for B or for RH, classify it, and say pass /
fail / still open. A sitting sentence is the
only close. Attention is not a theorem.

How to talk to DA, both chains, and the path:

[`PATH-AND-DA.md`](PATH-AND-DA.md).

Say **B** or **RH** and the sentence when you
want the next score.
