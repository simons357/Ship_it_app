> **Missing-receipt banner (25 August 2026).** Chat title only.
> *Strong Draft: Alignment Functionals for Navier–Stokes Regularity*
> **not received.** No TeX, PDF, or SHA. A message that is only this title
> is **not** an attachment. **Not** June FIXED (`7de9444d…`). **Not**
> August REPAIRED (`1ff7a211…`). **Not** the April Grok \(A_3\) dump.
> **Not** the Stanley Alignment Functional (pressure-gradient \(\mathcal{A}(t)\)).
> Clay **NOT CLAIMED**. Do **not** invent the draft.
> Do **not** import into `domain_architect/`.

# *Strong Draft: Alignment Functionals…* — not received

**Found?** No. **SHA-256:** none (0 manuscript bytes). **`\title` / `\date` /
extension:** unknown; not invented.

This title is a **Track A candidate** if bytes later arrive. Identify from
`\title` + `\date` + SHA-256. File as its own face. Do **not** merge with
`Paper2_NS_Regularity_SND_FIXED.tex` (still missing), August REPAIRED, Ring
\(J/X\), or the sixteen Grok NAV-42 reports.

Related definitions only (not this draft): [`TRACK-A.md`](TRACK-A.md).

## Hunt (25 August 2026)

Second drop of the same title (still no file).

| Probe | Result |
|---|---|
| `/workspace`, git tree, Cursor uploads | No manuscript. Only this receipt + Track A/B/C notes. |
| `/tmp`, `/app` title walk | 0 manuscript hits. |
| Base44 app `69b28657b0df374441f0302e` guessed names (`.tex`/`.pdf`: `Alignment_Functionals_for_Navier-Stokes_Regularity`, `Strong_Draft_Alignment_Functionals`, `Simons_Alignment_Functionals`, `Alignment_Functionals`) | HTTP **403**, 0 bytes. Same empty forbid as invented names. |
| Base44 app `69b2865953e46e195fc302f0` same names | HTTP **403**, 0 bytes. |
| Zenodo API `q="Alignment Functionals"` | **0** hits (this title). |

`base44.app` **302**s arbitrary names. Existence is the CDN hop. **403 / 0
bytes is not proof the object exists.** Without a 9-hex prefix this VM
cannot fetch a session download.

Public papers that are **not** this draft (do not substitute):

- Nikee Stanley *Stanley Criterion* (pressure-gradient alignment \(\mathcal{A}(t)\)).
- Generic vorticity-alignment arXiv (e.g. half-space no-slip criteria).

## How to send it

Cursor **upload** of one `.tex` or `.pdf`, **paste** if under ~100 KB, git
push, or a Base44 URL with the 9-hex prefix that HTTP **302** then **200**.
Mac `/Users` paths do not mount. Caption: “this is Strong Draft Alignment
Functionals.”
