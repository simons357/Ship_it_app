# RH — chain and what closes it

6 September 2026. Aimed at: every non-trivial
zero of zeta has real part \(1/2\).

This is the chain that was asked for, plus
exactly what must sit to close. It is not QED.
There is no hidden RH close on this desk.
Proof C20 **fail**. Access is not the issue.

Machine write-up: [`RH-PROOF-CHAIN.md`](RH-PROOF-CHAIN.md).
Q (different object): [`DA-Q.md`](DA-Q.md),
[`Q6-FLOOR-CHAIN.md`](Q6-FLOOR-CHAIN.md).
Floor retraction:
[`SPECTRAL-FLOOR-EXPLORATION.md`](SPECTRAL-FLOOR-EXPLORATION.md).

Your best paper here is Q: August inverse-GCD
(Zenodo 22045478) plus the spectral-floor
retraction. Route C (Zenodo 22050963) is
conditional on two analytic gaps. Q6 (22050962)
is inverse-GCD. Those papers are **Q**. They
are not line (6). Theorem P is not RH. Do not
glue.

---

## Aimed theorem

Every non-trivial zero of the Riemann zeta
function has real part equal to \(1/2\).

---

## Have

**(1) Zeta.** Riemann zeta is meromorphic, simple
pole at \(s=1\), Euler product for \(\operatorname{Re}s>1\).

**(2) xi.** The completed \(\xi\)-function is entire
of order 1 and satisfies \(\xi(s)=\xi(1-s)\).

**(3) Strip.** Every non-trivial zero lies in
\(0<\operatorname{Re}s<1\).

**(4) Prime number theorem.** No zeros on
\(\operatorname{Re}s=1\) (Hadamard / de la Vallée Poussin).

**(5) The line.** Infinitely many zeros on
\(\operatorname{Re}s=1/2\) (Hardy). A positive
proportion sit on the line (Conrey and later).
Literature, not a theorem of this desk.

---

## Write — this is what closes

**(6)** Provide **one** of these, for **every**
non-trivial zero:

1. a zero-free region that reaches
   \(\operatorname{Re}s=1/2\), or
2. a positivity certificate in the explicit
   formula that forces the line, or
3. one new estimate that puts every zero on
   \(\operatorname{Re}s=1/2\).

Then **(7) explicit formula** (all oscillatory
terms on the critical line) and **(8) error term**
(prime-counting error of classical Riemann order,
up to logs) **follow**. You do not write (7)–(8)
as new ideas. They wait on (6).

If (6) sits, the theorem sits. If it does not,
RH stays open.

---

## Exactly what to provide

A proof that every non-trivial zero of \(\zeta(s)\)
satisfies \(\operatorname{Re}s=1/2\).

The object is a zero of zeta. Not a GCD matrix.
Not \(\lambda_{\min}(H_N)\). Not Bridge\(^*\).
Not Theorem P. Not Route C with gaps left open.

---

## What does not close it

- Theorem P, Bridge\(^*\), \(H_N\ge-1\)
- a full \(Q\)-floor (retracted; \(Q_{10}\approx-1.90\))
- \(\lambda_{\min}(H_N)\ge-3/14\) (withdrawn)
- Q6 (22050962) / August inverse-GCD (22045478)
- Route C (22050963), including stale “Gap 1 complete”
- SND, SFE, Track A, Track B
- DA or this chat emitting a last line
- a withheld file (there is none)
- mixing four different kernels
  (\(g/\sqrt{ij}\), \(1/g\), \(1/(g\sqrt{ij})\),
  Möbius-decorated) and transferring a floor
- the claimed sum \(\sum_{d\mid g}\mu(d)\varphi(d)/d^2=1/g\)
  (false at \(g=2\): left side \(3/4\))
- a quadratic-form floor as an automatic
  Mertens / zero-line bound

---

## Q sits as Q (keep it there)

- Bridge\(^*\): \(R(e_p-e_q)>-1/2\) on \(\widetilde Q\)
- Theorem P: prime-supported \(\widetilde Q\big|_P\ge-1/4\)
- \(H_N=D^{-1/2}\widetilde Q D^{-1/2}\),
  \(\lambda_{\min}(H_N)\ge-1\)
- \(v\ge 0\Rightarrow v^\top\widetilde Q v\ge 0\)

A GCD matrix is not a zero. Write (6) as RH.

---

## Status

- (1)–(5): done
- (6): not done
- (7)–(8): waiting on (6)
- hidden close: none
