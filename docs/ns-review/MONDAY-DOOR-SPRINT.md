# Monday door sprint — knock harder, don’t open

**Deal (locked):** we are knocking on the door; we are not opening it.  
**Deadline:** Monday visual / specialist handoff.  
**Trophy lines:** forbidden (`solved`, `imminent breakthrough`, `Clay closed`).

---

## Where the door is

Main trunk open node (§5 of `PROOF-CHAIN-CLEAN.md`):

\[
|T_c|
\;\lesssim\;
\|v\|_2\, X^{3/2}
\quad\text{or equivalent geometry-only product control}
\]

that feeds the Young form of Lemma★. Ordinary energy-only 3D products do not give it. Five-lane / Bony diagnostics flag the **HH input channel** as the live bottleneck.

Φ-branch door (separate book): \(\int \|u^r/r\|_\infty\,dt\) uniform — not this sprint’s primary hinge.

---

## Sprint goals (through Monday)

| # | Goal | Success looks like |
| --- | --- | --- |
| 1 | **Measure distance** | Probe tables for \(\|T_c\|/(\|v\|_2 X^{3/2})\), \(R_\star\), HH-channel share on structured families |
| 2 | **Attack memo** | Short list of analytic moves (cancellations in \(T_c=M-\Lambda N\), HH restriction, near-shell) with status TRY / BLOCKED |
| 3 | **Push one move** | Either a partial reduction (conditional bound under a stated hypothesis) or a clean numeric ceiling on a named family |
| 4 | **Public face** | Journey/chain still shows open node as math; Monday posts use knocking language |

Not a goal: announcing the door opened.

---

## Attack routes (ordered)

1. **Structure on \(T_c\)** — write \(T_c\) as a divergence / canceling triad form; hunt exact identities that remove the worst HH piece.  
2. **HH-channel budget** — bound only the HH input contribution to \(T_c\); HL/LL by classical products.  
3. **Near-shell / two-shell families** — exact \(K_{\alpha\beta}\) probes (already in five-lane); use as laboratory, not as full proof.  
4. **Conditional close** — Lemma★ under an explicit HH or SND hypothesis (honest conditional paper, not Clay).  
5. **Negative control** — if a family drives \(R_\star\to\infty\), ★ packaging dies; record and stop.

---

## Commands

```bash
python3 scripts/ns_attacks/product_bound_probe.py
python3 scripts/ns_attacks/run_all_five.py   # if deps resolve
```

Artifacts: `/opt/cursor/artifacts/monday-door-sprint/`

---

## Exit Monday

Whatever we gain goes into the click-through chain as **sharper open-node math**. Bow-out post still available. Specialists get a better door description — not a fake key.
