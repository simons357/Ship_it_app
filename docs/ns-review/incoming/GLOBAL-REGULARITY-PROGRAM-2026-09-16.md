# Incoming — Navier–Stokes Global Regularity Program

**Incoming paste. 16 September 2026.
Not the tape. Not a theorem.
Ordinary NS is not solved.**

This is the ChatGPT-shaped write-up
Jonathan pasted into the Cursor agent
chat. Superscripts were flattened in
the phone paste (`22j` means
\(2^{2j}\)). Do not treat this file as
[`YES-NO-OPEN.md`](../YES-NO-OPEN.md).

Scored against the tape:
[`REPORT-AUDIT.md`](../REPORT-AUDIT.md) §8.
Who said what:
[`INCOMING-WRITEUPS.md`](../INCOMING-WRITEUPS.md).

---

Status Report — Classical (Unaugmented) Incompressible Equation, \(\mathbb{R}^3/\mathbb{T}^3\).

Prepared for: Jonathan R. Simons.
Scope: classical unaugmented 3-D
incompressible Navier–Stokes only.
No modified operator, no auxiliary
field. Clay Statement B **object**,
not a claim that B sits.

## 1. Executive summary (as sent)

Global regularity remains open.
Nothing in that cycle closed it.
What it claimed instead: displayed
Theorem H withdrawn; a conditional
piece derived; one candidate
mechanism tested for circularity and
said to have died cheaply; exact-shell
track CLAIMED; the leftover named as
control of \(\|\nabla u\|_\infty\) (or
BKM-type) from \(X\le M\) or from the
dynamics without assuming it.

Honest one-line from that write-up:
we know what is missing, it is hard,
one shortcut was ruled out.

**Tape:** OPEN as leftover. Not a
close. The “corrected peak-fraction
floor” in §2 of that write-up is
**not** unconditional.

## 2. What that write-up listed as proved

As sent (then scored):

- Exact shell budget
  \(\tfrac12\dot X_j+\widetilde D_j=-2^{2j}F_j\).
  **Tape: identity KEEP.**
- Commutator \(F_j=\langle[\Delta_j,u\cdot\nabla]u,\Delta_j u\rangle\).
  **Tape: identity KEEP.**
  Indexed Bony is still what a
  \(\rho\)-floor would need. This desk
  is not writing that floor as
  primary.
- Conditional \(A.2\): for \(X\le M\),
  \(\lvert F_j\rvert\le C\sqrt{M/(\nu\lambda_1)}\,X^{1/2}\mathcal D^{1/2}\).
  **Tape: sits.** Foundation is Hölder
  \((6,2,3)\) + Poincaré, not
  Kato–Ponce.
- “Corrected peak-fraction floor”
  \(D^-\rho\ge -A(t)\rho\) under
  \(\Gamma=\|\nabla u\|_\infty\) and a
  bound on \(j_*\).
  **Tape: NOT unconditional. BKM-adjacent.
  Not seated as a floor on this desk.
  \(A.3\) is a ceiling.**

## 3. Claimed, pending

Exact-shell \(K\le 16/9\) **CLAIMED**,
not proved. Firewall: exact-shell
\(\neq\) unrestricted estimate
\(\neq\) regularity. **Tape: agree.
Freeze sweeps.**

Kato–Ponce citation check. **Tape:
cheap, not primary. \(A.2\) does not
wait on it.**

## 4. What that write-up said has died

| Claim | Tape |
|---|---|
| Theorem H (original) | **DEAD.** Agree. |
| Universal \(M\)-free nonlinear bound | **DEAD.** Agree. |
| Universal \(t=0\) SND floor | **DEAD.** Agree. |
| **C10 (Cursor track)** | **NO as a program death.** (A) not seated. Leftover 5 stays OPEN. Route A is write-or-kill. |
| Geometric-tail / BKM as primary | **Demoted.** Agree as primary. Useful conditional only. |

## 5–7. Pathway and ceiling (as sent, then ordered)

That write-up’s order: Kato–Ponce;
sync a lower-Dini \(\rho\)-floor;
close 9D; next depletion candidate;
decide on centered drift; keep
\(\Gamma\)-conditional; hunt a
different quantity than \(\rho\).

**This desk’s order**
([`MASTER-PLAN.md`](../MASTER-PLAN.md),
[`UNAUG-NS-UNIFIED.md`](../UNAUG-NS-UNIFIED.md)):

1. Freeze H, 9D sweeps, \(\rho\)-floor
   as primary.
2. Route A: dynamical \(a_+\) without
   \(H^1/L^\infty/\)BKM, or name the
   death.
3. Route B: useful \(K\) for centered
   \(T_c\), independent.
4. SND instrument. Exact-shell stays
   CLAIMED. H1 not started from here.

Honest ceiling in that write-up is
right as a public sentence: not near
proof. Cover sheets should say
active program, conditional partials,
catalog of kills — not near proof.

---

End of incoming extract.
NS not solved.
