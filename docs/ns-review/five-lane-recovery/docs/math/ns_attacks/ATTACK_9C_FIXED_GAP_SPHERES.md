# ATTACK 9C — Fixed-gap spheres \(n\) and \(n+d\): natural same-shell ensemble (not a kill)

**Date:** 2026-09-10  
**Branch:** `cursor/ns-five-lane-lemma-star-1390`  
**Status:** **Did not kill ★.** Natural same-shell ensemble is **NOT** a kill. Kill lane remains **LIVE**. **NS not solved.**  
**Prior:** Attack 9B (exact-shell \(K_{\alpha,\beta}\)) — finite sample \(\max K\approx0.641\), not a kill — [`ATTACK_9B_EXACT_SHELL_CLOSING.md`](./ATTACK_9B_EXACT_SHELL_CLOSING.md).

## Family

Two Stokes eigen-spheres separated by a **fixed gap** \(d\), at shell indices \(n\) and \(n+d\):
\[
\operatorname{supp}\widehat v\subset\{k:\lambda_k=n\}\cup\{k:\lambda_k=n+d\}.
\]
Spectral variance \(\mathcal{D}_s\) is generated by the **gap** (the fixed \(|\alpha-\beta|=d\)), **not** by packet width / mode count \(m\). Closures available to the natural ensemble are only \(O(m)\) (linear in the number of modes on the shells), not a designed \(\Theta(m^2)\) closing set.

## Boxed observation (SoT lock)

| Quantity | Observation |
|----------|-------------|
| \(\mathcal{D}_s\) source | **Gap** \(d\), not packet width |
| Natural closures | Only \(O(m)\) |
| \(\mathcal{R}_\star\) vs \(n\) | **Falls** with \(n\): \(\approx 0.11\to 0.031\) |
| vs \(m^{1/2}\) | Does **NOT** track \(m^{1/2}\) |
| Verdict | Natural same-shell ensemble is **NOT** a kill of ★ |

## Why this is not a kill

A live kill needs \(\mathcal{R}_\star\to\infty\) (or unbounded \(K_{\alpha,\beta}\)) on a smooth family. Here \(\mathcal{R}_\star\) **decreases** as the shell index \(n\) grows (reported span \(0.11\to 0.031\)). The natural same-shell ensemble therefore fails the divergence criterion.

The structural reason is consistent with 9A/9B caveats: with \(\mathcal{D}_s\) fixed by the gap and only \(O(m)\) natural closures, stretching \((T_c)_+\) does not outrun the denominator \(\mathcal{D}_s\|v\|_2^2 Y\) enough to produce growth in \(\mathcal{R}_\star\). In particular, \(\mathcal{R}_\star\) does **not** scale like \(m^{1/2}\) on this ensemble.

## Probe / script status

**SoT-only until implemented.**

| Inventory check | Result |
|-----------------|--------|
| Dedicated 9C probe under `scripts/ns_attacks/` | **Absent** (contrast `attack9b_exact_shell_K.py`) |
| Sweep script in PR #48 | **None** |
| Sweep data / JSON in PR #48 recovered pack | **None** |
| Recorded span \(0.11\to 0.031\) | Locked as user/runtime truth into this SoT only |

Do **not** invent a sweep artifact. A future `attack9c_fixed_gap_spheres.py` (name TBD) should reproduce the span under the same controls as 9A/9B (amplitude invariance of \(\mathcal{R}_\star\) via `ratio_R_star_shape`, complete signed \(T_c\), report total \(T_c\) not HH→L-only).

See bookkeeping: [`FIVE-LANE-BOOKKEEPING.md`](../../../../FIVE-LANE-BOOKKEEPING.md).

## Decisive output

| Outcome | Meaning |
|---------|---------|
| \(\mathcal{R}_\star\) falls with \(n\) (\(0.11\to 0.031\)) | **Not a kill** |
| \(\mathcal{R}_\star\sim m^{1/2}\) on natural ensemble | **FALSE** for this family |
| Kill lane closed | **FALSE** — numeric non-kill ≠ closed |
| Next falsifier | Designed \(\Theta(m^2)\)-closure subset with **locked phases** — **Attack 9D** |

## Verdict

| Claim | Verdict |
|-------|---------|
| Attack 9C killed Lemma★ | **FALSE** |
| Natural same-shell / fixed-gap ensemble is a kill | **FALSE** |
| \(\mathcal{D}_s\) from packet width (not gap) | **FALSE** for this family |
| Kill lane closed | **FALSE** — kill lane **LIVE** |

**NS not solved.** Lemma★ **OPEN**. Kill lane **LIVE**.

## Next

[`ATTACK_9D_THETA_M2_LOCKED_PHASE.md`](./ATTACK_9D_THETA_M2_LOCKED_PHASE.md) — designed \(\Theta(m^2)\)-closure subset with locked phases (remaining packet falsifier).
