# Static frontier — arithmetic sign realizability

24 September 2026.
**This gate is frozen. Do not
alter it. Do not alter Lemma A.
No new potential. No new clock.
Not a close. NS not solved.**

Lemma A stays exactly as
seated:
[`LEMMA-A-SBP.md`](LEMMA-A-SBP.md).
SBP stays:
[`CORE-TAIL-SBP.md`](CORE-TAIL-SBP.md).
I₃ arithmetic parent:
[`FOURIER-TRIANGLE.md`](FOURIER-TRIANGLE.md).

Code:
`scripts/ns_attacks/first_variation_gate.py`.

---

## Exact neighboring expansion

\(\delta_m=\lambda_m-\Lambda\),
\(\sum T_m=0\),

\[
\mathfrak T_c
=\sum_m\lambda_m(\lambda_m-\Lambda)T_m
=\Lambda\langle\delta,T\rangle
+\sum_m\delta_m^2 T_m.
\]

That identity uses the **live**
\(T_m\) of the deformed state.

---

## Notation safeguard — do not mix orders

In the deformed state, \(T_m\)
itself varies. For the first
variation of an **asymptotic
neighboring-shell** family the
transfer variation is next
order. The first-variation
object is

\[
\boxed{
L_{1,N}
=\Lambda_N\langle\delta_N,T_N^{(0)}\rangle.
}
\]

Do **not** substitute
\(T^{(0)}\) into the full
finite-gap identity and call
it exact.

Heavy keeps the actual
neighboring \(T_m\) as a
remainder check:

\[
\boxed{
R_{2,N}
:=
\mathfrak T_{c,N}
-\Lambda_N\langle\delta_N,T_N^{(0)}\rangle.
}
\]

A legitimate first-variation
sequence has the predicted
higher-order \(R_{2,N}\).
If not, the deformation
changed polarization,
amplitudes, or topology too
strongly to represent this
gate.

---

## Outcome tree

Heterochiral, persistent,
with a neighbor:

If Heavy gets **BOTH SIGNS**
and \(\lvert\rho_N^\pm\rvert\ge c>0\):

\[
\boxed{
\text{universal first-order one-sided narrow depletion is false}.
}
\]

Stop static closure on this
question. The positive branch
goes to evolution. Then

\[
\Theta_N=\nu\kappa_N^2\tau_{U,N}
\]

is one of the most important
numbers in the project.

If Heavy gets systematic
**ZERO ONLY** on **homochiral**
families: consistent with the
Vandermonde delay. It does
not rescue NSE. Heterochiral
stays separate.

If Heavy gets **ONE SIGN**
heterochirally: determine
whether the restriction is
the Gram / lattice realization
problem. That is the first
serious bridge from I₃
arithmetic into the centered
dynamics, not merely into
occupancy.

**NO NEIGHBOR** is its own
category. Arithmetic rigidity
is not sign depletion.

---

## Seed

If BOTH SIGNS appears, save
the actual integer vectors,
helicity labels, polarizations,
and amplitudes — not merely
\(\delta\) and \(T^{(0)}\).

\[
\boxed{\mathcal A_N^{+}}
\]

is the canonical adversarial
seed for the dynamic phase:
an exact, reproducible,
increasingly narrow field
statically pointed in the
dangerous direction. Then
NSE gets the next move.

---

## Frozen board

\[
\boxed{\textbf{STATIC FRONTIER: arithmetic sign realizability}}
\]

\[
\Downarrow
\]

\[
\boxed{\textbf{DYNAMIC FRONTIER: dangerous-state persistence}}
\]

No more potentials.
No more reinterpretation of
the same clock.
Run the gate.

---

## Lock

Finite-gap split EXACT with
live \(T\). \(L_{1,N}\) uses
\(T^{(0)}\) only.
\(R_{2,N}\) is the remainder
check. Four categories only.
Lemma A unaltered.
Lemma B still OPEN on the
capacity desk, and is **not**
this gate.
Not DA-NS-2. NS not solved.
