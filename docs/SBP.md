# Summation by parts for \(\Phi_e\)

24 September 2026.
**The SBP identity is not a useful K.
It moves the tail. It does not pay it.
Not a close. ★ stays killed.
Catalog B open stays 1.**

Incoming ledger:
[`CENTERED-LEDGER.md`](CENTERED-LEDGER.md).
Chart reset:
[`RESET.md`](RESET.md).
Relative width:
[`WIDTH.md`](WIDTH.md).
Overnight page:
[`DA-NS-2.md`](DA-NS-2.md).
Target:
[`CENTERED-DRIFT.md`](CENTERED-DRIFT.md).
Press (Lemma A sits;
Lemma B OPEN):
[`PRESS.md`](PRESS.md).
Sign gate:
[`SIGN-GATE.md`](SIGN-GATE.md).
Machine: `python3 scripts/sbp.py`.
Does not overwrite `stokes_moments.py`.
Do not start leftover 1.
Do not weld \(\star\).
Do not cash B★ as G5.
Do not seat JGC.
Do not invent a bridge.
Do not run Taylor–Green.
The gate file
`packets/DA-GATE-RMS-CORE-TAIL-SBP-2026-09-24.md`
is not in this repo.

---

## Three kinds of sentence

**Exact.** \(\varphi_e\) expansion.
\(\varphi_e\ge 0\), zero at the
shell. CS \(\Lambda^2 E\ge Y\).
The rewrite
\(T_c=(d/dt)_{\mathrm{NL}}\Phi_e
+2\kappa_e^3 Q_a-(\Lambda-\lambda_e)N\)
**if**
\((d/dt)_{\mathrm{NL}}H_{1/2}=2Q_a\).
\(\nabla R\) at the core by
finite difference.

**Named comparison.**
Frozen \(\lambda_e=\kappa_e^2\)
versus live \(\kappa(t)=\sqrt{\Lambda}\).
Pick frozen. Do not mix.

**Not on this desk.**
The gate file. Chebyshev tail
and the exact factor of two.
The \(\lvert m\rvert\) flux
normalization. Taylor–Green
\(\Phi_e/Y\). \(\mathsf B^{\mathrm{prim}}\).
MASTER dossier.

---

## Weight — EXACT

Fix an epoch center \(\kappa_e>0\)
and \(\lambda_e=\kappa_e^2\).
\(\Lambda\) is a convex combination
of \(\lvert k\rvert^2\). Do not
read \(\Lambda\) as \(\lvert k\rvert\).

\[
\varphi_e(m)
=
\tfrac12(m-\kappa_e)^2
(m^2+2\kappa_e m+2\kappa_e^2)
=
\tfrac12 m^4
-\tfrac12\lambda_e m^2
-\kappa_e^3 m
+\lambda_e^2.
\]

The quadratic factor is
\((m+\kappa_e)^2+\kappa_e^2\ge\kappa_e^2>0\).
So \(\varphi_e\ge 0\), and
\(\varphi_e(\kappa_e)=0\) to
second order.
\(\varphi_e(0)=\kappa_e^4\).
At large \(m\), \(\varphi_e\sim m^4/2\).

Capacity

\[
\Phi_e
=
\sum_k\varphi_e(\lvert k\rvert)\lvert a_k\rvert^2
=
\tfrac12 Y
-\tfrac12\lambda_e X
-\kappa_e^3 H_{1/2}
+\lambda_e^2 E.
\]

Locked today.

---

## The rewrite — IF the flux

Nonlinear parts already sit:
\(E'=0\), \(X'=2N\), \(Y'=2M\).
If the incoming \(\lvert m\rvert\)
normalization holds,

\[
\Bigl(\frac{d}{dt}\Bigr)_{\mathrm{NL}}
H_{1/2}
=
2Q_a,
\]

then

\[
\Bigl(\frac{d}{dt}\Bigr)_{\mathrm{NL}}
\Phi_e
=
M-\lambda_e N-2\kappa_e^3 Q_a,
\]

and therefore, exactly,

\[
T_c
=
M-\Lambda N
=
\Bigl(\frac{d}{dt}\Bigr)_{\mathrm{NL}}
\Phi_e
+2\kappa_e^3 Q_a
-(\Lambda-\lambda_e)N.
\]

That is an elementary rewrite.
It is not a remainder and not
a useful \(K\).

The order-\(r\) multiplier
variation lands on \(\Phi_e'\),
not on \(\sum\lvert Q\rvert\).
That is the bridge they named,
stamped as an identity only.

This desk does **not** reproduce
Heavy’s \(\lvert m\rvert\) flux
\(2o(i-j)\) on heterochiral
triads / zero on homochiral
ones, nor
\(Q_a=\tfrac12(d/dt)_{\mathrm{NL}}
\|u\|^2_{\dot H^{1/2}}\).
Without that normalization the
rewrite stays conditional.
The gate file is not here.
Do not stamp the flux as the
dossier’s cross-radius helicity
primitive.

Claimed on their network, not
reproduced:
\(M^{\mathrm{het}}=\sum AHQ\),
\(N^{\mathrm{het}}=\sum AQ\).
The identity as written uses
full \(N\), not \(S_\Gamma\).
\(N\) is not \(S_\Gamma\).
\(\Phi_e\) is not
\(\Psi_\lambda^\varepsilon\).
Do not invent a bridge.

---

## One convention

**Frozen.** Epoch
\(\lambda_e=\kappa_e^2\) fixed.
The core is centered at
\(\kappa_e\). The moving term
\(-(\Lambda-\lambda_e)N\) stays.
\(2\kappa_e^3 Q_a\) can
telescope against a frozen
charge if the flux sits.

**Live.** \(\lambda_e=\Lambda(t)\).
The moving term vanishes.
\(2\kappa(t)^3 Q_a\) does not
telescope.

They wrote the identity with
frozen \(\lambda_e\). This page
keeps that. Do not mix the
two writings. Live \(\Lambda\)
in \(T_c=M-\Lambda N\) is the
physical barycenter. It is
not a second epoch center.

---

## Gradient at the core —
checked here

Heterochiral coefficient

\[
R
=
\frac{(i+o)(j+o)}{2o}(H-\Lambda),
\qquad
H=i^2+j^2+o^2+ij-o(i+j).
\]

At the core \((i,j,o)=(\kappa,\kappa,\kappa)\),
\(\Lambda=\kappa^2\):

\[
R=2\kappa^3,
\qquad
\nabla_{(i,j,o)}R=(5\kappa^2,5\kappa^2,0),
\qquad
\frac{\partial R}{\partial\Lambda}=-2\kappa.
\]

Finite-difference lock today.
The incoming first-order
\(\lvert R-2\kappa^3\rvert\le 5\kappa^3 L r+O((Lr)^2)\)
is the same linearization.
The Chebyshev tail bound and
the exact factor of two are
**not reproduced**. Those
sentences live in a gate file
that is not in this repo.

Signed-sum \(\to\) sum of
absolute values stays the
forbidden step. Diagnosis of
(4) stands.

---

## What it costs — all OPEN

Cauchy–Schwarz:
\(\Lambda^2 E\ge Y\), equality
on one shell.

\(\Phi_e/Y\) is not controlled
by \(r^2\) alone. On the unit
torus the press bounds it by
\(W_{\lambda_e}\):
[`PRESS.md`](PRESS.md).

- At the shell, \(\varphi_e=0\).
  Near the shell,
  \(\varphi_e\sim\tfrac52\kappa^2\delta^2\),
  so \(\Phi_e/Y=O(r^2)\).
- Low modes:
  \(\varphi_e(0)=\kappa_e^4\).
  That term is \(\kappa_e^4 E_{\mathrm{low}}\).
  A thin low tail can keep
  \(r\) tiny and still make
  \(\Phi_e/Y\gg r^2\).
- High modes:
  \(\varphi_e\sim m^4/2\), so
  \(\Phi_e/Y\sim 1/2\).
  Not small.

The tail can hold small
\(X\)-mass and small \(Y\)-mass
and essentially all of
\(\mathcal D_s\). Tail leakage
competes directly with
\(\nu\mathcal D_s\) on the tail.
That is the BROAD missing
estimate again.
[`WIDTH.md`](WIDTH.md).

Integrate \(\Phi_e'/Y\) by
parts and the returned term
is \(\Phi_e Y'/Y^2\). That is
absorbable only if
\(\Phi_e/Y\) is small. The
tail reappears exactly there,
as the weight of \(\varphi_e\)
away from the shell.

Viscous part of \(\Phi_e\) has
the good sign. Epoch resets
add a jump ledger. A jump
ledger is not a payment.
[`RESET.md`](RESET.md).

The residual is still

\[
2\kappa_e^3 Q_a
-(\Lambda-\lambda_e)N.
\]

Charge and moving \(N\) were
already leftover 4 / G4.
Filing them inside an
identity does not pay
DA-NS-2.
[`DA-NS-2.md`](DA-NS-2.md).

A finite \(\Phi_e/Y\) split
on Taylor–Green \(N=64\)
or \(N=96\) is not \(C\).
Do not run it as a close.
The algebraic split already
shows the returned term is
not small once the tail
holds mass.

---

## Endpoint — still OPEN

\[
\sup_n\int_0^T
\frac{[T_c^{(n)}-\theta\nu\mathcal D_s^{(n)}]_+}{Y^{(n)}}\,dt
<\infty
\]

is the Y-cousin target.
Tautological if \(K\) is
defined as that ratio.
Leftover 4 / G4. Not
leftover 1. Not leftover
6.
[`CENTERED-LEDGER.md`](CENTERED-LEDGER.md).

---

## What this page is not

- A close. Catalog B open
  stays 1.
- A seating of DA-NS-2.
- A restoration of ★.
- A seating of B★, LX,
  SAG, JGC, or
  \(\mathsf B^{\mathrm{prim}}\).
- A named death of G4.
- Leftover 1. Do not
  start H1.
- A reconstruction of the
  missing gate file.
- A Taylor–Green run.
- A bridge from
  \(\Psi\) to \(S_\Gamma\),
  or from \(\Phi_e\) to
  \(\Psi\).
- [`PATH-TO-CLOSE.md`](PATH-TO-CLOSE.md).

---

## Lock

The rewrite is elementary if the flux holds.
The SBP identity is not a useful K.
G4 stays OPEN. ★ stays killed.
NS not solved.
