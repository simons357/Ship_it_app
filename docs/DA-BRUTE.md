# DA brute — try every combination

`python3 scripts/da_machine.py brute`  
`python3 scripts/da_machine.py next --ask "try every combination on a quantum computer"`

A machine can exhaust a **finite list**. The missing
write is **all-data** (or every zero). Trying every
known attempt is a library. Combining them is glue.

A quantum device does not change the object.

```
  A MACHINE CAN EXHAUST                    THE WRITE IS
  ---------------------                    ------------
  finite n box                             all smooth data
  papers already seated                    one all-data integral
  Q_N through N=200                        every zero of zeta
  eps in {0.2, 0.05, 0}                    uniform as eps -> 0
  knob tuples                              the same PDE (no retune)

            finite list  --------------X------------>  all-data write
                         try-every does not cross

  QUANTUM DEVICE
    superposition of guesses  !=  a theorem
    Grover searches a list    !=  int_0^T R < infinity
    Shor factors integers     !=  Re rho = 1/2 for every rho
```

This desk already tried the finite lists that sit:
Track A lemmas, Track B through B41, \(Q_N\) through
\(N=200\), living walls L55–L76. That is the permutation
you already have.

---

## Scored

| Claim | Verdict |
|---|---|
| A machine may exhaust a finite list on one slot | **pass** |
| The finite lists on this desk were already tried and scored | **pass** |
| Trying every known attempt writes the leftover | **fail** |
| A quantum computer emits the estimate instantly | **fail** |
| Every permutation of knobs is the next write | **fail** |
| Sit every paper and vote the missing line | **fail** |
| A supercomputer at \(n=64\) is regularity | **fail** |
| A new all-data write may sit later; not as a sweep | **open** |
