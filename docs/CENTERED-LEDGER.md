# Incoming centered ledger — scored

24 September 2026.
**§1 identities already sit.
DA-NS-2 is the Y-cousin target,
not a theorem.
α+β=Λ is empty on two shells.
SAG / JGC are not seated here.
Paste truncated at §17.
Not a close. ★ stays killed.
Catalog B open stays 1.**

Overnight page:
[`DA-NS-2.md`](DA-NS-2.md).
Target:
[`CENTERED-DRIFT.md`](CENTERED-DRIFT.md).
Triangles:
[`FOURIER-TRIANGLE.md`](FOURIER-TRIANGLE.md).
Short interval:
[`INTERVAL.md`](INTERVAL.md).
Chart reset:
[`RESET.md`](RESET.md).
Relative width:
[`WIDTH.md`](WIDTH.md).
SBP / \(\Phi_e\):
[`SBP.md`](SBP.md).
Press (Lemma A sits;
Lemma B OPEN):
[`PRESS.md`](PRESS.md).
Sign gate:
[`SIGN-GATE.md`](SIGN-GATE.md).
Machine: `python3 scripts/centered_ledger.py`.
Does not overwrite `stokes_moments.py`.
Do not start leftover 1.
Do not weld \(\star\).
Do not cash B★ as G5.
Do not glue DA-NS-2 to H1.

---

## Three kinds of sentence

**Exact.** Algebra already on
this desk, or locked today
against the sitting gap
formula.

**Numerical.** Their printed
networks and optimizations.
Finite is not \(C\).

**Not on this desk.** SAG
static assembly, loop
certification, helical
176 / 4272 match, JGC
epoch budget as a write.

---

## §1 Centered core — already
PROVED here

\(X,Y,Z,\Lambda=Y/X\),
\(T_c=M-\Lambda N\),
\(\mathcal D_s=Z-\Lambda Y\ge 0\),
\(\Lambda'=2(T_c-\nu\mathcal D_s)/X\),
\((\log\Lambda)'=2(T_c-\nu\mathcal D_s)/Y\).

Their \(\tfrac12 X'+\nu Y=\mathcal N\)
is our \(X'=-2\nu Y+2N\).
Same object.
[`CENTERED-DRIFT.md`](CENTERED-DRIFT.md),
[`DA-NS-2.md`](DA-NS-2.md).

An identity is not a close.

---

## §2 Frozen endpoint — OPEN,
not a theorem

They write

\[
K_{\min,\theta}^{(n)}
=
\frac{[T_c^{(n)}-\theta\nu\mathcal D_s^{(n)}]_{+}}{Y^{(n)}}
\]

and box

\[
\sup_n\int_0^T K_{\min,\theta}^{(n)}\,dt
\le F(\nu,T,u_0)<\infty.
\]

That is the Y-remainder cousin
already named on
[`CENTERED-DRIFT.md`](CENTERED-DRIFT.md).
If it sits noncircularly and
cutoff-uniform, \(\Lambda\) stays
finite. That is G5-shaped.

It does **not** sit.
Defining \(K\) as that ratio is
tautological. Forbidden as a
proof.

This desk’s Route B packaging
uses \(K X\), not only \(K Y\).
Same hole.

**Numbering lock.** Old
[`DA-NS-2.md`](DA-NS-2.md)
called this leftover 6 / WRITE
(6). Living leftover 6 is RH.
Living leftover 1 is H1. This
integral is leftover 4 / G4.
Do not start H1 from this
page. Do not weld \(\star\).

Energy-class \(K_Y\sim\sqrt{E}\)
is already dead on \(v_n\).

---

## §3–§5 Triangle algebra

**Equal-input \(|k_\perp|^2\).**
\(\lvert k_\perp\rvert^2=\beta(1-\beta/(4\alpha))\).
\(\beta=4\alpha\) kills the
vertex. Exact. Already on the
exact-shell page.

**Two-shell product.**
\(T_c=(f(\beta)-f(\alpha))\tau_k\)
with \(f(\lambda)=\lambda(\lambda-\Lambda)\)
and \(\tau_p+\tau_q+\tau_k=0\)
is

\[
T_c=(\beta-\alpha)(\alpha+\beta-\Lambda)\tau_k.
\]

This **matches** the sitting
gap formula: on two shells
\(\alpha+\beta-\Lambda=\alpha\beta E/X\),
so

\[
T_c=(\alpha-\beta)(\alpha+\beta-\Lambda)T_\alpha
=
(\alpha-\beta)\frac{\alpha\beta E}{X}\,T_\alpha.
\]

Locked today.

**Three named zeros.**
\(\beta=\alpha\) is equal-length,
vacuous (\(T_c=0=\mathcal D_s\)).
\(\beta=4\alpha\) is flat, a
vertex zero, not a remainder.
\(\alpha+\beta=\Lambda\) is
**empty on two positive
shells**: \(\Lambda\) is a
convex combination of
\(\alpha,\beta\), so
\(\Lambda\le\max(\alpha,\beta)<\alpha+\beta\).
On many shells it can zero
one triangle. It does not
zero \(T_c\). Do not sell
three zeros as a useful \(K\).

**Unequal-length defect.**
\(T_c=[f(a)-f(c)]\tau_p+[f(b)-f(c)]\tau_q\)
is exact. Already the
three-eigenvalue stop on
[`TRIANGLE-LIFT.md`](TRIANGLE-LIFT.md).

---

## §6 Circle identity

\(w_{p,q;k}=2\rho\times(x\times y)\)
is not locked on this desk.
Their \(10^{-14}\) is a
sample. Not a remainder.

---

## §7 Static assembly — not
adopted here

Triangle / circle / star /
tree as sources of the
missing power: they report
dead, including
\(\Gamma_{\mathrm{star}}=1\).
No machine on this branch
reproduces SAG-6′.
Do not seat \(\Gamma=1\).
Do not write “mode reuse is
the leftover.”
The leftover is still a
useful \(K\), or a named
death of G4.

---

## §8 Loop gate — OPEN
elsewhere

Gauge \(\phi_k=\xi\cdot k\)
kills automatic frustration.
The prize
\(\Gamma_{\mathrm{cyc}}(N)\lesssim N^{-\delta}\)
is not a bound here.
A trend from optimization
is a lower bound. Not \(U_N\).
Even \(\Gamma\le 0.7\) would
not be a useful \(K\).

---

## §9–§15 Helical / charge

**Phase twins / orientation-only.**
Already killed on
[`DA-NS-2.md`](DA-NS-2.md):
quadratic data do not fix
\(\operatorname{sign} T_c\).

**Charge-only coercivity.**
Already killed: two-triad
witness \(Q_{a,1}+Q_{a,2}=0\)
and \(T_c^{\mathrm{het}}>0\).
Machine:
`scripts/centered_barycenter.py`.

**Heterochiral split, moving
center \(S_\Gamma\), residual
\((\Lambda-\lambda_e)\dot S_\Gamma\).**
Algebra on their network,
not a remainder. Do not
pointwise-estimate
\(\dot S_\Gamma\). They say
that. Keep it. It is not
G4.

Helical 176 / 4272 match is
**not reproduced** here.

---

## §16 JGC epoch budget —
still a target

Already named on
[`DA-NS-2.md`](DA-NS-2.md):
charge, radial, moving
center, homochiral, cross,
rotation/far, one-use
dissipation, summable
resets. All together,
cutoff-uniform.

Not sitting. A bounded
charge on one epoch is
not enough. If the last
line is \(\int\mathcal D_s/Y\),
\(\sup\Lambda\), \(\int Y\),
or a continuation norm,
stop.

---

## §17 Reset — jump now locked
from sitting \(W_K\)

The frozen variance

\[
W_K=\mathcal D_s+X(\Lambda-K)^2
=Z-2KY+K^2 X
\]

is exact and nonnegative.
Locked on this page.

The incoming paste broke at
the jump. The unique algebra
that follows from \(W_K\), with
the physical state fixed, is
now locked on
[`RESET.md`](RESET.md).
That is not a reconstruction
of their boxed text. The
remainder does not jump. A
reset is not a useful \(K\).

---

## What this page is not

- A close. Catalog B open
  stays 1.
- A seating of DA-NS-2.
- A restoration of ★.
- A seating of B★, LX, SAG,
  or JGC.
- Leftover 1. Do not start
  H1.
- [`PATH-TO-CLOSE.md`](PATH-TO-CLOSE.md).

---

## Lock

§1 sits. DA-NS-2 is OPEN.
The two-shell product sits.
α+β=Λ is empty on two shells.
Charge-only stays dead.
SAG / JGC are not seated.
G4 stays OPEN. ★ stays killed.
NS not solved.
