# H1 tube scores — first computation

Dated 10 September 2026. **Not a proof. H1 is not
proved. NS is not solved.**

SoT: [`H1-SOT.md`](H1-SOT.md).
Probe: `python3 scripts/h1_one_cylinder.py`

Two writings stay split. This is not \(A_{\mathrm{bad}}\).
This is not \(\mathcal R_\star\). This is not matrix
\(H_N\). The Ring Lemma direction bound is not used.

Numbers below are filled after the first run.
A blank here is not a close.

---

## What was asked

\(J\) and thinness on one explicit tube
(ABC / strained vortex), then whether \(J\) stays
bounded as amplitude grows.

Waiting is not scored on a snapshot. Do not cash
an imposed \(\tau\).

---

## Scores (pending first run)

The probe is in `scripts/h1_one_cylinder.py`.
File the table after the run. Do not invent it.
