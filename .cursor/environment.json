{
  "name": "Jonathan workspace",
  "install": "if [ -f requirements.txt ]; then pip install -r requirements.txt; fi; if [ -f package.json ]; then npm ci 2>/dev/null || npm install; fi",
  "ports": []
}
