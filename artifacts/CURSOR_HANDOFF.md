# Cursor handoff — Domain Architect / NS / Hilbert–Pólya

**Date:** 2026-09-15  
**Repo in this environment:** `simons357/Ship_it_app`  
**Branch that wrote this brief:** `cursor/hilbert-polya-fra-program-4642`  
**PR:** https://github.com/simons357/Ship_it_app/pull/98  
**Package:** `domain_architect` 0.4.8  

**How to start a new chat:** `Read CURSOR_HANDOFF.md and .cursorrules. Follow them.`

Paste this **full** brief into every Cursor and every repo (apps or NS). The do-nots are the point. Do not shorten the stop-rules because the current folder looks like “only apps” or “only NS.”

This is a **research-architecture freeze**, not a theory of everything, not Clay, not a proof of RH, not a canonical Simons Field Equation.

---

## 0. What this thread decided

Functional Role Analysis (FRA) is the product. Domain Architect is the auditor.

Organizing grammar (interface, **not** a cap, **not** a physical law):

\[
\Phi=\mathcal F(P,H,\psi,\lambda;E)
\]

DA decides component count from the subject (7, 8, 15, 40, …). Informal nicknames “slot / knob / five fingers” are **forbidden vocabulary**.

Statuses that must appear whenever a claim is nearby:

| Object | Status |
|---|---|
| Canonical SFE | `unresolved` |
| Riemann hypothesis | `not claimed` |
| Clay Navier–Stokes | `not claimed` |
| Hilbert–Pólya program | incomplete; no independent \(H\) with \(\operatorname{spec}(H)=\{\gamma_n\}\) |

What DA did in this thread:

1. Hilbert–Pólya completeness audit (`--hilbert-polya`). Circular fills refused.
2. Unfiltered Pólya dump (`--polya-probe`). George Pólya’s **theorems** survived the filter. Hilbert–Pólya / Berry–Keating / Connes did not complete. GUE is universality, not identity. Harmonic oscillator dies on \(N(T)\). Liouville-sum conjecture is **false**.
3. Millennium look (`--millennium-look`, `--pair`). Rhymes, not solutions. Green / Biot–Savart: `COMPATIBLE_DISTINCT`. Two \(\Phi\)s: `INCOMPATIBLE`.
4. Breakdown children (`--breakdown-children`). Refusals are **path guidance**, not leftover noise.
5. Kept NS model entered (`--ns-model`). DA accepted it as a **fluids book**. KEEP algebra in \(E\). Prize output empty. Barrier still \(\|u^r/r\|_\infty\).
6. Odlyzko–Pólya origin dump (`--odlyzko`). 1914 remark KEEP as a remark, not \(H\). Hilbert side empty. GUE chain already called weak by Odlyzko.
7. SND \(\Rightarrow\) regularity implication written (`docs/ns-review/SND-TO-REGULARITY.md`). Shell condition controls \(J/X\) only. Frequency drift of \(j_*\) is extra data. Written Theorem H / Theorem D assume \(X\le M\). Theorem D stays retired. No new numerical sweeps.

---

## 1. Three-sets rule (hard)

Never glue these three independently specified **sets** into one object. Route between them. Do not derive one from another by shared letters, shared words (“spectrum”, “heat”, “\(\Phi\)”), or cosmic analogy.

| Set | Name | What it is | What it is not |
|---|---|---|---|
| **A** | Auditor | Domain Architect / FRA in `domain_architect/` | A physical law, a public Base44 app, a proof engine |
| **B** | Fluids | Classical unaugmented NS; kept swirl algebra (NS-Φ) | Track A, SFE, Clay closed, Hilbert–Pólya |
| **C** | RH strategy | Hilbert–Pólya program + George Pólya theorems | A Hamiltonian already found; Clay NS; SFE |

**Sub-rule — three \(\Phi\) names.** Keep all three. Never write \(\Phi_{\mathrm{FRA}}:=\Phi_{\mathrm{swirl}}\) or merge either with Riemann’s kernel \(\Phi\).

| Name | Meaning |
|---|---|
| FRA \(\Phi\) | realized output of the instance (for NS: globally regular \(u(t)\), **unfilled**) |
| \(\Phi_{\mathrm{swirl}}=u_\theta/r\) | intensive swirl; KEEP algebra |
| Riemann kernel \(\Phi\) | Pólya 1926 cosine-transform kernel for \(\xi\) |

**Sub-rule — KEEP / PARK / REVIEW** (Zenodo inventory, separate from the three sets above):

- **KEEP** = cite (algebra, honest conditional statements).
- **PARK** = history / analogy / withdrawn prize glue. Do not green as proof.
- **REVIEW** = human call still needed. Do not auto-KEEP.

---

## 2. Hard do-nots

### 2.1 Hamiltonian

- Do not invent \(H\) from the zeros: \(\Phi:=\{\gamma_n\}\) or \(H=\operatorname{diag}(\gamma_n)\) is a **circular fill** (`NULL-HP-CIRCULAR`).
- Do not treat NS coupling \(H\) (convection / stretching / Stokes) as a Hilbert–Pólya Hamiltonian.
- Do not import inverse-GCD / Fock \(H_N\) as this \(H\).
- Do not merge Berry–Keating, Connes, GUE, and \(\operatorname{diag}(\gamma_n)\) by the letter \(H\).
- Do not use the harmonic oscillator: \(N(T)\) kills equal gaps (`NULL-HP-OSCILLATOR`).
- Do not skip **HP-S5** (spectral identity). Completeness of the *map* is not RH.
- Do not construct \(H\) from DHFA dynamics. Dynamics are downstream.
- Self-adjointness and \(\mathcal H\) cannot be declared after the spectrum is chosen from the zeros.
- Quantum Hilbert–Pólya fitted by **expansion** (\(\mathcal H\), inner product, \(\mathcal B\), \(D\), \(\Xi=0\), time-reversal), not by squeezing into five letters.

### 2.2 Extra viscosity is not Clay NSE

- Track A is a **different PDE**: extra dissipation \(\varepsilon^\alpha\mathbb P_{\mathrm{Leray}}\operatorname{div}(\lvert\nabla u\rvert^\beta\nabla u)\) (Ladyzhenskaya / \(p\)-Laplacian style).
- Global smoothness of Track A does **not** finish classical unaugmented 3D NS (Clay Statement B).
- Do not let the augmented track finish the unaugmented one.
- \(Q_1\)-augmented notes that use the KEEP identity are still not Clay.
- Lions \(\beta=0.6\) / \(\dot H^{1.3}\) relabel is **bookkeeping**. It does not repair the barrier. \(\dot H^{2.6}\) as an energy label is a mislabel (audit 22 Aug 2026).

### 2.3 Two Pólyas (do not collapse)

Keep **George Pólya** (theorems) distinct from **Hilbert–Pólya** (conjecture / strategy).

| Object | Status in DA |
|---|---|
| George Pólya theorems (LP class, Pólya–Schur, 1926 cosine criterion, PF kernels, 1918/1923 zeros, 1921 walk, membrane, rearrangement, …) | **Survived** the filter as usable objects |
| Hilbert–Pólya conjecture \(\operatorname{spec}(H)=\{\gamma_n\}\) | **Incomplete** |
| Pólya 1926 Acta integral representation of \(\xi\) | **Distinct** from the 1926 cosine-zero criterion; do not merge (`NULL-HP-POLYA1926`) |
| de Bruijn–Newman \(\Lambda\) | Continues 1926; \(\Lambda\ge 0\) theorem; RH \(\Leftrightarrow\Lambda=0\); do not close by quoting RH |
| Liouville-sum “Pólya” conjecture | **False** (`NULL-POLYA-LIOUVILLE`). Pólya is not an oracle |
| GUE | Universality / laboratory, not \(\operatorname{spec}(H)=\{\gamma_n\}\) (`NULL-HP-GUE`) |

Speech-to-text: Cole / Paulo / Polly = **Pólya**.

### 2.4 Other glue that is already refused

- SFE / UHF / DHFA \(\to\) NS or RH.
- Triple Lock, Bridge*, ARCHON “RH proved”, Millennium-from-SFE.
- CMB / Saturn / cosmic lattice as a proof of the swirl identity.
- Lemma★ collapse: Φ-renorm \(\Rightarrow\) Lemma★ \(\Rightarrow\) Clay B. **Refuse.**
- Viscous \(\nu\Delta\omega\) identified with de Bruijn–Newman heat on \(\Xi\).
- Shared word “spectrum” as a transformation.
- Base44 UI tables as a Domain Architect pass.
- Informal product language: slot, knob, five fingers.

---

## 3. The good NS model (what DA actually did)

The good model is **KEEP Φ-renorm / axisymmetric-with-swirl**, classical unaugmented NS.

Identity (algebra, not cosmology), \(r\neq 0\):

\[
\Gamma=r u_\theta,\qquad
\Phi_{\mathrm{swirl}}=u_\theta/r,\qquad
r^{-4}\partial_z(\Gamma^2)=\partial_z(\Phi_{\mathrm{swirl}}^2).
\]

`lem:Phieq` is correct (no spurious \(-\Phi/r^2\)).

**Open barrier:** \(\|u^r/r\|_\infty\) uniform in \(\varepsilon\) (`op:gronwall` / eq:B1). Equivalent in difficulty to swirl global regularity. Conditional reduction only. **Not Clay.**

When entered (`python -m domain_architect --ns-model`):

- DA accepts it as a **fluids book**. It does not become SFE or RH.
- \(\Phi_{\mathrm{swirl}}\) is renamed so it cannot occupy FRA \(\Phi\).
- KEEP identity sits in \(E\) as algebra. Prize output stays empty.
- Load-bearing gap is \(\|u^r/r\|_\infty\), not a missing Hamiltonian.
- Instance name: `kept-ns-swirl`. 16 components (5 interface + 11 extras).
- PARK list refused, not ingested.

Live narrative: `results/ns-model.txt`.  
Write-up: `docs/domain-architect/08-NS-MODEL.md`.  
Honesty card: `docs/ns-review/PHI-RENORM-WHAT-IS-KEPT.md`.  
Audit: `docs/ns-review/PHI-RENORM-AUDIT-2026-08-22.md`.  
TeX SoT: `docs/papers/swirl/Simons_PhiRenorm_Swirl_2026-06-30.tex`.

---

## 4. File paths (this repo)

Paths marked **this branch** are on `cursor/hilbert-polya-fra-program-4642`. Paths marked **other branch** exist in `Ship_it_app` git history but are **not** on this freeze; fetch the named branch before claiming they are missing. Do not invent replacements.

### 4.1 VORTEX / swirl / unaugmented NS

**This branch**

| Path | Role |
|---|---|
| `docs/ns-review/PHI-RENORM-WHAT-IS-KEPT.md` | KEEP / PARK card |
| `docs/ns-review/PHI-RENORM-AUDIT-2026-08-22.md` | 22 Aug 2026 audit; barrier open |
| `docs/papers/swirl/Simons_PhiRenorm_Swirl_2026-06-30.tex` | June 30 conditional paper |
| `docs/domain-architect/08-NS-MODEL.md` | What DA did with the kept model |
| `docs/ns-review/SND-TO-REGULARITY.md` | SND-U shell share; drift gap; \(X\le M\) circularity. Not Clay |
| `domain_architect/ns_model.py` | `--ns-model` |
| `data/domain_architect/historical_equations.json` | `NS-H001` vorticity, `NS-H002` KEEP, `NS-H003` Stokes, `NS-H004` Biot–Savart |

**Other branches (do not assume present here)**

| Path | Typical branch |
|---|---|
| `docs/UNAUGMENTED-R4-VORTICITY-PLAN.md` | `cursor/unaugmented-r4-vorticity-f80e` |
| `docs/SWIRL-PAPER.md`, `docs/H1-SOT.md`, `docs/H1-TUBE.md` | same |
| `docs/DA-LEADS.md`, `docs/DA-DESK.md`, `docs/LITERATURE-H.md` | same |
| `jonathan-handoff/GROK-HEAVY/01-swirl-publishing/` | five-lane / Lemma★ pack |
| `docs/AUGMENTED-NS-PROOF-CHAIN.md` | Track A (different PDE) |
| `docs/math/TAO-MATH-PANEL-SND-H.md`, `docs/math/SND-H-STATUS.md`, `docs/papers/SND_RING_LEMMA_NS.tex` | `cursor/tao-snd-h-panel-a0eb` (SND-U / Theorem H sources) |
| `docs/papers/submit/03_t2_shell_flux_gronwall.tex` | same (T2 \(\rho\); opposite packaging) |
| `docs/ns-review/SND-TWEET-DA-AUDIT.md` | `cursor/da-snd-gap-closure-0cc5` |
| `docs/TRACK-B-CLIMB-LAW.md` | `cursor/unaugmented-r4-vorticity-f80e` (barycenter drift; not an a priori) |

There is **no** file named `docs/VORTEX.md` in this checkout. Vortex-stretching literature is **not** a bound on classical \(X=\|\omega\|_2^2\). Gibbon identities are diagnostics. A forced-vortex picture is not unaugmented GR.

### 4.2 PÓLYA / Hilbert–Pólya

**This branch**

| Path | Role |
|---|---|
| `docs/domain-architect/06-HILBERT-POLYA-PROGRAM.md` | Completeness audit; not a proof of RH |
| `docs/domain-architect/07-POLYA-PROBE.md` | Full dump; filter scoreboard |
| `domain_architect/hilbert_polya.py` | `--hilbert-polya` |
| `domain_architect/polya_probe.py` | `--polya-probe` |
| `domain_architect/millennium_overlap.py` | `--millennium-look`, `--pair` |
| `domain_architect/breakdown_children.py` | `--breakdown-children` |
| `domain_architect/odlyzko_origin.py` | `--odlyzko` (1914 remark; Hilbert empty) |
| `results/polya-probe.txt` | Live narrative |
| `results/millennium-look.txt` | Live narrative |
| `results/pair-run.txt` | Green/Biot–Savart + \(\Phi\) collision |
| `results/breakdown-children.txt` | Children + path guidance |
| `results/odlyzko-polya.txt` | Origin dump narrative |
| `data/domain_architect/null_results.json` | `NULL-HP-*`, `NULL-POLYA-LIOUVILLE` |

Registry HP ids: `HP-H001`–`HP-H028` (`HP-H026` = 1914 remark; `HP-H028` = Hilbert origin empty).

### 4.3 Quantum Q&A

Quantum Hilbert–Pólya in **this** thread is answered inside the Pólya probe: fit by expansion, not by five letters. See `results/polya-probe.txt` (“How quantum fitted”) and `docs/domain-architect/07-POLYA-PROBE.md`.

**Other branches (gravity / quantum pairs, not a Hilbert–Pólya close)**

| Path | Typical branch |
|---|---|
| `docs/DA-GRAVITY-QUANTUM.md` | `cursor/unaugmented-r4-vorticity-f80e` |
| `docs/DA-FLUSH.md`, `scripts/da_flush.py`, `scripts/quantum_flush.py` | same |
| `docs/papers/zenodo-spectral/…/e39ad1642_QUANTUM_MILLENNIUM.tex` | historical; **PARK** prize-glue risk |

Quantum Lens / prime-manifold Hamiltonian: **off the desk**. Do not load into \(\omega\cdot S\omega\). Semiclassical GR+QFT coupling is a different book (`DA-GRAVITY-QUANTUM.md`): do not glue SM gauges onto it and call that RH or NS.

### 4.4 Domain Architect core (this branch)

| Path | Role |
|---|---|
| `domain_architect/` | Auditor package |
| `docs/domain-architect/00-AUDITED-BASELINE.md` … `08-NS-MODEL.md` | Human notes |
| `docs/domain-architect/04-NOTATION-COLLISIONS.md` | \(P,H,\Phi,\lambda\) collisions |
| `data/domain_architect/{historical_equations,conflicts,null_results}.json` | Immutable originals |
| `artifacts/CURSOR_HANDOFF.md` | This brief (source copy) |
| `CURSOR_HANDOFF.md` | Same brief at repo root |
| `artifacts/CURSOR_DA_RULES.md` | Stop-rules source |
| `.cursorrules` | Copy of the stop-rules |

### 4.5 Donner MIDI map

**Not in this checkout.** No Donner / MIDI map file is on this branch or on the NS/DA branches searched for this freeze.

Do **not** invent channel / CC maps. If the apps Cursor has a Donner MIDI map, paste it **verbatim** under `artifacts/DONNER_MIDI_MAP.md` and keep a copy next to this brief. MIDI is not a proof of NS, RH, or SFE.

---

## 5. Leray class names (do not rename)

Write the class you are in. A different class is a different theorem.

| Name | Use |
|---|---|
| \(\mathbb P_{\mathrm{Leray}}\) | Helmholtz / Leray projector on divergence-free fields. **Not** FRA \(P\), **not** primes |
| Leray–Hopf weak solution | Energy inequality; 1934. Energy does **not** give \(\|\omega\|_\infty\) |
| Local Leray (Lemarié-Rieusset) | Locally finite energy, no decay at infinity. Not a bound on global \(X\) |
| Forced Leray–Hopf (Albritton) | Different equation once \(f\) is on. Not unforced Clay |
| Unforced Leray–Hopf / CAP (Hou–Wang–Yang, announced) | Different class. Not a bound on smooth \(X\) |
| Very weak / Serrin without differentiability (Farwig) | Not Leray–Hopf |
| Axisymmetric-with-swirl (smooth on the axis) | The **kept** class for NS-Φ. Global regularity **open** |
| Track A augmented | Extra viscosity. Not Clay NSE |
| Mild / Koch–Tataru small critical | Small data. Not all-data \(X\) |
| Wild weak / convex integration | Different class. Not smooth blowup |

Clay Statement B in this program, when it is mentioned at all, is **unaugmented classical** regularity (restricted to swirl in the kept model). It is **not claimed**.

---

## 6. Base44 is not public DA

| Thing | What it is |
|---|---|
| **Public / canonical DA** | This Python package: `python -m domain_architect`. Notes in `docs/domain-architect/`. There is **no** separate public Domain Architect website on this freeze. GitHub Pages for the repo (when present) is not DA. |
| **Base44** | Hosted apps with a **different schema**. Examples in sister work: ChatVault glass `preview--6a58e103….base44.app`, Cosmo `cosmoevolution3d.base44.app`. The Base44 **editor** is a login wall; do not send it as “the app.” |
| **ChatVault** | Search / vault engine (other branch). Inquiry is DA; search is ChatVault. Not the same engine. Do not drain Route C / RH / swirl / unaugmented NS faces into the vault as if DA proved them. |

Do not treat a Base44 16/16 table, glass Conversation list, or Cosmo UI as a Domain Architect pass. Do not glue those catalogs to FRA.

Missing historical zip `Domain_Architect_Working_App_v1_4.zip` is still **not** in this repository. Do not fabricate it.

---

## 7. What not to assume

- That five FRA letters are a cap, or that a completed interface map is RH or Clay.
- That GUE spacing \(\Rightarrow\) \(\operatorname{spec}(H)=\{\gamma_n\}\).
- That Pólya 1921 transience in \(d\ge 3\) is 3D NS regularity (closest rhyme only: same Green family).
- That Biot–Savart / \(R(\kappa)=1/\kappa^2\) is smoothness.
- That 2D NS regularity (no stretching) finishes 3D Clay.
- That recurrent \(d=1,2\) vs transient \(d\ge 3\) is a prize argument by itself. It is **path guidance**.
- That \(\dot H^{1.3}\) closed the barrier.
- That Lemma★, Track A, or SFE is the kept NS model.
- That this environment contains a second “apps” git repo. **This VM has only `Ship_it_app`.** Paste the same brief into the other Cursor/repo by hand if it lives elsewhere.
- That Base44 is DA.
- That a Donner MIDI map exists here.
- That files on `cursor/unaugmented-r4-vorticity-f80e` or `cursor/domain-architect-app-f96b` are on this branch.

---

## 8. Commands (this branch)

```bash
python -m domain_architect "∇²Φ = 4π G ρ"
python -m domain_architect --registry
python -m domain_architect --hilbert-polya
python -m domain_architect --polya-probe
python -m domain_architect --millennium-look
python -m domain_architect --pair
python -m domain_architect --breakdown-children
python -m domain_architect --ns-model
python -m domain_architect --odlyzko

python -m unittest \
  tests.test_domain_architect_acceptance \
  tests.test_domain_architect_units \
  tests.test_hilbert_polya \
  tests.test_polya_probe \
  tests.test_millennium_overlap \
  tests.test_breakdown_children \
  tests.test_ns_model \
  tests.test_odlyzko_origin
```

---

## 9. Next steps (honest)

**NS path (set B)**

- Work the live barrier: uniform-in-\(\varepsilon\) control of \(\|u^r/r\|_\infty\).
- Or a stretching bound on this class that does **not** assume the barrier.
- If Track A is used, **say so**; it is a different PDE.
- Give swirl a name other than \(\Phi\) when it sits next to FRA output or Riemann’s kernel.
- SND \(\Rightarrow\) regularity: `docs/ns-review/SND-TO-REGULARITY.md`. Next math is M-free flux and a drift law for \(j_*\), **not** more DNS. Do not green Theorem D. Do not merge SND-U with T2 \(\rho\).

**RH path (set C)**

- Independent self-adjoint \(H\) on a declared \(\mathcal H\), **or**
- an RH-free LP / 1926 cosine / PF / Turán check on Riemann’s \(\Phi\) or \(\xi\), **or**
- \(\Lambda\le 0\) without assuming RH.
- If a map to NS is claimed: an explicit checked transformation, not the letter \(\Phi\) and not the word spectrum.
- Do not skip HP-S5.

**Auditor (set A)**

- Keep refusing circular fills, oscillator, GUE-as-identity, three-way \(\Phi\) merge, SFE→NS.
- Do not revive retired SFE-PUB / SFE-QM / SFE-HAM as canonical.
- Two Cursors share this file so they do not drift.

**Apps Cursor (if separate)**

- Paste this whole brief anyway.
- Base44 \(\neq\) public DA.
- If a Donner MIDI map exists there, copy it into `artifacts/DONNER_MIDI_MAP.md` here without treating it as physics.

---

## 10. Identity lock

- Author of the research program: Jonathan Simons.
- Domain Architect is software, not a chat persona and not Cursor.
- Representation of a known equation is not derivation.
- Refusals are path guidance.
