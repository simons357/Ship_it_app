# DECISIONS.md

**Date:** 24 August 2026  
**Repo:** `github.com/simons357/Ship_it_app` (workshop). This is **not** a dedicated ChatVault product repo. A fresh GitHub repo was requested; this agent cannot create one.

**Which build won:** Jonathan reversed the morph-glass look lock (25 Aug 2026) and then asked for **multiple skins**, not glass. Live UI in `chatvault/` ships four crisp skins via CSS variables on `:root[data-skin=…]`: **Steel** (default charcoal `#101112` + amber `#ffbf1a`), **Ink** (near-black + ice cyan), **Signal** (navy + magenta), **Day** (warm paper + cobalt). Switcher is in the sidebar; persist key `chatvault.skin.v1`. **Glass is forbidden** as a selectable skin — no `backdrop-filter`, no morph-grain overlay. Tagline stays **OS for your AI**. The git tree `chatvault/` remains the canonical **engine / data model** (ChatVaultEntry, CLAIM_LEDGER, BM25F fielded retrieval). Glass remains archived as a Base44 look, not shipped chrome.

**Search engine:** Public GitHub “ChatVault” repos are other people’s products. This tree ships `chatvault/js/search.mjs` — hybrid RRF over BM25F, character n-grams, TF-IDF cosine, stems/typos, and RM3. Ledger status is never a ranking signal. `origin:ai` / `origin:human` designate AI chats vs real records and are not unfielded quality boosts. Domain Architect’s homepage search dock opens this engine (`python -m domain_architect --site`). E8/lattice ranking is **EXPERIMENTAL / not shipped**.

**Brand marks:** The owner vault-door logos stay local files under `chatvault/assets/` (light original upload, dark UI mark; morph-glass grain kept on disk, not used as the live backdrop). Do not replace them with a CSS dial. Do not hotlink Base44.

**Origin (26 Aug 2026):** every record has `origin_class` `ai_generated` | `human_record`. Search `origin:ai` / `origin:human` is a match gate, not a score. Domain Architect is the **inquiry** box (`POST /api/inquiry`); ChatVault is the **search** box. A finished FRA inquiry can file into ChatVault JSON (`--drain-chatvault` or Inquire + file). DA is not this vault’s brain and does not prove NS/RH. Finished chats drain via `chatvault-export` JSON or loopback `127.0.0.1:7847`. Any source (sounds, video, papers, letters) files as a human record. Browser movies/audio remain stubs; `python3 -m domain_architect --ingest-chatvault PATH` writes JSON sidecars into `chatvault/inbox/` and copies media locally. Operational report: `docs/chatvault-audit/CHATVAULT-OPERATIONAL.md`.

**Why not let Base44 glass win outright:** It holds ~14 conversation records, but they read as SaaS seed data (rate limiter, CRM, investor deck), not Jonathan’s research corpus. Seed data must be archived, not used as “live content wins.” Its `Conversation` schema has no claims, theorems, gaps, or ledger. Rebuilding that model later is the expensive mistake. C2 is a landing page plus Drive OAuth. C3 is an empty paper vault. Claude’s original is historical and still not in this workshop.

**What this agent refuses:** exporting Base44 by guessing credentials; tagging `Ship_it_app` `main` as `v0.1-canonical`; fabricating `build-a` / `build-b-glass` / `build-legacy` branches without source (HTML snapshots already live under `docs/chatvault-audit/evidence/`); turning on Stripe; wrapping the Base44 preview in Capacitor; deleting any Base44 record.
