Media copies from `python3 -m domain_architect --ingest-chatvault`.

Gitignores video, audio, and PDF here. JSON sidecars live in the parent
`inbox/` directory and are what git holds. Do not commit secrets.
