# DA gate — arithmetic sign realizability

**24 September 2026.** Static frontier after the locked low-tail
gate. This filing does **not** alter

[`DA-GATE-RMS-CORE-TAIL-SBP-2026-09-24.md`](DA-GATE-RMS-CORE-TAIL-SBP-2026-09-24.md),
[`DA-GATE-PHI-VS-FROZEN-VARIANCE-2026-09-24.md`](DA-GATE-PHI-VS-FROZEN-VARIANCE-2026-09-24.md),
or
[`DA-GATE-LOW-TAIL-SNAPSHOT-2026-09-24.md`](DA-GATE-LOW-TAIL-SNAPSHOT-2026-09-24.md).

Lock: `scripts/da_gate_arithmetic_sign_realizability.py`.
Rally: [`../docs/ns-recovery/CENTERED-RESIDUAL-BOARD.md`](../docs/ns-recovery/CENTERED-RESIDUAL-BOARD.md).

**Not a closure theorem.** Ordinary NS is not solved. Soft X silent.
Do not put \(K(t)\) in the PDE.
No more potentials. No more reinterpretation of the same clock.
The neighboring-shell family is **not on this tree** and is not
invented. The outcome tree is armed, not executed.

---

## Notation safeguard — EXACT split; first variation is not the split

Write \(\delta_k=\lambda_k-\Lambda\) and let \(T_k\) be the modal
transfer. The centered definition \(T_c=M-\Lambda N\) expands
uniquely as

\[
T_c
=
\Lambda\langle\delta,T\rangle
+\sum_k\delta_k^2 T_k.
\]

That identity uses the **live** \(T_k\) of the state. In a discrete
neighboring-shell deformation those \(T_k\) also vary.

For the first variation, the object is

\[
L_{1,N}
=
\Lambda_N\langle\delta_N,T_N^{(0)}\rangle,
\]

provided the family is the asymptotic neighboring-shell family, so
the transfer variation contributes only at the next order.
\(T_N^{(0)}\) is the undeformed / leading transfer. It is **not**
the live neighboring \(T_k\).

Heavy must retain the actual neighboring \(T_k\) separately as a
remainder check, and must **not** silently substitute \(T^{(0)}\)
into the full finite-gap identity.

Consistency test:

\[
R_{2,N}
:=
T_{c,N}
-\Lambda_N\langle\delta_N,T_N^{(0)}\rangle.
\]

A legitimate first-variation sequence must show the predicted
higher-order behavior of \(R_{2,N}\). If it does not, the
deformation changed polarization, amplitudes, or topology too
strongly to represent the intended gate.

Silent substitution \(T^{(0)}:=T_{\mathrm{live}}\) collapses \(R_{2,N}\)
to the exact quadratic remainder \(\sum\delta_k^2 T_k\). That is a
notation trap, not the first-variation check. The lock on this book
records that trap and refuses it.

---

## Outcome tree — ARMED, not run

Four categories. Do not collapse them.

### BOTH SIGNS, heterochiral, persistent

If Heavy obtains a family with

\[
\lvert\rho_N^\pm\rvert\ge c>0,
\]

then

\[
\textbf{universal first-order one-sided narrow depletion is false}.
\]

Stop static closure work on this question. The positive branch goes
directly into evolution. Then

\[
\Theta_N=\nu\kappa_N^2\tau_{U,N}
\]

is one of the most important numbers in the project. \(\tau_{U,N}\)
is **named, not constructed here**.

Save the actual integer vectors, helicity labels, polarizations,
and amplitudes — not merely \(\delta\) and \(T^{(0)}\). That object
is the canonical adversarial seed

\[
\mathcal A_N^{+}
\]

for the dynamic phase: an exact, reproducible, increasingly narrow
field that is statically pointed in the dangerous direction. The
NSE itself gets the next move.

### ZERO ONLY, homochiral

Consistent with the Vandermonde delay. It does **not** rescue the
full NSE. Heterochiral remains separate.

### ONE SIGN, heterochiral

Then ask whether the restriction is encoded by the Gram / lattice
realization problem. That would be the first serious bridge from
the \(I_3\) arithmetic
([`../docs/ns-recovery/FOURIER-TRIANGLE-GEOMETRY.md`](../docs/ns-recovery/FOURIER-TRIANGLE-GEOMETRY.md))
into the centered dynamics rather than merely into occupancy.
Do not invent that bridge here.

### NO NEIGHBOR

Its own category. Arithmetic rigidity is **not** sign depletion.

---

## Board freeze

\[
\textbf{STATIC FRONTIER: arithmetic sign realizability}
\]

\[
\Downarrow
\]

\[
\textbf{DYNAMIC FRONTIER: dangerous-state persistence}
\]

The locked low-tail / charge / epoch-motion last mile is not
reopened and is not rewritten. This gate is the static job.
Heavy’s neighboring-shell family is not on this tree, so no
verdict is stamped.

---

## What this does not do

It does not alter the SBP, \(\phi/d\), or low-tail snapshot gates.
It does not invent the neighboring-shell family or \(\mathsf B^{\mathrm{prim}}\).
It does not substitute live \(T_k\) for \(T^{(0)}\).
It does not treat NO NEIGHBOR as depletion.
It does not obtain DA-NS-2.
It does not stamp BOTH / ONE / ZERO signs.

No new estimate is claimed. No continuation criterion.
**NS not solved.**
